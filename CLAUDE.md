# QilinStory 專案指南

武俠遊戲《異麒麟：旭日初升》的敘事文本專案。時代：東漢光和二年（179 年）。全案繁體中文。

## 動筆寫任何劇情文本之前，必讀三檔

1. `給AI看的指南/文本創作指南.md` — 主控文件（第零層位階最高：高概念、引擎、關卡規則、[panel]/[em7] 指令、不寫內心話）
2. `給AI看的指南/世界觀.md` — 宇宙層設定與寫作紅線（標記：已定／暫定／未定／舊案／本劇不揭）
3. `給AI看的指南/武俠文風創作指南.md` — 文風準則（口吻、聲口卡、詞彙紅綠燈、標竿範例、自檢清單）

事後審稿與逐詞查表用 `給AI看的指南/武俠文風審校清單.md`。

## 檔案分工

- `劇情/` — 劇本主工作區（.md）。**文風創作發生在這裡**；`Json/` 是轉譯產物（轉換規則見 `劇情/轉成json指南.md`），不手寫。
  - **分層（2026-08-28 起，逐步施工中）：** 一個箱庭一個資料夾 `劇情/<箱庭名>/`，**一份 JSON 一個 .md**，檔名為兩位數序號＋JSON 名（序號＝舊單檔的 §N，所以既有「`小溪村.md` §4-5」這類指路仍讀得通）；`00 總覽.md` 收檔頭、收錄 JSON 表、說話者對照與分章索引。**單 JSON 的箱庭**資料夾內就一份 .md、維持原檔名不編號，也沒有 `00 總覽.md`。**`Json/` 有子資料夾的照抄**（`劇情/英豪府/` 底下再分 `英豪府遴選/`、`英豪府遴選(後)/`）；**`Json/大地圖/` 那層不做**，箱庭平放在 `劇情/` 第一層。**14 個箱庭已全數改制完成（2026-08-28）**。
  - **養成模式事件（2026-08-28）：** `劇情/<年>年事件/`，**一個月一個檔、主線與支線同月合檔**，檔名 `<兩位數>月(主)<事件簡稱>(支)<事件簡稱>.md`（月份補零好排序，簡稱是為了從檔名就找得到戲）。檔內以 `## 主線｜N.` `## 支線｜N.` 分節，N 沿用舊 `<年>年 主線／支線事件.md` 的 §N。**一個月檔對應兩份 JSON**（`主線事件/`＋`探索事件/`），改一份就重讀該檔的那一節。
  - **人物線：一個角色一個資料夾 `劇情/<角色>/`（2026-08-29 起，逐步施工中）。** 資料夾內 `<角色>.md` 是**設計端總覽**（整條線怎麼設計、事件連結、哪裡加好感、引擎、相關地城、待辦），**事件劇本各自成檔、與它同一層，並依遊玩順序冠兩位數編號**（`01 仗義疏財.md`…）；**該角色專屬的地城設定檔也搬進來，冠 `@` 前綴**（`@設定_五原障.md`）。**已改制：呂信**（七支＋設計端，原 `呂信線總綱.md` 已併入 `呂信.md`）、**赫連娜娜**（六段劇本拆檔）、**郭嘉**（2026-08-29：四支完稿搬入冠 `01`–`04`，設計端 `郭嘉.md`；第 5–8 支未寫，地城定在雁門）。**未改制的人物稿仍平放在 `劇情/` 根層**（子羽線）。**賈詡**（2026-08-30：資料夾已建，目前只有 `01 兩卷.md` 暫名；任務線形狀＝**解謎線**，四支要什麼還沒定。設計全文在 `@角色設定/賈詡.md` 末節〈設定拍板進度〉，要動筆前先讀）。
  - ⚠ **跨角色共用的支線不搬**（例：`劇情/雙世冥劍.md` 呂信＋子羽），留在根層。
  - `劇情/舊創作稿/` — 已轉 JSON 且該 JSON 已回讀成總覽稿的創作稿，**歷史版本，不編輯、不引用為現行文案、不刪**（仍是設定檔的出處）。詳見該資料夾 `README.md`。
  - ⚠ 例外：`劇情/` 底下的**文案總覽稿**（`<箱庭>.md`、`<年>年 主線事件.md`、`<年>年 支線事件.md`、`<角色>.md`，檔頭有「由 `Json/` 回讀產生」聲明）是從 JSON 手工回讀的產物，**不是創作稿**，不可直接編輯、不可回轉 JSON；哪個 JSON 在哪幾份總覽稿見 `劇情/索引.md`。規則見 `給AI看的指南/Unity對話同步流程.md`〈任務一之附件：文案總覽稿〉。
- `舊文案/` — 最早期的對話 JSON，全是歷史版本（不編輯、不引用、不刪）。哪些已被現行稿吸收、哪些完全未用可當素材，見該資料夾 `README.md` 盤點清單；`舊文案/舊Json檔/` 另見 `劇情/索引.md`〈舊扁平檔 → 現行對話〉。
- `@角色設定/` — 設定檔。角色怎麼說話另見文風指南第四節聲口卡。**`@地點/` 與 `@結局/` 已清空刪除**，內容都搬進對應的箱庭／人物資料夾並冠 `@` 前綴。
  - ⚠ **已改制箱庭的地點檔／結局檔搬進該箱庭資料夾**，冠 `@` 前綴（例：`劇情/小溪村/@設定_小溪村.md`、`@結局_小溪村.md`）。**冠 `@` ＝ 設定檔**，位階仍高於同資料夾的腳本（見下方硬紅線第一條）。**2026-08-28 已全數搬完**；**`@地點/` 與 `@結局/` 均已清空刪除**（`五原障.md` 於 2026-08-29 搬進 `劇情/呂信/@設定_五原障.md`）。
- `@事件圖/` — 天下異聞錄文案底稿（`天下異聞錄.md`）。事件圖 PNG 本體在 `圖檔/`。
- `圖檔/` — 事件圖 PNG（廢棄圖在 `圖檔/Abandon/`）。遊戲以 `EnableEventBG(圖檔ID)` 呼叫，與資料夾路徑無關；搬圖不必改 JSON。
- `給AI看的指南/` — 各種給 AI 的規則（擲骰、戰鬥、立繪、貴重品、畫面〔對話背景圖／轉場／畫面特效〕指令轉換等）。**要配樂先查 `給AI看的指南/音樂指令轉換規則.md`**（BGM／音效對照表；**情境欄由作者填，AI 不得自行判定，空白就問**，ID 不得自創）。兩地 git 交接待辦見 `給AI看的指南/跨機待辦.md`。
- Unity 同步：文案修正套進遊戲、新對話進 Unity、翻譯，一律走 `給AI看的指南/Unity對話同步流程.md`（`Json/` 是遊戲內文案的唯一事實來源，entryID 一經同步不可改號）。

## 硬紅線（違反即錯，不分場合）

- 設定牴觸時的順位：**世界觀.md ＞ 各角色／地點檔 ＞ 既有腳本**（世界觀.md 第十一節）。
- 這些詞**永遠不進遊戲文本**（連旁白、道具描述、異聞錄都不行）：五胡亂華、司馬、篡魏、三國歸晉、輪迴、重來、前世（世界觀.md 第八節）。
- 饕餮相關段落必過 `@角色設定/饕餮.md` 第十節廢案清單／停用術語。
- **世界觀.md 沒寫的就是還沒決定**：留白或詢問，AI 不得自行發明神名、封法、動機、來歷、數值。
- 新寫對白不寫內心話（文本創作指南 2.1a）；主角說話者標籤一律用 `你`。
- JSON 節點的 `title`（Unity 對話圖節點標題）是作者的：**AI 不得自行新增、修改或刪除**，新節點一律不寫此欄位（Unity對話同步流程.md 鐵則 8）。

## 慣例

- 文件內狀態標記：**已定**／**暫定**／**未定**／**廢案**；寫作紅線用 `>` 引用區塊帶 ⚠。
- 拍板直接改進 `文本創作指南.md` 正文對應節（**不再寫版本記錄**，2026-08-29 作者裁示；舊記錄封存於 `給AI看的指南/文本創作指南_版本記錄（歸檔）.md`）。
- 引擎標記（[panel]、[em7]、Sequence、ModifyData）不受文風審校約束。
