# 跨機待辦

對話不會跟 git 走；兩地只認本檔。明細仍在 `劇情/索引.md`。做完刪列或打勾。

最後更新：2026-09-07

## 現在要做

- [ ] **Unity 檢查 · 七夕事件（2026-09-07 作者新增）。** 在 Unity 裡把 `主線事件/180年/7月.json`（103 節點、入口 `#1`）整場跑過。09-07 回讀那批（表情 17、`zh_TW` 95 格）作者已匯入；這條是進遊戲檢查演出，不是再匯一次。
- [ ] **Unity 檢查 · 端午事件（2026-09-07 作者新增）。** 在 Unity 裡把 `主線事件/180年/5月.json`（84 節點、入口 `#1`）整場跑過。已進 Unity；這條是進遊戲檢查演出，不是再匯一次。

- [x] **✅ 已完成（2026-09-07）：`儺隊過街` 改列蕭靈犀線，repo 與 Unity 已同步。** `Json/蕭靈犀線/01儺隊過街.json`（36 節點，新資料夾，比照 `呂信線/`／`賈詡線/`／`赫連娜娜線/`）；`Json/探索事件/180年/1月.json` 留空節點 `#1` 當調用點。回讀稿 `劇情/蕭靈犀/01 儺隊過街.md` 已依 JSON 全段重生成、四欄位零不符。流程照作者定的「AI 先做 → 作者匯入並自行修改 → 作者匯出 → AI 回讀」跑完一輪。
  - ✅ 作者已在 Unity 把 `#34` 的 `ModifyData(FavorabilityExp,MC8,50);` 移到 `Continue()` 之前（原本排在後面可能不執行）。
  - ⚑ **兩件小事留給作者**：①入口 `#1` 的 `title` 仍是「儺隊過街」，調用點的 `Description` 卻寫「蕭靈犀(1)儺隊過街」，兩邊不一致（`title` 是作者欄位，AI 不動）；②`#34` 的 `Sequence` 有 `DisableDialogueBG();;`（多分號）與結尾 `Continue()`（少分號）兩個小瑕疵。


- [x] **✅ 已完成（2026-09-07 作者確認）：上巳改掛 181 年 3 月，Unity 養成開始月與對話已對上。** JSON＝`Json/主線事件/181年/3月.json`（78 節點、入口 `#1`），key＝`主線事件/181年/3月`。180 年 5 月主線改掛端午。BGM／音效仍未填；背景暫用 `Grassland`。

- [x] **✅ 已完成（2026-09-07 作者確認）：`呂信線/02第三百四十九次.json` 四處小修已再匯入 Unity。** 只動四個欄位，未增刪節點、`links` 未動：`#4040` `zh_TW` 多引號、`#4097` `zh_CN` 缺 `＊`、`#77` 補 `[panel=1]`、`#4092` 多餘分號。


- [x] **✅ 已完成（2026-09-07 盤點＋修正）：MC1 立繪 `pic=13`／`pic=14` 全案盤完。** 09-04 那批改判（`c8b901c`）是有效的：`pic=13` 81→46、`pic=14` 48→3。之後又漲到 63／4，2026-09-07 逐句盤了那 67 處——**62 處是合法用法**（`13`＝怒吼／宣示：武道大會七句叫陣、破廟激憤質問、觀心兩句宣示、各處「戰吧！」；`14`＝疲勞：打完架腿轉筋、拚酒舌頭發沉、剛睡醒打哈欠），**5 處誤填已改**：`黑鐵嶺礦坑` `#2054` `14`→`11`（警戒不是疲勞）、`呂信線/02第三百四十九次` `#681`／`#66` `13`→`11`（心虛／狼狽不是怒吼）、`破廟/阿傑…水井` `#3607` `13`→`1`（平靜攤牌）、`探索事件/179年/12月` `#837` `13`→`1`（敬酒客套）。現為 **59／3**。
  - ⚠ **先前記的「33 份要重新匯入」「修正被匯出蓋掉」兩句都是誤判**（統計腳本在中文路徑上靜默失敗回 0），已更正。
  - ✅ **這 5 處作者 2026-09-07 確認已匯入 Unity**（只動 `Sequence`，未增刪節點、`links` 未動）。
  - 四處「不就是大魚壓小魚？」（`180年/2月` `#4068`／`#4089`、`遴選結束` `#622`／`#3910`）維持 `13`——主角當面頂撞赤龍幫，情緒往上，算宣示。
  - **改判本身是有效的**（該 commit 刪掉 81 行 pic=13/14、只加回 1 行），剩下的 46／3 是**合法用法**：`pic=13`＝怒吼／宣示、`pic=14`＝疲勞（見 `立繪指令轉換規則.md` §4.1.2）。
  - **改判後又漲了 17 處 `pic=13`／1 處 `pic=14`**——來源是之後新寫的戲或從 Unity 匯出帶回的內容，**沒有逐句核過是不是又照舊名目表填錯**。
  - **⚠ 先前記在本檔的「修正被 Unity 匯出蓋掉」是誤判**（統計腳本在中文路徑上靜默失敗回 0），已更正。工作區與 HEAD 只差 3 個檔，沒有被蓋掉的情形。
  - **要做的事**：盤那 67 處（26 份檔），逐句判是「怒吼／宣示・疲勞」的合法用法，還是又一次照名目表誤填；只有後者要改。**不是整批重做。**
  **第一波（pic=14，46 處）15 檔：** ~~`呂信線/02第三百四十九次`~~ **（✅ 這一檔大部分已隨 2026-09-04 改制進 Unity：`#3`→`9`、`#5`／`#10`→`12`、`#23`→`11`、`#25`→`19`、`#31`→`12`、`#40`→`13`＋「！」、`#50`→`18`、`#3999` 已補 `AudioControl(StopMusic);`；`#1999`／`#2999`／`#2003`／`#3003` 隨重複段刪除已不存在。**
  **⏳ 這一檔只剩三格待你裁**（AI 不自行改，因為作者已在 Unity 手動定過這幾格）：`#4003`「三百四十八次——你跟羽兄，是誰贏得多？」仍 `pic=14`（沉吟追問 → 建議 `12`）；`#66`「你踢沙——風是往你這邊吹的」與 `#681`「……沒有，什麼都沒有。」現為 `pic=13`，但兩格都是心虛／狼狽（→ 建議 `11`，`13` 依第二波判準是怒吼宣示）。**）**、`呂信線/五原障`（15 處＋3 節點去 Disable）、`呂信線/英布`（3）、`呂信線/留信`（3）、`賈詡線/01兩卷`（含 `#20` 去 Disable）、`觀心`（6）、`大地圖/天下第一武道大會`（4＋4 節點去 Disable）、`大地圖/水濂洞`（2）、`大地圖/雲中/吉祥賭場`（2 處改 `17`）、`趙王洞/山賊窩`（3）、`小溪村/有間客棧`、`小溪村/牧羊人、茶博士`（各含後繼去 Disable）、`主線事件/180年/3月`、`探索事件/180年/3月`、`根據地/小表妹/對話(雲中客棧)`（含 `#255` 去 Disable）。
  **第二波（pic=13，82 處判完、38 處改動）新增 18 檔：** `主線事件/179年/6月`、`主線事件/179年/7月`（`#215`→`11`、4 處命格旁白→`8`）、`古墓演出劇情`（3→`11`）、`呂信線/01仗義疏財`（`#122`→`11`）、`呂信線/渡江`（2→`11`）、`大地圖/宗緯家`（`#2080`→`17`、`#2095`→`19`）、`大地圖/雲中/街道`（`#45`→`1`）、`大地圖的小溪村`（`#56`→`9` 去Q，`#57` 去 Disable）、`小溪村/碰瓷`（`#2028`→`11`）、`小溪村/童淵`（`#2050`→`8`）、`小溪村後山/攔路匪、樹下刀客1`、`小溪村後山/赫連娜娜、張寧`（`#3`/`#79`→`11`）、`小溪村後山/雍掉下去`、`破廟/老和尚、墳墓、供桌`（`#3381`→`12`）、`破廟/開場、蕭靈犀`（`#3574`→`9` 去Q＋`#3544` 去 Disable、`#3593`/`#3599`→`18` 談條件）、`破廟/阿傑、阿佑、阿偉、水井`（`#3931`→`12`）、`破廟/黑衣武者`（`#3373`→`8`）、`英豪府遴選/蔡邕、蔡琰、張仲景、張寧`（`#54`→`17` 放電）。
  ⚠ 判定保留不必動：`pic=14` 留 2 處（`破廟/開場` `#3532` 哈欠、`黑鐵嶺礦坑` `#2054`）；`pic=13` 留 44 處（武道大會宣示喊話等，20 檔）。✅ `scripts/convert_guanxin*.py` 兩支過時轉換腳本**已刪除**。✅ `根據地/小表妹` §1 `#190` 的 `pic=12` **作者裁示不動**。

- [x] **✅ 已完成（2026-09-07 作者在 Unity 確認）：選項通則第一批 29 處發言格寫長，14 份 JSON 已在 Unity。** 原記錄說「要重新匯入」，但 2026-09-07 作者按匯入時 Bridge 沒有報任何新增／修改——表示早已同步。抽驗 `9月.json` `#85`／`#153`／`#222` 三句宣言，發言格都比選項長，repo 與 Unity 一致。（2026-09-05 作者裁示、同日施工。）
- [x] **✅ 已完成（2026-09-07 作者在 Unity 確認）：選項通則第二批 96 個發言格，31 份 JSON 已在 Unity。** 原記錄說「要匯入」，但作者 2026-09-07 按匯入時 Bridge 沒報新增節點，並在 Unity 端確認 `大地圖的小溪村` `#2056` 存在。AI 端抽驗 `#2056`–`#2060`／`179年7月 #2416`–`#2417`／`180年2月 #4088`–`#4089`／`五原障 #3121`–`#3122` 全在 repo，且該批檔案與 HEAD 無差異。（完工紀錄檔已於 2026-09-07 刪除，需回溯查 git 歷史）。（2026-09-05 作者逐條核可後施工。）
  **⚠⚠ 這批「動了節點數」，與前幾批只改欄位不同**：匯入務必走 Bridge 的「**更新既有對話**」路徑（依 `JsonEntryID` 印記對應），**不可砍掉重匯**——砍掉重匯會讓 Unity 的 Inspector ID 與 JSON `entryID` 永久脫鉤（鐵則見本檔 `01仗義疏財` 那條）。新節點沒有 `JsonEntryID` 印記，Bridge 會自動建新格。
  **逐檔（＋新增數：新號段，節點數變化）：** `大地圖的小溪村`（+5：`#2056`–`#2060`，138→143）、`主線事件/179年/7月`（+2：`#2416`–`#2417`，316→318）、`主線事件/180年/2月`（+2：`#4088`–`#4089`，86→88）、`呂信線/五原障`（+2：`#3121`–`#3122`，375→377）、`大地圖/天下第一武道大會`（+2：`#4107`–`#4108`，504→506）、`大地圖/黑鐵嶺礦坑`（+2：`#3991`–`#3992`，162→164）、`英豪府遴選/糖炒栗子`（+2：`#4087`–`#4088`，14→16）、`大地圖/雲中/街道`（+1：`#4164`）、`大地圖/宗緯家`（+1：`#4236`）、`大地圖/水濂洞`（+1：`#4348`）、`大地圖/竹林小徑`（+1：`#2201`）、`大地圖/雲中/吉祥賭場`（+3：`#4230`–`#4232`）、`小溪村/棋局殘譜、村長`（+2：`#2052`–`#2053`）、`小溪村/牧羊人、茶博士`（+2：`#2090`–`#2091`）、`小溪村/華陀、老學究 `（+2：`#2055`–`#2056`）、`小溪村/賭鬼兄妹`（+1：`#2566`）、`小溪村/釣魚、廢棄的屋子`（+1：`#2056`）、`小溪村後山/攔路匪、樹下刀客1`（+1：`#2095`）、**`探索事件/179年/12月`（+16：`#2333`–`#2348`，165→181）**、`探索事件/179年/3月`（+1：`#2090`）、`探索事件/179年/6月`（+3：`#2069`–`#2071`）、`破廟/老和尚、墳墓、供桌`（+1：`#3406`）、`破廟/開場、蕭靈犀`（+5：`#3639`–`#3643`）、**`破廟/阿傑、阿佑、阿偉、水井`（+17：`#3981`–`#3997`，373→390）**、`破廟/黑衣武者`（+4：`#3396`–`#3399`）、`英豪府遴選/大樹守衛、廖淳、曹操、晨瑛`（+2：`#4235`–`#4236`）、`英豪府遴選/開場、子羽、呂信`（+7：`#4090`–`#4096`）、`英豪府遴選(後)/羽之演武`（+1：`#4124`）、`英豪府遴選(後)/遴選結束`（+2：`#3909`–`#3910`）、`趙王洞/遺跡`（+3：`#4402`／`#4404`／`#4405`，288→291——`#4342`／`#4343` 共用擲骰格 `#4344`，匯流後只留一格發言 `#4402`，寫成兩邊都接得上的合併句）、`小溪村後山/赫連娜娜、張寧`（**節點數不變 172**，是把既有空節點 `#766` 填上字）。
  **同批一併改的（不增節點）：** 約 40 個選項縮短（`text`／`zh_TW`／`zh_CN`）；6 個動作描述選項 `「」`→`（）`（`破廟/老和尚` `#3361`／`#3359`／`#3384`、`破廟/阿傑` `#3262`／`#3263`、`破廟/黑衣武者` `#3325`）、`趙王洞/山賊窩` `#27` 去掉多包的引號；三處髒字（`古墓演出劇情` `#293` 句首句號〔作者裁定〕、`破廟/阿傑` `#3222`「五株錢」→「五銖錢」、`探索事件/179年/12月` `#790`「既非不奸」→「既非奸」）。
  ⏳ **29 份總覽稿待回讀**（Unity 同步後依 `劇情/索引.md` 逐份重讀）：本批 30 檔的對應總覽稿全數，只有 `劇情/呂信/05 五原障.md` 是創作稿、已同步鏡像。
  ⚠ `呂信線/02第三百四十九次` 整檔未動（作者 Unity 編輯中）。

- [x] **✅ 已完成（2026-09-07 查證）：`9月.json`＆`11月.json` `#8` 的 `AudioControl(StopMusic);` 已在。** 兩檔 `#8` 現為 `SetPortrait(MC5,pic=1); SetPortrait(MC8,pic=14); AudioControl(StopMusic); AudioControl(PlayMusic,BGM_19);`。起因：上游 `#3`（赤龍幫未出資分流）在播 `BGM_23`，到 `#8` 交接點會直接疊 `BGM_19`。其餘 5 處入口型嫌疑經作者說明（引擎進事件自動暫停箱庭音樂、離開自動恢復）全數不動，清查表見 `音樂指令轉換規則.md` §1。（2026-09-04 疊播確認後作者裁示。）

- [ ] **審稿 · 張寧〈01 那一碗〉（2026-09-04）。** `劇情/張寧/01 那一碗.md` 初稿，未轉 JSON、未進 Unity。台詞可改；**五拍與那一句「妳配的」不動。** 任務號、婦人 `actorID`、藥房夜曲子仍 `＿＿`。動筆前先讀 `張寧.md` 第四節與第十二節紅線。

- [ ] **審稿 · 赫連娜娜〈09 爹的筆記〉〈10 查清楚了〉（2026-09-04）。** `劇情/赫連娜娜/09 爹的筆記（CF45）.md`、`10 查清楚了（CF45 支線）.md`。兩份都是創作稿、未轉 JSON。09＝七卷本文＋拿到時的話（份四～七已依文風 V2.2 修過一輪）；10＝七份齊後的支線，解鎖 `TigerRock`。⚠ **entry 3 以後要不要順延仍待作者定**，審稿時不要自行改號。

- [x] **✅ 已完成（2026-09-07 作者確認）：呂信〈第三百四十九次〉對話已進 Unity。** key＝`呂信線/02第三百四十九次`。`LuxianChose` 初值 `2`、`LuxianVar` 初值 `0` 已建。**這輪先不建任務表**——JSON 裡沒有 `SetQuestState`，開／收任務日後再掛；**不要自創 `CF42` entry**（那 8 顆是五原障／留信用的）。

- [x] **✅ 已完成（2026-09-04 匯入 Unity）：`呂信線/02第三百四十九次.json` 全面改制，234 → 121 節點。** 作者在 Unity 砍掉 2xxx／3xxx 兩份重複段、新建通用變數 **`LuxianVar`（初始 0）**；AI 依 `劇情/呂信/02 第三百四十九次.md` 補完 `#4006` 之後的 43 格，收尾改由 `#236` 依變數分四岔、共用尾格 `#4090`（關表情、關背景、變數歸零）。
  **變數表：** `1`＝正面打（成敗都算）／別的辦法失敗 → 7-D；`2`＝別的辦法成功 → 7-B；`3`＝我不是他 → 7-C；**洞悉成功在 `#4007` `+10`**，收尾條件寫 `> 10` → 7-A（＝「洞悉成功優先」）。⚠ **`LuxianVar` 只活在這一場，出場前後各歸零一次；呂信線的結算變數是另一個 `LuxianChose`（初始 2），兩者不可混。**
  **同批修掉的三個會出事的地方：** ①`#237`／`#238` 原本都設 `3`（別的辦法失敗會走到「是找錯了」那條他沒說過的收尾）→ `#238` 改 `1`；②`#240`／`#236` 是空節點卻沒有 `Continue();`（正面打那條會卡死）→ 補上；③`#4000` 改成傳酒罈之後 `#4004` 還寫「碗」→ 全場酒器統一成罈。另依作者裁示併掉 `#74`＋`#75`、`#28` 簡體對齊繁體「我不用兵器」。

- [x] **✅ 已完成（2026-09-04 作者確認）：妖林、幽冥禁地已在 Unity 匯入並掛上箱庭物件。** 現行 repo：`大地圖/妖林.json`（**6 節點**，收 `CF45` entry 2）、`大地圖/幽冥禁地.json`（**7 節點**，收 `CF45` entry 1）。key＝`大地圖/妖林`／`大地圖/幽冥禁地`（`NetherForbiddenGrounds` 有 s）。其餘互動（雜兵、殷紅將、五重機關、份四～七的卷）要不要併進同一份 JSON，另裁。

- [ ] **⚠ 卷號對照（作者 2026-09-04 改，全案適用）：`WanderingNotesPage1`＝卷一（08 檔）、`Page2`＝卷二（幽冥禁地）、`Page3`＝卷三（妖林，原〈異聞錄・化妖考〉）——號沒動，名字整批往前挪一格**（小溪村那本已改名《小溪村雜記》、退出 `CF45`）。**本檔以下各條施工紀錄裡寫的「卷二」若指的是娜娜第一次拿到的那卷，一律讀作卷一。** 另有一卷**〈序章〉**（他為什麼離開她們母女）待寫，見 `劇情/赫連娜娜/@設定_爹的筆記.md`〈五之二〉。
- [x] **⛔ 已作廢（2026-09-07 查證）：`08第一頁.json` 的「em7 減量」那一輪從來沒有進 JSON。** 當時提案把 em7 由 9 個減到 2 個（`#104`／`#108`／`#126`／`#129`／`#130`／`#131`／`#4068`），但**現行 JSON 仍是帶 em7 的版本**，2026-09-05 回讀時已依 JSON 把 MD 全部改回，六條相關 ⚑ 註記也標成作廢。**⛔ 不要照這一條去減 em7——那會把現行 JSON 改壞。** 同批的「阿娘→阿母」七格改字是另一回事，已完成並匯入（見上）。

- [x] **✅ 已完成（2026-09-07 作者確認）：`主線事件/180年/11月.json` 那四項早就改完了。** ⚠ **先前把 09-03 回讀疑點當「還欠」是過期資訊**——`#4` 已是十一月旁白、`#8` 已是冬天版（不是封門抄家貼錯）、`#4335` 孤兒已刪、跳過補播也不要再當未修去掃。**下一輪不要再把這四條唸成待辦。** 兩檔重複維護面仍在，但不當現在要做。


- [x] **✅ 已完成（2026-09-03）：`呂信線/01仗義疏財.json` 四處修正已匯入 Unity，兩邊同步。** 未增刪節點（仍 108）、`links` 未動。

  | # | 改動 | 為什麼 |
  | :-: | :--- | :--- |
  | 1 | `#1001` **刪掉** `Conditions: IsPassFight() == true;` | 戰鬥拆掉後的殘骸，全檔 `BeginFight` 已是 0。`#1001` 是 `#99` 的**唯一**出口，留著會讓酒量檢定成功那條停在 `#99` |
  | 2 | `#981` `DisableCharacterExpression(1)` → `(2)` | `#98` 開的是位置 2 的 `Anger_3`，原本關位置 1（空轉），生氣表情會留到尾格 |
  | 3 | `#115` `BloodSplash_Middle,Stop` → `Sword_Multiple_ScreenRed,Stop` | `#114` 開的是後者；`BloodSplash_Middle` 全案沒有任何一處 Play 過 |
  | 4 | `#119` 補 `StopAllParticle();` 與 `OpenPanel(2, close)@1;` | 酒量失敗那條呂信站 `[panel=2]`，原本沒關；轉場前清特效見 `畫面指令轉換規則.md` §3.3 |

  **✅ 已複驗**：108 節點、無重複／斷鏈／孤兒；`IsPassFight`／`BeginFight` 皆 0；剩下的 `Conditions` 只有 `#39`／`#43`（洞悉）與 `#88`／`#110`（酒量）四個檢定分支；特效 Play/Stop 全配對；走完全檔沒有任何表情沒關。

  **⚠⚠ 匯入前必讀：Unity 的 Inspector `ID` ≠ JSON 的 `entryID`。** `AndyCursor用/DialogueJsonBridge.cs` 建立**新**對話時是依 JSON 陣列順序重發流水號（`GetNextDialogueEntryID`，START=0），JSON 的 `entryID` 存進節點的 **`JsonEntryID`** 欄位。**所以「砍掉本地對話重新匯入」會讓兩套號碼永久脫鉤**（本檔 2026-09-03 就踩過：repo `#104` 在 Unity 是 86）。
  - **在 Unity 找節點請認 Dialogue Text 或 `JsonEntryID` 欄位，不要認 ID。**
  - **這次直接匯入（不要先砍）**：Bridge 走「更新既有對話」那條路時是依 `JsonEntryID` 印記精準對應的，不會重新發號。


- [x] **✅ 已匯入 Unity（2026-09-03 作者完成）：`主線事件/180年/9月.json`。** 360 節點，本輪共動 15 處、**未增刪節點、`links` 未動**：
  - 作者改的 12 處：甄筠 `MC9` 由 `[panel=2]` 移到 `[panel=4]`（9 句）、`#4366` 補 `OpenPanel(2/5, close)`、`#291` 拿掉 `StopMusic`＋`BGM_47`、`#143` 拿掉 `BGM_38`。
  - 回讀時補的 3 處：**`#305` 補 `OpenPanel(4,close)@1;`**（甄筠移到 panel 4 後，三家離場沒人關她的立繪）、`#143` 連 `StopMusic` 一併拿掉（否則到 `#212` 有 23 格無音樂）、`#4356` `SetPortrait(MC9,pic=4)` 改回 `pic=1`（誤改）。
  - **`Variable["BreakAlliance"]` 已在 Unity 建好、初值 `0`**（作者 2026-09-03 確認）。`C2M1` entry 3 見卡在作者。
  - 回讀稿：`劇情/180年事件/09月(主)填補赤字結算.md`（§3 已於 2026-09-03 依併檔後新編號整段重生成，360 節點全對過）。

- [x] **已完成（2026-09-04）張寧線 · 設計端已建；第 1 支初稿待審，其餘未寫。** `劇情/張寧/張寧.md` 第五版提案。第 1 支〈那一碗〉初稿見上面審稿；其餘支仍提案；**一支 JSON 都沒有。** 不要當 Unity 要建去掃。

- [x] **✅ 已作廢／早已結案（2026-09-07 查證）：2026-09-03 那筆「Unity 已修訂、JSON 未匯出」。** ①`呂信線/仗義疏財` → 已改名為 `01仗義疏財.json` 並於 2026-09-03 匯出、修正、再匯入，兩邊同步；③`赫連娜娜線/08第一頁` → 已數輪匯出／回讀（最後一次 2026-09-04 20:26），repo 與 Unity 一致。**本條不必再做。**
  **✅ ②`02第三百四十九次` 這一條已結（2026-09-04）：** 作者已匯出 → AI 改制補完 → 作者已匯入，`.md` 也依最終 JSON 逐句回讀過（零落差），詳見上面已勾項。
  - ✅ **2026-09-03（第二輪）：`08第一頁.json` 已照最新稿整份重寫，34 → 28 節點。**
    **這一份是正本，Unity 端請直接匯入它。** ⚠ **代價講明**：Unity 那邊若還有未匯出的手改，匯入時會被蓋掉——要保留就先匯出比對再決定。新稿全文見 `劇情/赫連娜娜/08 第一頁（180年4月主線）.md`（檔頭列了作者這輪的九條指正）。

    **作者拍板的兩條前提**（整場骨架都從這裡長出來）：**①她在市集攤子上就認出爹的字**——發現不在畫面上，這場戲演的是「她怎麼開這個口」；**②卷二便籤在她把卷子攤給主角看的那一下開**——玩家先讀完全文，**此後任何角色都不得再複述卷子內容**。

    | # | 改動 | 節點 |
    | :-: | :--- | :--- |
    | 1 | **簡帛考據修正**（東漢仍以簡牘為主，「簡帛」是現代合稱、帛不論斤賣；北地漢簡多木簡）：擔子裡改成**簡**、她翻著那卷仍是**紙** | `#94`／`#95`／`#97`／`#106` |
    | 2 | **開卷二便籤**：`ModifyData(Valuable,...)` 掛在新的 `#126`（「你自己看」），緊接 `#125` 空節點跑 `ShowValuableMemo(WanderingNotesPage1);`（甲式攔推進） | **`#126` 改寫、`#125` 新增** |
    | 3 | **併成一場**：原入口二 `#115`–`#124`（隔日）整段刪除 | **刪 10 格** |
    | 4 | **刪 `#100`**（「接過去，只看了一眼，手就停住了」）——前提①，發現不能在畫面上再演一次。連帶 `#101` 拿掉 `DisableCharacterExpression(1);`（`#100` 的 Surprise 沒了） | **刪 1 格** |
    | 5 | **刪 `#105`**（「這一卷本姑娘收了」）——**東西本來就是她自己翻出來、自己買的，沒人跟她搶**。收卷與離開併進末尾 `#111` 旁白 | **刪 1 格** |
    | 6 | **刪 `#127`／`#128`**（南下晉陽、兩個地方一東一西）——玩家已從便籤讀到，角色不複述 | **刪 2 格** |
    | 7 | **地圖 ID 更正**：`NetherForbiddenGround` → **`NetherForbiddenGrounds`**（有 s）。`MapLock` 兩條現在掛在 **`#134`**（主角「帳先擱著」） | `#134` |
    | 8 | **小犀有立場了**：`#131` 由主角句改成她的（`actorID` `MC1`→`MC8`，晉陽來回六七天、這個月帳沒平）；`#106` 改問「上頭寫著卷二，那卷三呢」 | `#131`／`#106` |
    | 9 | **對白重寫**：開場旁白、`#102`（「他來過這種地方」無指涉 → 改問她是不是一路跑回來）、`#103`（拿掉驚嘆號，改成避而不答）、`#104`（她早讀過，改成指給他們看）、`#132`／`#133`（把手改輕）、`#107`／`#108`（卷是散佚的，她那一漏）、`#134` 改成主角自己的決定、`#111` 末尾旁白 | 全場 |

    **流程**：`#91`–`#97` → **`#126`** → **`#125`**（便籤）→ `#98`–`#99` → `#101`–`#104` → `#129`–`#133` → `#106`–`#108` → **`#134`**（`MapLock`）→ `#110` → `#109`（`CF45`）→ `#111` → `#114`（尾格）。**入口只剩 `#91`，Unity 端要收掉第二個入口。**
    **已驗**：28 格全有 `zh_CN`、`zh_TW` 與 `text` 一致、`[/em7]` 後都有標點、`title` 只有 `#91`（作者的，未動）、連線一條到底無孤兒。
    **✅ 貴重品 `WanderingNotesPage1`～`7` 在 Unity 已建檔**（作者 2026-09-03 確認）。
    **⚠ 連帶：`09 爹的筆記（CF45）.md`〈份一・拿到時〉已由六格砍到四格**，兩邊要一致。

  - ⚠⚠ **2026-09-03（第三輪）：作者又改了 `08第一頁.json`，28 → 31 節點，repo 版已回讀進 `08 第一頁（180年4月主線）.md` 與 `09 爹的筆記（CF45）.md`〈份一〉。**

    | # | 作者的改動 | 節點 |
    | :-: | :--- | :--- |
    | 1 | **主角收尾句換掉**：「……帳先擱著。」→「呼，果然路子還是在外頭啊，說不定找著寶貝，府裡這筆虧空就填上了呢。」（不正面表態，講府裡的窟窿） | `#134` |
    | 2 | **那道門拆成兩句演**：先講成聽來的傳聞、自己還不信，再由卷子坐實，並補「小犀，妳那本帳，說不定就在門後頭擱著」 | `#133` 改寫、**`#4068` 新增** |
    | 3 | **新增收尾兩格**：娜娜「那兩處都在晉陽那一帶」＋系統旁白「（發現：幽冥禁地、妖林。）」；`MapLock` 兩條移到後者 | **`#4069`／`#4066` 新增** |
    | 4 | **卷二轉手**：`#4066` 把 `WanderingNotesPage1` 從 `Player` −1、`MC22` +1（東西最後在她手上） | `#4066` |
    | 5 | **小犀的問句全部重寫**；全場補齊表情特效 | `#129`／`#131`／`#106` 等 |

    **✅ 已驗**：31 格一條直線無分支、單入口 `#91`（`title` 作者的，未動）、單結尾 `#114`、無重複 entryID／斷鏈／孤兒；**表情特效 17 組開關全部配對**；便籤走甲式且排在 `ModifyData(Valuable,…)` 之後（合 `貴重品指令轉換規則.md`）；`NetherForbiddenGrounds`／`DemonForest` 兩個 ID 全案都在用。

    **✅ 三件已裁示維持現狀（作者 2026-09-03）。⛔ 下一輪不得當成錯誤去「修」：**
    1. **尾格 `#114` 只寫 `SetQuestState("CF45", "active");`，刻意不收 entry 1／2。** 前一版 JSON 有這兩行，作者已決定拿掉。`09 爹的筆記（CF45）.md` 那張 entry 表僅供對照，不是補回來的依據。
    2. **`#4068` 的 `SetPortrait(MC22,pic=11)` 維持。** `立繪指令轉換規則.md` §4.4 說 `11` 是別名格、疑惑建議寫 `14`——**本格是刻意的例外，不適用那條建議。**
    3. **卷二在 `#4066` 提早轉回娜娜身上（早於主角「先給妳」三格）維持。** 不要挪到 `#109`／`#111`。

  - ✅ **`呂信線/02第三百四十九次` 已全部結案（2026-09-04 匯入 Unity）**，改名、`zh_TW`、選項 `actorID`、`[panel=1]`、尾格、`EnableDialogueBG` 那批全在裡面。⚠ **檔名／對話 key 是 `呂信線/02第三百四十九次`**（Bridge 拿相對路徑當 key，Unity 端標題／`JsonSource` 印記已一起改過，別再改回舊名）。

  - ⚠ **2026-09-03 補：`呂信線/仗義疏財.json` 的 repo 版已改名並修過**（作者指出的三個問題）。**同樣不可直接匯入 Unity**——先從 Unity 匯出，再把下列改動套到匯出檔上：

    | # | 改動 | 節點 |
    | :-: | :--- | :--- |
    | 1 | **檔名改成 `01仗義疏財.json`**（比照 `賈詡線/01兩卷`、`赫連娜娜線/08第一頁`）。⚠ **Bridge 是用相對路徑當對話 key**（`ResolveConversationKey`），`呂信線/仗義疏財` → `呂信線/01仗義疏財`；**key 含「/」時對不到就直接當新對話另建一份**，所以 **Unity 端的對話標題（或 `JsonSource` 印記）要一起改成 `呂信線/01仗義疏財`**，否則下次匯入會多長一份 | — |
    | 2 | **選項節點掛回主角的 `actorID`**：`0` → `MC1` | `#51`／`#53`／`#55`／`#67`／`#70`／`#85` |
    | 3 | **補 `zh_TW`**（原本 99 個有文案的節點一個都沒有；繁體正文＝`text`） | 全檔 99 格 |
    | 4 | **補對話背景圖**（原本全檔沒有 `EnableDialogueBG`）：`#1` 開 `Lnn`；`#881` 的 `@1` 槽加 `EnableDialogueBG(Yunzhong)@1;`（客棧內→門外） | `#1`／`#881` |
    | 5 | **新增 2 個節點（鐵則 6）**：`#119` 轉場回客棧（`EnableDialogueBG(Lnn)@1`，三條路 `#104`／`#109`／`#117` 匯合前，因為 `#120` 起是算酒錢、後院刷碗）；`#127` 尾格（`DisableAllCharacterExpression();DisableDialogueBG();Continue();`，`#126` links 改 `[127]`） | **111 → 113 節點** |

    **未動**：`role_Farmer`／`role_ZhaiZhu` 兩個暫名 actorID（待作者定號）。

  - ⚠⚠ **2026-09-03（第二輪）：作者又改了 `01仗義疏財.json`，repo 版已回讀進 `劇情/呂信/01 仗義疏財.md`。** 108 節點、入口 `#4314`（作者新建，帶 `title`）、單一結尾 `#127`；全部可達、無斷鏈、無重複 entryID。

    | # | 作者的改動 | 節點 |
    | :-: | :--- | :--- |
    | 1 | **戰鬥整段拿掉**：`BeginFight(Combat,78)` 那格改寫成 `StopAllParticle();Continue();`，戰鬥緩衝格與**整條戰敗分支**刪除，改用刀光特效帶過 | `#99` 改寫；刪 `#100`／`#105`–`#109`／`#1061` |
    | 2 | **NPC `actorID` 佔位填掉** | `role_Farmer`→`role84`、`role_ZhaiZhu`→`role87` |
    | 3 | **門外那段換 panel**：呂信 `1`→`2`、寨主 `2`→`1`，表情位置參數跟著改 `2` | `#90`–`#98`、`#111`–`#116` |
    | 4 | **改字一處、拿掉 `[em7]` 前綴兩處** | `#681` 改字；`#41`／`#123` 去前綴 |
    | 5 | **加刀光特效**、`#101` 加 `SetPanel(MC2, 1, immediate)` | `#981`／`#114`／`#115`／`#101` |

    **⚠⚠ 這四件事在確認之前就跟著匯進 Unity 了（2026-09-03）——現在是線上的問題，不只是 repo 的問題。第 1 項會斷線，見〈現在要做〉第一條：**
    1. **`#1001` 還掛 `Conditions: IsPassFight() == true;`，但全檔已無任何 `BeginFight`。** 它是 `#99` 的**唯一**出口——條件不成立這一場就走不下去。**會斷線的硬傷。**
    2. **`#981` 關錯表情位置**：`#98` 開的是位置 `2`（`Anger_3`），`#981` 關的是位置 `1` → 生氣表情一路留到尾格 `#127` 才消。
    3. **`#115` 關錯特效 ID**：`#114` 開 `Sword_Multiple_ScreenRed`、`#115` 關 `BloodSplash_Middle`（從未開過）→ 酒量失敗那條的刀光沒關；`#119` 轉場也沒有 `StopAllParticle();`。
    4. **`#119` 只關 panel 0、1**，但酒量失敗那條呂信站 `[panel=2]` → 立繪留在畫面上，`#121` 又讓他在 panel 1 出現一次。


- [x] **✅ 已重新匯入 Unity（2026-09-03 作者完成）：`主線事件/180年/9月.json`。** 錯誤條件式已還原，兩邊同步。以下留作紀錄。

  **當初做錯的事：** AI 把 `#79`／`#147`／`#216` 當成「擲骰的成功分支」，補上 `Conditions: IsPassDice() == true;`。**它們其實是匯流點**，從好幾條路進來——

  | 節點 | 入口 |
  | :--- | :--- |
  | `#79` | `#78`（擲骰）＋ **`#768`**（`BreakAlliance` 那條，玩家**檢定失敗**才走到） |
  | `#147` | `#146`（擲骰）＋ **`#156`**（甄筠回應「認錯」選項，**沒擲骰**） |
  | `#216` | `#215`（擲骰）＋ **`#225`**（沒擲骰）＋ **`#231`／`#240`**（兩條都是**檢定失敗**） |

  掛上擲骰條件之後，那些非擲骰／擲骰失敗進來的路**全部被擋死**，玩家會卡在上一格。**同日發現、還原成空條件，作者已重新匯入，Unity 兩邊一致。**

  **教訓（寫給下一輪）：擲骰分支的成功那半留空條件，往往不是漏寫，是因為那一格同時是別條路的匯流點。動 `Conditions` 之前先查入口。**

  **同批另外 7 處是好的，不必動**（作者逐條核可）：

  | # | 節點 | 改動 |
  | :-: | :--- | :--- |
  | 1 | `#307` | 呂信「師弟」→「**小子**」（英豪府不排行，文風指南四之二硬規則） |
  | 2 | `#29` | 徐榮「資金」→「**銀錢**」（甲類） |
  | 3 | `#30` | 徐榮「報酬」→「**酬金**」（甲類） |
  | 4 | `#281` | 褚人飛「你還不夠格」→「**你壓不住這張案**」（「夠格」現代白話；換成他自己的案面意象） |
  | 5 | `#34` | 主角「又是這齣戲碼重演」→「**怕是照樣再來一遍**」（「重演」現代語） |
  | 6 | `#310` | 賈詡「將近三倍**有餘**」→「將近三倍**來**」（「將近」＋「有餘」打架） |
  | 7 | `#4384` | 半形括號 → **全形**「（看向郭嘉。）」 |

  三欄同步（`text`／`zh_TW`／`zh_CN`），節點數不變（360）。**回讀稿已同步**：`劇情/180年事件/09月(主)填補赤字結算.md` L254／257／268／430／438／974／1051／1060。
  **⛔ 作者裁示不動的**：`#4335` 測試節點（「登登登，登大人囉老鐵」，孤兒節點但有 `title`，會把 `C2M1` entry 1／2 設 success）；甄筠 `#143`–`#208`／黑狼王 `#212`–`#272` 共 43 格缺 `[panel]`；`#75`「一絲不易察覺的微笑」；`#4339`「做得漂亮」；panel 跳號。**下一輪不要再去「修」。**

- [x] **這輪不管（2026-09-07 作者：不重要）：`呂信線/02第三百四十九次` 09-03 那張清單剩兩條沒裁＋`3-3` 沒打架卻接到「算你的」。** 不要再當現在要掃；紀錄留著日後要管再翻。其餘十條已隨改制進 Unity；謀略難度 `5` 作者已在 JSON 裡定為 5，提案作廢。**現行 121 節點，`.md` 與 JSON 逐句一致。**

  | # | 還沒裁的 | 節點 |
  | :-: | :--- | :--- |
  | 1 | **洞悉那四格要不要併成兩格、並拿掉「你忽然懂了：」。** 現行是四格：`#4007` 放罈子很輕／`#4008` 沒有立刻答＋兩句推理／`#4009`「**你忽然懂了：**那個數字不是戰績。`[em2]`…`[/em2]`」／`#4010`「`[em2]`他是在數還剩幾次`[/em2]`」。09-03 的提案是併成兩格、把「你忽然懂了：」刪掉改寫成「他不是在數贏了幾次——`[em2]`他是在數還剩幾次`[/em2]`」（7.1b：整串推理可留，但不該明著替玩家宣布）。**AI 這次補節點時照現行 JSON 的字生成，沒有自行套用。** | `#4007`–`#4010` |
  | 2 | **選項 2 的措辭「三百四十八次都贏」沒有依據。** 洞悉成功那條他根本沒答（「那種事有什麼好記的」），失敗那條只說「當然是我」——贏得多≠都贏。09-03 提案改成「**打了三百四十八次還嫌不夠**」（用他自己 `#14` 講過的話）。要改的是 `#4030` 一格（選項與展開已合併成同一格，不必兩處改）。 | `#4030` |

  **⏳ 設計決定（作者裁）：`3-3`（我不是他）那條根本沒打架，可是 `§6` 子羽照樣說「第三百四十九次，算你的」。** 現在有 `LuxianVar` 在手，成本比 09-03 低：`3` 與 `13`（洞悉成功時 +10）就是沒打過那條。要嘛子羽那句依變數分兩版、要嘛改一句讓三條都成立、要嘛 `3-3` 不接 `§6`。**AI 不自行處理。**

- [ ] **180／181 年節慶事件（2026-09-06 進度）。**
  - ✅ **1 月〈儺隊過街〉** 已定稿、已轉 JSON，`劇情/蕭靈犀/01 儺隊過街.md`。✅ **2026-09-07 作者確認已進 Unity**（對話 `探索事件/180年/1月`、閘門：180 年 1 月選「探險」且 `CF16` 已完成、`role84` 無立繪）。
    - ✅ **2026-09-07 補表情特效 8 處已進 Unity（作者確認）**（只動 `Sequence`，沒有增刪節點、沒碰文字）：落點：`#4` 小犀 `Proud`、`#7` 主角 `Proud`、`#8` 旁白格替小犀開 `Surprise`、`#16` 主角 `Anger`、`#26`／`#30` 主角 `Question`、`#27`／`#31` 小犀 `Pain`。每個都在下一格開頭關掉，`#34` 尾格 `DisableAllCharacterExpression()` 兜底。
    - ✅ **2026-09-07 `#4` 拆格已進 Unity（作者確認）**，34 → 35 節點（鐵則 6）。原句 52 字 → `#4`「表哥，這個你就外行啦。這種熱鬧日子正好看。」（23 字，`Proud` 留在這一拍）＋**新節點 `#35`**「哪樣貨賣得動、哪樣貨擺了一整天也賣不掉，小犀都得記著呢。」（30 字，接手 `DisableCharacterExpression(2)`）。
    - ✅ **已裁示不改**：`#31`「村子裡的。」被節奏腳本判為碎句——那是全場最重的一拍，**刻意留短**（作者 2026-09-07 確認）。日後審稿不必再列。
  - ✅ **181 年 3 月〈上巳〉** 已轉 JSON、已改掛（2026-09-06：月份是農曆，上巳只能在三月；180/3 已被第三章開場佔住，故順延一年）。JSON＝`Json/主線事件/181年/3月.json`（78 節點、入口 `#1`），稿＝`劇情/181年事件/03月(主)上巳.md`。✅ **2026-09-07 作者確認已進 Unity**（養成開始月＝**181 年 3 月**）。BGM／音效未填；背景暫用 `Grassland`。
  - ✅ **5 月〈端午〉** 已轉 JSON（2026-09-06 由 6 月改到 5 月）：`Json/主線事件/180年/5月.json`（84 節點、入口 `#1`），稿在 `劇情/180年事件/05月(主)端午(支)死地生綠.md`。✅ **2026-09-07 作者確認已進 Unity**。⏳ **進遊戲檢查見上面未勾項。**
  - ✅ **7 月〈七夕〉** 已轉 JSON（2026-09-06 由 8 月改到 7 月）：`Json/主線事件/180年/7月.json`（103 節點、入口 `#1`），稿在 `劇情/180年事件/07月(主)七夕.md`。✅ **2026-09-07 作者確認已進 Unity**。⏳ **進遊戲檢查見上面未勾項。**
  - ⏳ 重陽仍在 `劇情/180年節慶事件/`：未轉 JSON、未進 Unity；戰鬥 ID（重陽兩場）、背景圖 `Yard`／`Village`、全部 BGM 仍待作者填。九月九日與 9 月填補赤字結帳日同月，掛法尚未定。

- [x] **已完成（2026-09-04）主角立繪特效 ID 換成 Player。** `Json/` 64 檔 619 處 `EnableCharacterExpression` 第二參數 `MC1-1` → `Player` 已全部改完，只動 `Sequence`。`SetPortrait` 第一參數維持 `MC1`。

- [x] **已完成（2026-09-04）稱謂改口已從 Unity 匯出。** 這台 2026-08-31 已匯出。賈詡＝先生／子羽＝羽兄／徐榮維持師父；賈詡自稱賈某。呂信線那 17 處仍未進 Unity。徐之交際 `#681`「為師」不必改。

- [x] **已完成（2026-09-04）赫連娜娜線 · Unity 要建。** 07～12＋三座箱庭設定＋`@設定_爹的筆記.md`。08 第一頁已轉 JSON。✅ 妖林／幽冥禁地 Unity 已匯入並掛物件（2026-09-04）。⏳ 08 改字仍見上面未勾項。

- [x] **已完成（2026-09-04）呂信線 · Unity 要建。** `Json/呂信線/` 七檔。第 2 支對話已進 Unity；**任務表這輪先不做**（2026-09-07 作者）。

- [x] **已完成（2026-09-04）賈詡〈兩卷〉· Unity 要建 CF44。** `Json/賈詡線/01兩卷.json` 佔位已填。entry `Description` 見卡在作者。

- [x] **已完成（2026-09-04）匯出頁搜尋清單的程式不在這次提交裡。** 病徵仍在：對話名的 `/` 在 Unity Popup 會被拆成子選單。預定改成搜尋框＋依資料夾分組的勾選清單。另一台若還握有那份 UI 改動，請再推一次。

- [x] **已完成（2026-09-04）DialogueJsonBridge 補排疊在原點的舊節點。** 這台 2026-08-31 已編譯並匯出。匯入頁按鈕「把疊在原點的節點排進畫布」，不碰文案、可 Ctrl+Z。

- [x] **已完成（2026-09-04）DialogueJsonBridge 重複印記改號。** 這台 2026-08-31 已編譯並匯出。水濂洞匯出 349→358 節點（有增有刪，含 `#4310` 起一批新號），依總覽稿核對，不要當死節點清掉。

- [x] **已完成（2026-09-04）Bridge 死節點：先按清除鈕再匯出。** 匯出匯入來回就多長節點的根因已修。執行順序：1. 編譯無紅字 2. 先按清除鈕（只刪 5 個無印記且無連入的死副本）3. 再匯出。

- [x] **已完成（2026-09-04）音訊 ID 全案校正。** JSON 11 檔 21 處已改、只動 `Sequence`。全案 `AudioControl` 已通過 enum 驗證。Unity 已核：`BGM_39` 與 `BGM_40` 都是 1039；`Skill_HeavyPunch` 不存在，已改成 `Others_Punch_Single_Normal_1`。

- [x] **已完成（2026-09-04）文案兩條硬規則進 Bridge。** 開頭不得有空白；`LoadLevel` 下一句一定要是 `Continue()`。匯入擋下、匯出照實寫但 Console 點名。JSON 已全案修完。雲中街道新增 `#4163`、五原障新增 `#3120`（關卡 ID 仍是 `＿＿`）。

- [x] **已完成（2026-09-04）第二章結算場 · Unity 要建／接線。** 三份已併進 `主線事件/180年/9月.json`（360 節點）。Unity 建 `BreakAlliance` 初值 0。11 月那四項作者 2026-09-07 確認早就改完，不要再當待辦。

- [x] **已完成（2026-09-04）轉場殘留立繪：既有稿還沒補。** 規則已寫進 `畫面指令轉換規則.md` §2.7：`OpenPanel(位置, close)@1` 掛在轉場的 `@1` 槽。作者指示不要碰已完成的 JSON。

- [x] **已完成（2026-09-04）黑鐵嶺分支條件確認。** `黑鐵嶺礦坑.json` 各入口分流與三選一的 `Conditions`。核 `CF16`／`CF17`／`CF21` 是否仍對得上現行分流。呂信線開場、子羽、呂信.json `#4059`／`#4061` 也用 `CF16`，一併核。對不上先記，不要默默改任務表。

- [x] **已完成（2026-09-04）趙王洞白虎道妖 · Unity 任務／入口待核。** JSON 已是新稿（`#45`–`#48` 二問）。這台 2026-08-31 已匯出遺跡（288 節點未增刪）。不要再當確認舊稿還是新稿重做一遍。

- [ ] **台詞長度上限已立規、兩檔已拆，全案還有 257 格紅線（2026-09-04）。** 作者定：**台詞可見字數 50 內、紅線 60**（`[panel]`／`[em2]`／`[em7]`／`[var]` 標籤不計；em7 中間的動作字要算）。規則在 `文本創作指南.md` **2.1b** 與 `轉成json指南.md`〈轉檔前必掃〉；**`文風節奏檢查.py` 已加這條**（過 50 列出、過 60 標紅線，hook 會自動回報）。
  - ✅ 已拆：`大地圖/幽冥禁地.json` `#2`（66 字）→ `#2`＋`#7`、`#4`（98 字，全案最長）→ `#4`＋`#6`（**5 → 7 節點**）；`大地圖/妖林.json` `#4`（88 字）→ `#4`＋`#6`（**5 → 6 節點**）。卷子轉移的 `ModifyData` 都掛在後一格（收卷子那拍）。✅ **Unity 端已跟上**（2026-09-04 作者確認已匯入並掛物件）。
  - ⏳ **既有稿的存量沒動**：全案掃描**紅線（>60）257 格、超標（51–60）295 格**，大宗在 `主線事件/179年/7月`（23）、`大地圖/竹林小徑`（18）、`破廟/阿傑…`（18）、`吉祥賭場`（17）、`雲中/街道`（12）。要不要回頭拆、拆哪幾檔，作者決定。
  - ✅ 便籤格四段順序經查**沒有錯**（`SetContinueMode(original)@Message` 在前、`Continue()@Message` 在後＝全案 322 格零例外的既定寫法），作者 2026-09-04 同意維持。

- [x] **✅ 已完成（2026-09-07 作者確認，第二輪）：娜娜〈死地生綠〉已匯入 Unity。** `Json/赫連娜娜線/12死地生綠.json` **47 節點**（作者重寫多句、新增 `#47` 換曲格 `BGM_18`，`#30`→`#47`→`#31`）；`Json/探索事件/180年/5月.json`（佔位空殼 `#1`）。**`#37` 改成 `SetQuestEntryState("CF45", 3, "active")`——是「觸發」不是「收」**；entry 3 由 `大地圖/白鹵澤.json` `#5` 收 `success`（兩場合成同一顆 entry 的 active→success，原本給白鹵澤的 entry 4 因此空出來）。同批修掉四處：`#4` 表情名 `Ide`→`Idea`（原本是不存在的特效名）、`#12` `zh_CN` 繁體「蹤」→「踪」、`#20`／`#21` `Conditions` 多餘空格、`#37` `Description` 改成「觸發」。入口條件掛 Unity：`CurrentQuestEntryState("CF45", 1) == "success" and (CurrentQuestEntryState("CF45", 2) == "success");`
- [x] **這輪不管（2026-09-07 作者：不重要）：白鹵澤撿卷戲。** JSON 已建（`Json/大地圖/白鹵澤.json`，5 節點），地城做出來再掛；尾格收 `CF45` **entry 3** `success`（那一顆由 `12死地生綠.json` `#37` 開 `active`）。不要再當現在要掃。
- [x] **這輪不管（2026-09-07 作者：不重要）：「七卷齊 → 支線 → 伏虎石」懸空。** 末卷改放白鹵澤之後舊接法不成立；AI 建議支線 `10 查清楚了` 的入口直接讀七顆 entry 都 `success`，**未實作**。不要再當現在要掃。

- [x] **✅ 已完成（2026-09-07 作者確認）：`08第一頁.json` `#104` 的用詞修正已匯入 Unity。** 「阿娘」→「阿母」（作者 2026-09-06 裁示：漢代口語作「阿母」，見漢樂府〈孔雀東南飛〉；匈奴語的「母」無可考、不得自創）。**`text`／`zh_TW`／`zh_CN` 三欄都改了，只動欄位值、沒有增刪節點。** 同批的 `12死地生綠.json` 與 `09`／`10`／`12` 三份稿也一併結案。

- [x] **✅ 已完成（2026-09-07 作者確認已匯入）：09-07 回讀兩檔。** `主線事件/180年/7月.json`（七夕）與 `主線事件/179年/5月.json`（張仲景授醫，25 → 31 節點）都已進 Unity。5 月動了節點數，當時走更新、沒砍掉重匯。

  **`主線事件/180年/7月.json`（七夕．103 節點，只動 `Sequence` 與翻譯欄，沒有增刪節點）**
  - **表情 0 → 17**：原本 61 格台詞掛零（全案規模最大的一檔）。小犀 `#5`／`#12`／`#25`／`#29`／`#33`、娜娜 `#39`／`#47`／`#53`／`#61`／`#64`／`#65`、張寧 `#77`／`#83`／`#93`／`#96`、主角 `#38`／`#43`。
  - ⚠ **兩處 Disable 走特殊路徑**：`#12`（小犀「你要是有事就去罷」）的下一層是三個括號動作選項，依體例留空，所以 `DisableCharacterExpression(2)@1` 掛進 `#16`／`#35`／`#68` **三個轉場的 `@1` 槽**；`#77` 的下一層有擲骰格（只能有四段），Disable 落在 `#81` 與 `#99`。
  - **`zh_TW` 補 95 格**（原本全缺）；`#11` 的 `zh_CN`「娜娜姊姊」→「姐姐」。
  - ⏳ **音樂作者手動處理**（整檔仍無 `AudioControl`）。

  **`主線事件/179年/5月.json`（張仲景授醫．25 → 31 節點，⚠ 有節點增刪，鐵則 6）**
  - **三條孤兒 `DisableCharacterExpression` 清掉**：`#31`／`#40` 關的是 `1`（這一場沒有人站 panel 1）、`#35` 關的 `0` 已在 `#21` 關過。另把 `#27` 的 `Nervous(0)` 從隔四格（`#30`）**移到下一格 `#28`**。
  - **`#2050` 錯字**：「在生活中**或**的經驗」→「**獲得**」（三語同步）。
  - **`#24` 的 `zh_CN` 對齊繁體**：原本簡體被改寫成「微量」，還原成「少少用上一星半點」（同步流程：只做繁簡轉換、不改用詞）。
  - **拆 6 句**（皆超過 2.1b 上限，其中 3 句破 60 紅線）：`#21`→`#42`、`#22`→`#43`、`#23`→`#44`、`#26`→`#45`、`#31`→`#46`、`#35`→`#47`；`#26` 同時把半形括號 `(打了個哈欠，揮揮手)` 改成 `[em7]…[/em7]`。⚠ `#22` 的 `Surprise_2` 留在前半格，關閉改由新格 `#43` 接手，`#23` 原本那條已移除。
  - ✅ **半形刪節號已改**（作者 2026-09-07 核可）：`#34`「…轉瞬便消失在小徑盡頭。你佇立良久**…**）」與 `#2049`「**…**」，`text`／`zh_TW`／`zh_CN` 共 6 個欄位，`...` → `…`。全檔半形標點現在 0 處。
  - ⏳ **未動、待裁示**：`#2050` 那句系統教學旁白——`[panel=6]＊（在生活中獲得的經驗：[em2]生活經驗[/em2]可以用於命盤來獲得[em2]技能[/em2]。）＊`——「經驗」「技能」屬審校清單乙／丙類禁詞，清單建議這種機制說明移進提示框，但那是設計決定，不是文風錯。

## 卡在作者

agent 不得自行決定。

- [x] **已裁示維持 · 本曲不做** 郭嘉線＝DLC（2026-09-03 作者確認）。`劇情/郭嘉/` 稿留著，**這次不要建 Unity、不要填任務 ID、不要裁、不要搬〈逝者無聲〉。** 日後 DLC 再開。`CF46` 留給那時候。
- [x] **已裁示維持** 戰敗即 `ShowEnding(Ending_1)`（Game Over）。作者 2026-08-28 裁示**刻意這樣設計，全案不要改**——`宗緯家`、`小溪村`、`小溪村後山`、`水濂洞`、`破廟`、`雲中`、`黑鐵嶺礦坑`、`179年 支線事件`、`趙王洞/山賊窩` 都是同一個寫法。`趙王洞_白虎道妖_改動追蹤.md`〈複-1〉的改案不採納。記在本檔以免下一輪又去「修」。
- [x] **已裁示維持** [panel] 剩 23 格。三個多人同場檔案。作者已裁示維持現狀、不要改。記在本檔以免下一輪又去「修」。
- [x] **已裁示維持 · 特例不寫進指南（2026-09-03）** `主線事件/180年/9月.json` §舌戰三家用到 `[panel=4]`（甄筠）與 `[panel=5]`（呂信），與 `文本創作指南`〈多名 NPC 同時對話〉「說話者只用 1、2、3」牴觸。**作者裁示：這一場六人同框是特例，指南不改、也不要去「修」JSON。⚠ 別的場次仍照指南只用 1、2、3，不要拿這一場當前例。** 記在本檔以免下一輪又被當成錯誤。
- [x] **已完成（2026-09-04）第二章 · `C2M1` entry 3。** 雙投版收尾 `9月.json` `#325` 寫它當「三家危險平衡成立」（**不是舊稿寫的 entry 4**，2026-09-03 查證）。
- [x] **已解決（2026-09-03 查證）** 第二章 · 甄家 entry 2 已掛：`大地圖/水濂洞.json` `#379`／`#380`／`#4338`／`#4341` → `success`、`#4344` → `failure`（同格一併收 `CF40`）。結算分流讀得到甄家那一路。
- [x] **已解決（作者 2026-09-02 改制＋2026-09-03 確認 Unity 已建）** 第二章 · 舌戰三家的 14 個變數 → 單一 `Variable["BreakAlliance"]`（初值 `0`）；換曲點 → `BGM_32`。
- [x] **已完成（2026-09-04）賈詡〈兩卷〉· 只剩 entry 的 `Description` 文字。** ✅ JSON 佔位全部填完（任務 `CF44`、3 顆 entry、蔡邕門檻 Lv2、`#81` 好感 `-20`、`#88` +50／賞錢 400）。三顆 entry 的 `Description`（Unity 任務表欄位，不在 JSON）建議文字在 `劇情/賈詡/01 兩卷.md` 各處與附錄一。
- [x] **已完成（2026-09-04）赫連娜娜 · 你要填的空。** 虎腹戰鬥 ID；背景 `Desert`／`Yard`／`Cave`、音效 `Others_StoneDoor` 核 ID；勸阻好感門檻（提案第 3 級，`IsFavorabilityLevelPass("MC22", 3)`）；元祖場要不要事件圖。
- [x] **已完成（2026-09-04）赫連娜娜 · 你要裁的。** **感情線四事件提案**（`赫連娜娜.md`〈感情線〉：一百文／端藥／頭巾／下一處；任務與閘門作者自建，好感閘 1/2/3/4 級；R4＝伴侶＋天機盤信物＋後宮修羅場）；名號「玄妍」；「第四層＝她自己的決定」；放下線「天機盤不轉了」留不留；虎腹守門的東西；10 檔選 1 加不加好感。
- [x] **已完成（2026-09-04）** `BrokenWall_Right` 方向。`英豪府遴選/開場、子羽、呂信.json` #481（#484 收特效）。方向是作者選的 Right（不是 Left）。同 ID 也出現在 `大地圖/青霧林/不知名的野外.json`。對照 `給AI看的指南/畫面指令轉換規則.md`。
- [x] **已解決（作者 2026-09-02 已刪）** `Json/主線事件/180年/6月.json`。空檔已不在 repo；索引盤點那句「空陣列無可回讀」過期，重跑盤點時再改，不要自己填劇情。
