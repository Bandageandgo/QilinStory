# 跨機待辦

對話不會跟 git 走；兩地只認本檔。明細仍在 `劇情/索引.md`。做完刪列或打勾。

最後更新：2026-09-02

## 現在要做

- [ ] **主角立繪特效 ID 換成 `Player`（2026-08-31 作者裁示）。** `Json/` **64 檔 619 處**的 `EnableCharacterExpression` 第二參數 `MC1-1` → `Player` 已全部改完，**只動 `Sequence`**、沒有增刪節點、`text`／`zh_TW`／`zh_CN` 一處沒碰。**`SetPortrait` 的第一參數維持 `MC1` 不動。** ⚠ **要在 Unity 端確認引擎吃得下 `Player` 這個 ID**（若引擎那邊的對照名不同，回報一聲，改字很快）。檔單與處數見 `劇情/索引.md`〈待進 Unity〉2026-08-31 追加批次；規則已同步進 `給AI看的指南/立繪指令轉換規則.md` 與 `劇情/轉成json指南.md`。

- [x] **這台 2026-08-31 已從 Unity 匯出** 稱謂改口對應 JSON（2026-08-29 拍板：賈詡＝先生／子羽＝羽兄／徐榮維持師父；賈詡自稱賈某）。文案欄未改；links 壓成單行。**呂信線那 17 處仍未進 Unity。** 未動的「師父」全部是徐榮或張仲景被拒的那幾句，**刻意保留**。`徐之交際 .json` `#681`「為師」不必改。

- [ ] **赫連娜娜線 · Unity 要建**（`劇情/赫連娜娜/07`～`11`＋`@設定_伏虎石.md`）。**08 第一頁已審、已轉 JSON；伏虎石仍未轉。** 明細見 `劇情/赫連娜娜/赫連娜娜.md`〈要補進 Unity 的東西〉。

  | 類 | 內容 |
  | :--- | :--- |
  | 任務 `CF45` | 14 entry：1 開／2–8 七頁／9 她要查／10 查清楚解鎖／11 收著或毀了／12 給了她／13 放下／14 接受。11⇄12、13⇄14 互斥，全用 `success` |
  | 貴重品 | `WanderingNotesPage1`～`7`、`EvilNurturingScroll` |
  | 地圖點 | `TigerRock` |
  | 對話 | ✅ **〈第一頁〉已於 2026-09-02 由 `4月.json` 拆出獨立成檔：`Json/赫連娜娜線/08第一頁.json`**（24 節點，沿用原號 `#91`–`#114`，入口 `#91`；`CF45` 與 entry 1／2 在 `#110`）。**同月賈詡〈兩卷〉也拆成 `賈詡線/01兩卷.json`，兩支從此各一檔、互不相干**；`4月.json` 只剩空節點 `#116` 去調用賈詡那份，**娜娜這一場由作者在 Unity 另一處手動接，不走對話指令**。伏虎石仍未建（建議 `大地圖/伏虎石.json`）；伏虎石仍未建（建議 `大地圖/伏虎石.json`）；~~遺跡插秘籍場~~ **✅ 已寫入 `#4378`–`#4401`**（`#4164` links 改 `[4378]`；回讀 `劇情/趙王洞/03 遺跡.md` §3-6a）。**這台 2026-08-31 已匯出 4 月（114 節點、旁白 `2`→`role2`）與遺跡（288 節點未增刪）**。**2026-09-02 稽核 `08第一頁.json`**（只動 `Sequence`，沒有增刪節點）：**`#111`「她走了」補 `OpenPanel(1, close);`**——娜娜離場但立繪一直留在畫面上（沒有轉場，照 `畫面指令轉換規則.md` §2.7 的例外掛在那句旁白上）；**補表情 3 處**（`#92` 娜娜闖進來 `Proud`、`#100` 認出她爹的字 `Surprise`、`#112` 小犀起疑 `Question`，各自在下一格關）。立繪覆蓋原本就完整、`pic` 值都在範圍內。 |
  | 說話者 | 「聲音」掛 `role2` |

- [ ] **呂信線 · Unity 要建**（`Json/呂信線/` 七檔 **1 024** 節點）。**⚠ 位置是暫置的，entryID 從 1 起編——搬檔與改號必須在同步前做完（鐵則 6）。** 明細見 `劇情/呂信/呂信.md` §8。

  | 類 | 內容 |
  | :--- | :--- |
  | 任務 `CF42` | **建議號**（全案 `CF04`–`CF41` 已用、**`CF44` 賈詡〈兩卷〉已佔用（作者 2026-09-02 定）**、`CF45` 娜娜、`CF46` 郭嘉建議；**只剩 `CF42`／`CF43` 空**）。**要在 Unity 確認沒被佔。** 8 個 entry：赤龍幫出錢／被拉起來／老兵三選一（算・放・帶）／留信三落點（正・反甲・反乙）。寫 13 處、讀 8 處，對照表見 `呂信.md` §7 |
  | 貴重品 | `UnsentLetter`（未寄的家書）、`WuYuanHorsemanship`（技能卡「五原騎術」）——**兩筆都還沒建** |
  | NPC | `role141` 馬賊（含哨騎）、`role142` 馬賊頭目、`role143` 老兵——**三個都要新建**；`role_Farmer`／`role_ZhaiZhu`（仗義疏財的農夫、寨主）仍是暫名待定 |
  | 戰鬥場次 | ✅ `78` 仗義疏財、`79` 五原障戰鬥一已填。⏳ **戰鬥二 `80` 只給一個號，強化版 `#2066` 要不要獨立號待定**；**戰鬥三（頭目）`#608` 未給號** |
  | 關卡 | 五原障箱庭的 `LoadLevel` ID 未定（`五原障.json` `#70`） |
  | 結局 | 結局圖 ID（`改名.json` `#30`）作者說**還沒做**；兩個 `ShowEnding` 編號（`#35`／`#36`）未定 |
  | 入口 | **五原障要開三個入口**：`#1` 書房派差事、`#40` 廣場（可重複點）、`#100` 進箱庭自動觸發 |
  | 變數 `LuxianChose` | 初始 2；正 +1、反 −2。**沒建的話正反判定全部悄悄失效、不會報錯** |
  | ⚠ 先修 | 全線旁白 `actorID` 寫成 `"2"`，全案慣例是 `"role2"`（461 節點）。同步後 entryID 不能改，actorID 還能改，但愈早愈省事 |

  **2026-09-02 · 轉場收立繪面板（`OpenPanel(0, close)@1;OpenPanel(1, close)@1;`）已補**：`仗義疏財.json` 四個轉場、`第三百四十九次.json` 三個新轉場。**`第三百四十九次.json` 225 → 228 節點**——「日頭偏西→日頭下去了」是同景時間跳躍，原本硬切，補成轉場格（`#1999`／`#2999`／`#3999`，接在 `#64`／`#49`＋`#69`／`#77` 之後），並補上開場音樂（`#1` `BGM_19`）與轉場換曲（`BGM_23`）——該檔原本整份沒有任何 `AudioControl`。**呂信線七檔合計 1 021 → 1 024 節點。** 其餘檔案作者自行手動處理，未動。

  **2026-09-02 · `仗義疏財.json` 已補畫面與音樂（106 → 111 節點，屬鐵則 6 的節點增刪，搬檔／建圖前先看這條）：**

  | 項 | 內容 |
  | :--- | :--- |
  | 新增 4 個轉場格 | `#581` 酒過一個時辰（黑幕 `@1` 換回 `BGM_19`）、`#881` 客棧內→客棧門外、`#1001` 戰鬥結束（**`IsPassFight() == true;` 從 `#101` 移到這格**）、`#1061` 呂信獨自清街。四格都是 `畫面指令轉換規則.md` §2.1 的四段，參數沒改 |
  | 新增 1 個緩衝格 | `#981`（`DisableCharacterExpression(1);Continue();`）。原本 `#98` 開的 `Anger_2` 要等打完才關，特效會整場掛在畫面上；戰鬥觸發格只能有那四段，所以獨立一格 |
  | 刪掉玩家看得到的舞台指示 | `#60`／`#89`／`#101`／`#107` 的旁白原本寫著「畫面淡出淡入」「場景到客棧門外」——**全案只有這一檔有這個問題**，已改成真的轉場格 |
  | 音樂 7 處 | `#1` `BGM_19`＋`Others_Talking`、`#10` `BGM_23`、`#33` `BGM_38`、`#581@1` `BGM_19`、`#80` `BGM_28`、`#81` `Others_Fire`、`#120` `BGM_19`。全部照 `音樂指令轉換規則.md` §2 作者已定的情境表，沒自創 ID。**兩件請作者裁**：戰鬥那格要不要補 `BGM_14`（該曲作者已標「預定給舌戰三家」）；本關沒用 `BGM_32` |
  | 還原 7 格說話者 | `#12`／`#14`／`#17`／`#34` → `role_Farmer`、`#80`／`#93`／`#96` → `role_ZhaiZhu`。**2026-08-31 那次 Unity 匯出把它們壓成 `"0"`**（同 `舌戰三家` MC11 那件事）。⚠ **Unity 端沒把這兩個角色建起來之前，每匯出一次就會再壓一次** |
  | 兩條過期 `Description` | `#50` 原寫「Unity 端請勾『只播一次』」——作者 2026-08-30 已確認 Unity 沒這功能；`#99` 原寫「場次 ID 待作者填」——`78` 早已填入 |
  | 對話背景圖 **沒加** | 本關在雲中客棧（有實際場景，比照 `根據地/小表妹/對話(雲中客棧).json`）。**若這支要併進 `探索事件/` 當養成事件，就得補 `EnableDialogueBG(Lnn)` 與每條結尾的 `DisableDialogueBG();Continue();`，門外那段還要再換一次背景** — 先定這支的家在哪 |

- [ ] **賈詡〈兩卷〉· Unity 要建任務 `CF44`（JSON 已由作者定稿）。** `Json/賈詡線/01兩卷.json`（**89 節點**，**入口 `#347`**；由 `4月.json` 拆出獨立成檔，2026-09-02）。創作稿 `劇情/賈詡/01 兩卷.md`（已對稿）。**要建：`CF44` 三顆 entry ＋ `Description`**——1＝差事（`#26` 開／`#90` 收）、2＝沒說他還是給了（`#51`）、3＝說了實話（`#63`）；**⚠ entry 編號與 2026-08-31 舊稿不同，1 與 3 對調。** `#78` 讀 entry 3、`#84` 讀 entry 2，任務 ID 已寫死 `CF44`，不再是佔位。⚠ **`CF44` 是賈詡線整條線的任務，不是本支的**（作者 2026-09-02 定）：`#26` 開一次 `active`，**四支事件各收自己那顆 entry，刻意不關主任務**——所以沒有 `SetQuestState("CF44","success")`，建任務時不要當成漏寫。**✅ 佔位全數填完**（`#81` 好感 `-20`、`#88` +50／400）。⏳ **未匯出、未進 Unity。**

  **2026-09-02 稽核已補（只動 `Sequence`／`Conditions`，沒有增刪節點、沒碰 `text`／`title`／`links`）：**
  - **立繪 19 格**：賈詡 13 格（`#6`／`#13`／`#16`／`#18`／`#21`／`#25`／`#66`／`#68`／`#69`／`#71`／`#81`／`#87`／`#88`）、蔡邕 6 格（`#36`／`#37`／`#47`／`#50`／`#57`／`#59`）——原本整格沒有立繪指令，畫面會停在上一個人的圖。五立繪角色只用 `1`–`5`。
  - **⚠ 蔡琰的立繪沒有收**：`#29` 她在 `[panel=2]` 說話，之後一路留在蔡邕書房裡。`#30` 轉場補了 `OpenPanel(2, close)@1;`。
  - **面板收在黑幕裡**：`#26`／`#51`／`#63` 原本在轉場**前**一格關面板（畫面還亮著，立繪會當場消失），改掛到 `#27`／`#30`／`#64` 的 `@1` 槽（`畫面指令轉換規則.md` §2.7）。`#348` 那個多餘的關法留著沒動，多關無害。
  - `#35`／`#36` 補蔡邕愣住那一拍的 `Surprise`（下一格關）。
  - `#78`／`#84` 的 `Conditions` 補分號。

- [ ] **郭嘉線 · Unity 要建**（`劇情/郭嘉/01`～`08`＋`@設定_雁門.md`）。資料夾已於 2026-08-29 `2948bcc` 上傳，另一台 pull 即可（本機 `05 嫌髒.md` 若還有未提交改動另說）。01–04 完稿、05–08 初稿待審。**`GuojiaChose` 沒建的話正反判定全部失效。** 另：07 讀 `IsFeatLevelPass("Player","MedicalExpertise",5)`，確認醫術等級上限 ≥5。

  | 類 | 內容 |
  | :--- | :--- |
  | 變數 `GuojiaChose` | 初始 2；正 +1、反 −2；第 7 支門檻 ≥3（作者 2026-08-29 定名）。`05`／`06` 寫、`07` 讀 |
  | 01–04 的任務 | ID 未定（建議 `CF46`，作者定），15 entry，各檔附錄一有對照表 |
  | 07 看診四顆 entry | 正・治成／正・未治成／反甲／反乙（08 開春讀） |
  | 情報網 #19 | 雁門・山村，郭嘉捎來（06 入口） |
  | 08 結局圖 | 兩張（暫記 `GuoJiaEnding_Clear`／`GuoJiaEnding_Packet`）＋兩個 `ShowEnding` 編號 |

- [ ] **⚠ 匯出頁搜尋清單的程式不在這次提交裡**（2026-09-02 待辦曾寫「已改」）。這台同步時 repo 的 `AndyCursor用/DialogueJsonBridge.cs` 被整份刪掉，已從 `831b7d7` 救回（09-01 印記／死節點／`CheckEntry` 版）。**病徵仍在**：對話名的 `/` 在 Unity 的 Popup 會被拆成子選單。**預定改法**：下拉選單換成**搜尋框 ＋ 依資料夾分組（`主線事件/180年`）的勾選清單**——關鍵字用空白隔開要全部命中（打「主線 9月」即可），也能直接打對話 ID；群組預設收合、一開始搜尋就自動展開命中的組；可勾多段一次匯出。另一台若還握有那份 UI 改動，請再推一次；沒有就等下次補。
- [x] **這台 2026-08-31 已編譯並匯出** DialogueJsonBridge 補排疊在原點 (0,0) 的舊節點。匯入頁按鈕「把疊在原點的節點排進畫布」，不碰文案、可 Ctrl+Z。
- [x] **這台 2026-08-31 已編譯並匯出** DialogueJsonBridge 重複 JsonEntryID 改號。**水濂洞匯出 349→358 節點**（有增有刪，含 `#4310` 起一批新號），另一台依總覽稿核對，不要默默當死節點清掉。
- [ ] **⚠ 已改、待 Unity 端編譯確認＋按一次清除鈕（先做這條，再匯出任何東西）** DialogueJsonBridge「匯出匯入來回就多長節點」根因修掉（2026-09-01）。**病徵**：作者發現水濂洞有 `#405`／`#409`、武道大會有 `#4106`／`#4107` 兩格作用完全相同；節點數一路長（水濂洞 349→358→369、武道大會 503→504）。**根因**：匯出時遇到「沒有 `JsonEntryID` 印記」的節點（＝在 Unity 裡手動加的），會拿它的 Unity id 當 JSON entryID 寫出去，**但沒把印記寫回節點**。下次匯入時這個 JSON entryID 對不到任何印記 → 工具再建一個新節點（新的帶印記）→ 原節點變成沒有連入的死節點留在圖上。**每來回一次就多長一份。****修法**：①匯出改走 `AssignJsonIds()`——沒印記的節點沿用自己的 Unity id（被占才配新號）並**一律把號寫回印記**；印記自己重複時仍是後出現的配新號（印記優先於無印記的沿用值）。②匯入時新增「認領」：JSON 對不到印記的 entryID 若等於某個無印記節點的 Unity id，直接認領並補印記，不另建新節點。③匯入頁新增按鈕「**清掉沒有印記、也沒有連入的死節點（整個資料庫）**」。

  **執行順序（順序錯了會把死節點洗成正式節點）：**

  1. 編譯確認無紅字。
  2. **先按清除鈕**——已模擬過，全庫只會刪這 5 個，都是無印記且無連入（走不到）的死副本，內容與各自的活節點一字不差，活的那份帶著印記與全部連線：

     | 對話 | 刪掉的 Unity Entry | 對應還活著的節點 |
     | :--- | :--- | :--- |
     | `大地圖/天下第一武道大會` | `4106` | `4107`（印記 4106、連入 `836`） |
     | `大地圖/水濂洞` | `354`、`388`、`390`、`405` | `406`／`407`／`408`／`409`（印記 354／388／390／405） |

  3. 清完再做匯出／同步。**順序反了**：新版匯出會替那 5 個死節點補上印記，它們就變成正式 JSON 節點，再也分不出哪份是死的。

- [ ] **音訊 ID 全案校正（2026-09-01 作者裁示）。** 新增 `給AI看的指南/音樂指令轉換規則.md`（BGM／音效查曲表，**情境欄由作者填，AI 不得自行判定**）。ID 的唯一事實來源是 Unity 的 `Assets/GameResources/Script/Scripts/Audio/AudioID.cs`（`AudioID` enum，184 個 ID；BGM 只到 `BGM_47`）。**全案 `AudioControl` 現已 100% 通過 enum 驗證**，只剩舌戰三家 `#291` 的 `＿＿` 佔位。共 11 檔 21 處，**全部只動 `Sequence`，沒有增刪節點**：

  | 檔 | 改動 |
  | :--- | :--- |
  | `主線事件/179年/4月` | #2 `AudioControl(Play Music,BGM_19)`→`AudioControl(PlayMusic,BGM_19)` |
  | `主線事件/179年/7月` | #2228 `AudioControl(PlayMusic,Others_Carriage)`→`AudioControl(PlaySFXOneShot,Others_Carriage)`；#2320 `AudioControl(PlayMusic,Others_Carriage)`→`AudioControl(PlaySFXOneShot,Others_Carriage)`；#2404 `AudioControl(PlayMusic,Others_Carriage)`→`AudioControl(PlaySFXOneShot,Others_Carriage)`；#2412 `AudioControl(PlayMusic,Others_Carriage)`→`AudioControl(PlaySFXOneShot,Others_Carriage)` |
  | `古墓演出劇情` | #52 `AudioControl(PlaySFX,Skill_HeavyPunch )`→`AudioControl(PlaySFX,Others_Punch_Single_Normal_1)`；#304 `AudioControl(PlaySFX,Skill_HeavyPunch )`→`AudioControl(PlaySFX,Others_Punch_Single_Normal_1)` |
  | `大地圖/雲中/街道` | #30 `AudioControl(PlayMusic,Others_Talking)`→`AudioControl(PlaySFXOneShot,Others_Talking)` |
  | `小溪村後山/赫連娜娜、張寧` | #694 `AudioControl(PlaySFXOneShot,Roar)`→`AudioControl(PlaySFXOneShot,Others_Roar)`；#737 `AudioControl(PlaySFXOneShot,Fire)`→`AudioControl(PlaySFXOneShot,Others_Fire)`；#745 `AudioControl(PlaySFXOneShot,Fire)`→`AudioControl(PlaySFXOneShot,Others_Fire)`；#752 `AudioControl(PlaySFXOneShot,Notice)`→`AudioControl(PlaySFXOneShot,Others_Notice)` |
  | `探索事件/179年/12月` | #720 `AudioControl(PlaySFXOneShot,Fire)`→`AudioControl(PlaySFXOneShot,Others_Fire)`；#767 `AudioControl(PlaySFXOneShot,Water)`→`AudioControl(PlaySFXOneShot,Others_Water)`；#857 `AudioControl(PlaySFXOneShot,Fire)`→`AudioControl(PlaySFXOneShot,Others_Fire)` |
  | `探索事件/179年/3月` | #83 `AudioControl(PlayMusic,Others_Talking)`→`AudioControl(PlaySFXOneShot,Others_Talking)` |
  | `探索事件/180年/3月` | #4148 `AudioControl(PlaySFXOneShot,Fire)`→`AudioControl(PlaySFXOneShot,Others_Fire)` |
  | `破廟/阿傑、阿佑、阿偉、水井` | #3645 `AudioControl(PlaySFXOneShot,Others_Others_Thunder)`→`AudioControl(PlaySFXOneShot,Others_Thunder)` |
  | `英豪府遴選(後)/徐之交際 ` | #675 `AudioControl(PlaySFXOneShot,Bird)`→`AudioControl(PlaySFXOneShot,Others_Bird)`；#677 `AudioControl(PlaySFXOneShot,Bird)`→`AudioControl(PlaySFXOneShot,Others_Bird)` |
  | `英豪府遴選/開場、子羽、呂信` | #428 `AudioControl(PlaySFXOneShot,Notice)`→`AudioControl(PlaySFXOneShot,Others_Notice)` |

  ⚠ **兩件 Unity 端要看的事**：
  1. **`AudioID.cs` 有 bug**：`BGM_39` 與 `BGM_40` 的值都是 `1039`，`BGM_40` 等於指向 `BGM_39` 那首。要用 `BGM_40` 前得先修 enum。
  2. `Skill_HeavyPunch` 這個 ID **整個專案都不存在**（enum／AudioAsset／.cs 全無），已依裁示改成 `Others_Punch_Single_Normal_1`。

- [ ] **已改、待 Unity 端編譯確認＋建兩個節點** 文案兩條硬規則進 Bridge（2026-09-01 作者拍板）。①**對話開頭不得有空白**（開頭一個空格，遊戲內排版會多吃一次換行）；②**`LoadLevel(...)` 的下一句一定要是 `Continue()`**（換場之後同一格剩下的指令不保證跑得到）。`AndyCursor用/DialogueJsonBridge.cs` 新增 `CheckEntry()`：**匯入時擋下**（跳視窗＋Console 列出，不套用）、**匯出時照實寫出但在 Console 點名**。規則已寫進 `給AI看的指南/畫面指令轉換規則.md` §0 鐵則 4／§2.6／§4 自檢，與 `劇情/轉成json指南.md` §3.8＋兩節〈轉檔前必掃〉。JSON 已全案修完（兩項現在都是 0），**其中兩檔動了節點數（鐵則 6）**：

  | 檔 | 改動 | 增刪節點 |
  | :--- | :--- | :-: |
  | `大地圖/雲中/街道.json` | `#114` 的 `LoadLevel(Command_YunzhongInn)@1` 從轉場裡拆出來，**新節點 `#4163`**（`LoadLevel(Command_YunzhongInn);Continue();`）；連線 `#114 → #4163 → #4147` | **是（164→165）** |
  | `呂信線/五原障.json` | `#70` 原本整格**沒有 `Continue()`**，補齊轉場四段，**新節點 `#3120`**（`LoadLevel(＿＿);Continue();`）。⚠ 關卡 ID 仍是 `＿＿`，見上面呂信線那條 | **是（374→375）** |
  | `主線事件/179年/7月.json` | `#2052`／`#2341`：`ModifyData`／`MapLock`／`DisableEventBG`／`DisableDialogueBG` 移到 `LoadLevel` 之前 | 否 |
  | `趙王洞/山賊窩.json` | `#4163`：`MapLock` 移到 `LoadLevel` 之前 | 否 |
  | `趙王洞/遺跡.json` | `#4237`：`LoadLevel` 從第一句移到 `Continue()@Message(EndVideo)` 前一句、同掛該訊號（全案唯一的等訊號例外） | 否 |
  | `破廟/開場、蕭靈犀.json` | `#3604`／`#3618`／`#3629` 去掉開頭空白（`text`＋`zh_TW`，6 欄位） | 否 |
  | `破廟/阿傑、阿佑、阿偉、水井.json` | `#3584`／`#3587`／`#3589`／`#3595`／`#3598`／`#3613`／`#3626` 去掉開頭空白（`text`＋`zh_TW`，14 欄位） | 否 |

- [ ] **第二章結算場 · Unity 要建（2026-08-30）。** `劇情/舌戰三家.md`（雙投）與 `劇情/填補赤字結算.md`（獨家）。**JSON 已轉完，待 Unity 建／接線。** 門見 `劇情/填補赤字_觸發分流.md`。

  | 類 | 內容 |
  | :--- | :--- |
  | 任務 | ✅ **總任務＝`C2M1`**（`主線事件/180年/3月.json` `#158` 開）。獨家版寫 `SetQuestState("C2M1","success")`；雙投版寫 `SetQuestEntryState("C2M1",4,"success")`。**entry 4＝三家危險平衡成立，只有雙投版寫**——182 春破局靠「有沒有 entry 4」分流。⚠ **entry 4 這個號要在 Unity 確認沒被佔** |
  | ⚠ **entry 2（甄家）還沒掛** | 位置已標在 `劇情/水濂洞/水濂洞.md`「⚠ 作者：『投資成立』任務狀態掛此」那三格（`#208`／`#215`／`#219` → `success`）與拒絕合作的 `#4292`（→ `failure`）。**不補，結算場的分流與觸發條件都讀不到甄家那一路** |
  | 對話 | ✅ **2026-09-02 作者把三份併成一份**：`主線事件/180年/9月.json` **360 節點**（分流門＋獨資版＋雙投版），原 `填補赤字結算.json`、`舌戰三家.json`、`6月.json` 已刪除。另 `11月.json`（10 節點）不變。三語欄位齊、連通性已驗（2026-09-02 重驗：無斷鏈、無孤兒、無死路）。11 月下旬饕餮骰揭露、12 月上旬董卓認子、`12月.json` 仍未建。**舊號 ↔ 新號換算表在 `劇情/180年事件/09月(主)填補赤字結算.md` 檔頭。** |
  | ✅ **立繪／表情已補** | 2026-09-02：`9月.json` **67 格補 `SetPortrait`**（主角 24 格原本整段沒立繪，另 MC9／MC4／MC10／MC5／MC7／MC8／MC2）、**5 格補表情特效**並在下一格關閉。只動 `Sequence`，沒有增刪節點；`title`／`text` 沒碰。同時修掉三條走不通的 `IsPassDice() == true;`（`#79`／`#147`／`#216` 同時當選單樞紐用，選項擲骰失敗返回時會把對話卡死）、刪掉 `#320` 重複又缺參數的 `ModifyData(Coin,5000);`、`#322` 補關表情、23 處 `Conditions`／`Script` 補分號 |
  | ✅ **變數只剩 1 個** | 作者 2026-09-02 把 14 個 `＿＿NN` 改成單一 `Variable["BreakAlliance"]`（18 處 `+1`；`>4` 褚人飛離席、`>5` 甄筠、`>6` 黑狼王），那 34 個變數格已從 JSON 刪除。**Unity 端建這一個變數就好——但一定要給初始值 `0`**：JSON 裡沒有任何一格替它初始化，Lua 拿 `nil` 去比大小會出錯。`劇情/舌戰三家.md`〈附錄五〉那張 14 個變數的表已作廢 |
  | ✅ **曲號已填** | 原 `舌戰三家.json` `#291` 的 `AudioControl(PlayMusic,＿＿)` 佔位已不在；換曲改成 `9月.json` `#279` `StopMusic` ＋ `#282` `PlayMusic,BGM_32`（徐榮攤帳那一格），與 `音樂指令轉換規則.md` 給 `BGM_32` 標的「大事件收尾」一致 |
  | ⚠ **跨對話連線剩兩條** | 併檔後 9 月那兩條已在檔內接好（`9月#4`→`#4351` 雙投、`9月#8`→`#4336` 獨資）。**Unity 端還要接**：`11月#6`→`9月.json#4351`、`11月#10`→`9月.json#4336`；另兩條斷頭待該檔建好：`11月#2`→饕餮骰揭露、`11月#9`→乞丐結局。**交接處刻意不關背景**，關背景在結算場自己的尾格 |
  | 觸發點 | ✅ **劇本已寫：`劇情/填補赤字_觸發分流.md`。** ⛔ 沒有常駐對話點（作者裁示：玩家不必回箱庭回報）——就是兩次主線強制事件，各讀 `C2M1` entry 1／2。**JSON 已建**（`9月.json`／`11月.json`）；**Unity 端兩處都還沒建**。11 月同檔還有饕餮骰揭露（未建），結算排在它前面 |
  | 立繪 | ✅ **`MC11` 黑狼王＝五立繪**（作者 2026-08-30 確認）。⚠ **這台 2026-08-31 匯出後 26 句 `actorID` 變成 `0`**，Sequence 仍 `SetPortrait(MC11)`。另一台回讀不要把說話者改成系統節點。 |
  | 音樂 | 舌戰三家 §Ⅳ 4-0 賈詡起身那一格 `AudioControl(PlayMusic,＿＿)` **曲號作者自己處理**。獨家版與回報入口**都不設換曲點** |

- [ ] **轉場殘留立繪：規則已寫進指南，既有稿還沒補（2026-09-02 作者提出）。** 症狀是轉場過去之後上一段的立繪還站在新的一景裡——`SetPortrait` 開的面板沒有人關，淡出淡入只蓋黑幕、不動面板。

  **規則已定並寫入** `給AI看的指南/畫面指令轉換規則.md` **§2.7**（＋§0 鐵則 5、§2.1 ③、§4 自檢）與 `劇情/轉成json指南.md` §3.8：**`OpenPanel(位置, close)@1` 掛在轉場的 `@1` 槽裡，不另開一格**（另開一格會讓立繪在畫面還亮著時當場消失，而且多一個節點）。位置＝這一段用過的 `[panel]`，主角是 `0`，旁白 `[panel=6]` 不必關，多關無害。

  **⏳ 既有稿沒動**（作者指示不要碰已完成的 JSON）。掃描結果：全案 **177 個 Fade 轉場只有 39 個關了面板**，其中 **100 個**「轉場前三格內才剛有人露臉」＝實際會殘留：

  | 檔 | 處數 | | 檔 | 處數 |
  | :--- | :-: | :-- | :--- | :-: |
  | `大地圖/雲中/吉祥賭場.json` | 16 | | `古墓演出劇情.json` | 4 |
  | `大地圖/天下第一武道大會.json` | 13 | | `探索事件/179年/12月.json` | 4 |
  | `主線事件/179年/7月.json` | 9 | | `探索事件/179年/6月.json` | 4 |
  | `觀心.json` | 8 | | `主線事件/180年/4月.json` | 3 |
  | `大地圖/水濂洞.json` | 5 | | `呂信線/仗義疏財.json` | 3 |
  | | | | `主線事件/180年/9月.json` | 2 |

  其餘散在 `大地圖/宗緯家`、`小溪村/釣魚、廢棄的屋子`、`破廟/阿傑…` 等十餘檔各 1–2 處。要補的話是**只動 `Sequence`、不增刪節點**的改動；重掃指令：`grep -rl "PlayFeelFeedback(Fade" Json/ | xargs grep -L "OpenPanel"`。

- [ ] 黑鐵嶺分支條件確認。`Json/大地圖/黑鐵嶺礦坑.json` 各入口分流與三選一（殺伐／救贖／血刀）的 `Conditions`。要看分支裡任務有無更新：`CF16`（條目 1–5 收場）、`CF17` 寒鐵、`CF21` 血池等 `SetQuestState`／`SetQuestEntryState` 是否仍對得上現行分流。呂信線 `英豪府遴選/開場、子羽、呂信.json` `#4059`／`#4061` 也用 `CF16` 當條件，一併核。對照 `劇情/黑鐵嶺礦坑/黑鐵嶺礦坑.md`。對不上先記，不要默默改任務表。
- [ ] **趙王洞白虎道妖。** **✅ JSON 已是新稿**（`#45`–`#48` 二問）。這台 2026-08-31 已匯出遺跡（288 節點未增刪）。Unity 任務／入口是否接好仍待核。不要再當「確認舊稿還是新稿」重做一遍。

## 卡在作者

agent 不得自行決定。

- [x] **已裁示維持** 戰敗即 `ShowEnding(Ending_1)`（Game Over）。作者 2026-08-28 裁示**刻意這樣設計，全案不要改**——`宗緯家`、`小溪村`、`小溪村後山`、`水濂洞`、`破廟`、`雲中`、`黑鐵嶺礦坑`、`179年 支線事件`、`趙王洞/山賊窩` 都是同一個寫法。`趙王洞_白虎道妖_改動追蹤.md`〈複-1〉的改案不採納。記在本檔以免下一輪又去「修」。
- [x] **已裁示維持** [panel] 剩 23 格。三個多人同場檔案。作者已裁示維持現狀、不要改。記在本檔以免下一輪又去「修」。
- [ ] **第二章 · `C2M1` entry 4。** 雙投版寫它當「三家危險平衡成立」。要在 Unity 確認沒被佔，沒定就不要自創。
- [ ] **第二章 · 甄家 entry 2 還沒掛。** 水濂洞 `#208`／`#215`／`#219` → `success`、`#4292` → `failure`。不補則結算分流讀不到甄家那一路。
- [x] **已解決（作者 2026-09-02 自行改制）** 第二章 · 舌戰三家的 14 個變數 → 單一 `Variable["BreakAlliance"]`；換曲點曲號 → `BGM_32`（`9月.json` `#282`）。**只剩「在 Unity 建 `BreakAlliance` 並給初值 `0`」這一件。**
- [ ] **賈詡〈兩卷〉· 只剩 entry 的 `Description` 文字。** ✅ JSON 佔位全部填完（任務 `CF44`、3 顆 entry、蔡邕門檻 Lv2、`#81` 好感 `-20`、`#88` +50／賞錢 400）。**⏳ 三顆 entry 的 `Description`（Unity 任務表欄位，不在 JSON）待定** — 建議文字在 `劇情/賈詡/01 兩卷.md` 各處與附錄一。
- [ ] **赫連娜娜 · 你要填的空。** 虎腹戰鬥 ID；背景 `Desert`／`Yard`／`Cave`、音效 `Others_StoneDoor` 核 ID；勸阻好感門檻（提案第 3 級，`IsFavorabilityLevelPass("MC22", 3)`）；元祖場要不要事件圖。
- [ ] **赫連娜娜 · 你要裁的。** 名號「玄妍」；「第四層＝她自己的決定」；放下線「天機盤不轉了」留不留；虎腹守門的東西；10 檔選 1 加不加好感。
- [ ] **郭嘉 · 01–04 任務 ID。** 未定（建議 `CF46`），15 entry。沒定號就不要自創任務編號去建。
- [ ] **郭嘉 · 你要填的空。** 06 兩個戰鬥 ID；07「府將散」掛哪個任務狀態／年月；各支好感數字。
- [ ] **郭嘉 · 你要裁的。** 那東西的本名、「更遠是哪裡」；05 §2「收人先問你家裡死了幾個」兩句留不留；05／06／07／08 正式檔名；04 檔名〈其所不見〉要不要改〈無人知〉。
- [ ] **郭嘉 · `01 逝者無聲` 放哪。** 三人共用支線，放郭嘉資料夾與 CLAUDE.md「跨角色共用不搬」相牴，要嘛加例外要嘛搬回根層。
- [ ] `BrokenWall_Right` 方向。`英豪府遴選/開場、子羽、呂信.json` #481（#484 收特效）。方向是作者選的 Right（不是 Left），待作者在遊戲裡複核畫面方向對不對。同 ID 也出現在 `大地圖/青霧林/不知名的野外.json`。對照 `給AI看的指南/畫面指令轉換規則.md`。
- [ ] `Json/主線事件/180年/6月.json` 是空陣列 `[]`。索引與 `劇情/180年事件/` 已註明無可回讀。刪或留或等內容，等作者裁示，不要自己填劇情。
