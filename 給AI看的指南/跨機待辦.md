# 跨機待辦

對話不會跟 git 走；兩地只認本檔。明細仍在 `劇情/索引.md`。做完刪列或打勾。

最後更新：2026-08-31

## 現在要做

- [ ] **稱謂改口已改進 `Json/`，待同步 Unity（2026-08-29）。** 作者拍板：**賈詡＝先生（主角自稱弟子）／子羽＝羽兄（不再叫羽師兄）／徐榮維持師父**；英豪府不是門派、橫向不排行。全表見 `給AI看的指南/武俠文風創作指南.md` 第四節之二〈稱謂與關係〉。
  **已改 33 個節點（三語系同步）：** `主線事件/180年/2月.json` #4009・#4038／`主線事件/180年/3月.json` #70・#113・#143／`探索事件/180年/3月.json` #4214／`趙王洞/遺跡.json` #88／`古墓演出劇情.json` #299／`英豪府遴選/蔡邕、蔡琰、張仲景、張寧.json` #45・#46／`英豪府遴選(後)/遴選結束.json` #536・#591、`羽之演武.json` #4071、`賈之嗜好.json` #545／`呂信線/` 五檔 17 處（**呂信線尚未進 Unity，無同步問題**）。
  **⚠ 依鐵則 7，同步前先 commit。** 未動的「師父」全部是徐榮或張仲景被拒的那幾句，**刻意保留**。
  **連帶（同日）：賈詡自稱一律改「賈某」，不再用「為師」**（那是徐榮的自稱）。`Json/` 現存「為師」只有 `英豪府遴選(後)/徐之交際 .json` `#681`（徐榮），**不必改**；創作稿與指南 10 份已改。

- [ ] **赫連娜娜線 · Unity 要建**（`劇情/赫連娜娜/07`～`11`＋`@設定_伏虎石.md`）。**08 第一頁已審、已轉 JSON；伏虎石仍未轉。** 明細見 `劇情/赫連娜娜/赫連娜娜.md`〈要補進 Unity 的東西〉。

  | 類 | 內容 |
  | :--- | :--- |
  | 任務 `CF45` | 14 entry：1 開／2–8 七頁／9 她要查／10 查清楚解鎖／11 收著或毀了／12 給了她／13 放下／14 接受。11⇄12、13⇄14 互斥，全用 `success` |
  | 貴重品 | `WanderingNotesPage1`～`7`、`EvilNurturingScroll` |
  | 地圖點 | `TigerRock` |
  | 對話 | ✅ `主線事件/180年/4月.json` **已建**（`#91`–`#114` 第一頁，入口 `#91`；同檔 `#1`–`#90` 是賈詡〈兩卷〉，兩個入口互不相干）；伏虎石仍未建（建議 `大地圖/伏虎石.json`）；~~遺跡插秘籍場~~ **✅ 已寫入 `#4378`–`#4401`**（`#4164` links 改 `[4378]`；回讀 `劇情/趙王洞/03 遺跡.md` §3-6a）。**4 月與遺跡都尚未同步進 Unity**；遺跡增節點觸及鐵則 6，同步前先問「這個對話蓋過印記了嗎」 |
  | 說話者 | 「聲音」掛 `role2` |

- [ ] **呂信線 · Unity 要建**（`Json/呂信線/` 七檔 1 044 節點）。**⚠ 位置是暫置的，entryID 從 1 起編——搬檔與改號必須在同步前做完（鐵則 6）。** 明細見 `劇情/呂信/呂信.md` §8。

  | 類 | 內容 |
  | :--- | :--- |
  | 任務 `CF42` | **建議號**（全案 `CF04`–`CF41` 已用、`CF45` 娜娜、`CF46` 郭嘉建議；`CF42`–`CF44` 空）。**要在 Unity 確認沒被佔。** 8 個 entry：赤龍幫出錢／被拉起來／老兵三選一（算・放・帶）／留信三落點（正・反甲・反乙）。寫 13 處、讀 8 處，對照表見 `呂信.md` §7 |
  | 貴重品 | `UnsentLetter`（未寄的家書）、`WuYuanHorsemanship`（技能卡「五原騎術」）——**兩筆都還沒建** |
  | NPC | `role141` 馬賊（含哨騎）、`role142` 馬賊頭目、`role143` 老兵——**三個都要新建**；`role_Farmer`／`role_ZhaiZhu`（仗義疏財的農夫、寨主）仍是暫名待定 |
  | 戰鬥場次 | ✅ `78` 仗義疏財、`79` 五原障戰鬥一已填。⏳ **戰鬥二 `80` 只給一個號，強化版 `#2066` 要不要獨立號待定**；**戰鬥三（頭目）`#608` 未給號** |
  | 關卡 | 五原障箱庭的 `LoadLevel` ID 未定（`五原障.json` `#70`） |
  | 結局 | 結局圖 ID（`改名.json` `#30`）作者說**還沒做**；兩個 `ShowEnding` 編號（`#35`／`#36`）未定 |
  | 入口 | **五原障要開三個入口**：`#1` 書房派差事、`#40` 廣場（可重複點）、`#100` 進箱庭自動觸發 |
  | 變數 `LuxianChose` | 初始 2；正 +1、反 −2。**沒建的話正反判定全部悄悄失效、不會報錯** |
  | ⚠ 先修 | 全線旁白 `actorID` 寫成 `"2"`，全案慣例是 `"role2"`（461 節點）。同步後 entryID 不能改，actorID 還能改，但愈早愈省事 |

- [ ] **賈詡〈兩卷〉· 待進 Unity（2026-08-31 JSON 已轉）。** `Json/主線事件/180年/4月.json` `#1`–`#90`（入口 `#1`）。**同檔 `#91` 以後是娜娜第一頁，改本支不要動。** 創作稿 `劇情/賈詡/01 兩卷.md`。8 處佔位見該檔附錄二——**尤其 `#78` 任務 ID 不填則 6-A 永遠不播。**

- [ ] **郭嘉線 · Unity 要建**（`劇情/郭嘉/01`～`08`＋`@設定_雁門.md`）。資料夾已於 2026-08-29 `2948bcc` 上傳，另一台 pull 即可（本機 `05 嫌髒.md` 若還有未提交改動另說）。01–04 完稿、05–08 初稿待審。**`GuojiaChose` 沒建的話正反判定全部失效。** 另：07 讀 `IsFeatLevelPass("Player","MedicalExpertise",5)`，確認醫術等級上限 ≥5。

  | 類 | 內容 |
  | :--- | :--- |
  | 變數 `GuojiaChose` | 初始 2；正 +1、反 −2；第 7 支門檻 ≥3（作者 2026-08-29 定名）。`05`／`06` 寫、`07` 讀 |
  | 01–04 的任務 | ID 未定（建議 `CF46`，作者定），15 entry，各檔附錄一有對照表 |
  | 07 看診四顆 entry | 正・治成／正・未治成／反甲／反乙（08 開春讀） |
  | 情報網 #19 | 雁門・山村，郭嘉捎來（06 入口） |
  | 08 結局圖 | 兩張（暫記 `GuoJiaEnding_Clear`／`GuoJiaEnding_Packet`）＋兩個 `ShowEnding` 編號 |

- [ ] **已改、待 Unity 端編譯確認** DialogueJsonBridge 補排疊在原點 (0,0) 的舊節點（2026-08-29）。成因：2026-08-28 以前的版本匯入時不給座標，節點全落在 (0,0)；上一條的自動排版只管「這次新增」的節點、既有節點不動，而且沒差異的檔不會跑同步，所以舊的 (0,0) 一直留著。改法：(1) 同步時把該對話疊在原點的節點連同新增節點一起排（確認視窗會列出「疊在原點的舊節點 N 個」）；(2) 匯入頁新增按鈕「把疊在原點 (0,0) 的節點排進畫布（整個資料庫）」，不碰文案、可 Ctrl+Z；整段全在原點的對話走整段重排，只有部分在原點的沿資料庫連線掛到上游節點下方。排版改沿資料庫連線找上游（原 `AutoLayoutNewEntries` 併入 `LayoutUnplacedEntries`）。另一台 pull 後覆蓋進 Unity、開一次確認無紅字，再按那顆按鈕清掉現有的 (0,0)。
- [ ] **已改、待 Unity 端編譯確認** DialogueJsonBridge 匯出時替重複的 JsonEntryID 印記改號（2026-08-29）。事件：`Json/大地圖/水濂洞.json` 從 Unity 匯出後 entryID 4175 出現 4 次（另 4184／4230／4297／4298／4301／4302 各 2 次），同步被「entryID 重複」擋下。成因：Unity 裡複製貼上節點會連 JsonEntryID 印記一起複製，匯出就寫成同號；這 9 個副本是 fd92d0f（2026-08-27）重建 JSON 時就已剔除的死節點，但 Unity 端一直沒清，每次同步都被當孤兒放著，匯出又冒出來。處置：(1) JSON 已修回 349 個節點（以 28d144c 為準、保留 Unity 給的 13 個 title），可直接同步；(2) Bridge 匯出時重複印記的後出現副本自動改配新號（最大號＋1 起）並寫回資料庫、Console 列出，文案端在 JSON 刪掉即可於下次同步移除；匯入時印記重複也會在 Console 警告。⚠ 作者 2026-08-29 已在 Unity 刪掉「大地圖/水濂洞」對話再重建：其他對話指向它的跨對話連線（若有）會失效，新對話 ID 也不同，Unity 端要核一次進洞入口。
- [ ] **第二章結算場 · Unity 要建（2026-08-30）。** `劇情/舌戰三家.md`（雙投）與 `劇情/填補赤字結算.md`（獨家）。**JSON 已轉完，待 Unity 建／接線。** 門見 `劇情/填補赤字_觸發分流.md`。

  | 類 | 內容 |
  | :--- | :--- |
  | 任務 | ✅ **總任務＝`C2M1`**（`主線事件/180年/3月.json` `#158` 開）。獨家版寫 `SetQuestState("C2M1","success")`；雙投版寫 `SetQuestEntryState("C2M1",4,"success")`。**entry 4＝三家危險平衡成立，只有雙投版寫**——182 春破局靠「有沒有 entry 4」分流。⚠ **entry 4 這個號要在 Unity 確認沒被佔** |
  | ⚠ **entry 2（甄家）還沒掛** | 位置已標在 `劇情/水濂洞/水濂洞.md`「⚠ 作者：『投資成立』任務狀態掛此」那三格（`#208`／`#215`／`#219` → `success`）與拒絕合作的 `#4292`（→ `failure`）。**不補，結算場的分流與觸發條件都讀不到甄家那一路** |
  | 對話 | ✅ **四份已轉**：`主線事件/180年/9月.json`（15 節點）、`11月.json`（10）、`填補赤字結算.json`（37）、`舌戰三家.json`（333）。三語欄位齊、連通性已驗。另有 11 月下旬饕餮骰揭露、12 月上旬董卓認子、`12月.json` 都未建 |
  | ⚠ **要建 14 個變數** | `舌戰三家.json` 用 `＿＿01`–`＿＿14` 佔位記「講過哪幾條／哪一條敗過／背景洞悉過沒過」。**用途逐一寫在該節點的 `Description` 欄位**，對照表見 `劇情/舌戰三家.md`〈附錄五〉。**沒建之前，選項 4／5 與三家第二拍的「需洞悉成功」那一條永遠不會出現** |
  | ⚠ **`舌戰三家.json` `#293` 曲號** | `AudioControl(PlayMusic,＿＿)`——全檔唯一換曲點（賈詡起身），待作者填；現行全案已用到 `BGM_47` |
  | ⚠ **跨對話連線（全案第一次）** | 月檔只放分流門與季節首格，結算場是獨立對話。**`links` 表示不出來，Unity 端要自己接四條**：`9月#4`→舌戰三家、`9月#8`→填補赤字結算、`11月#6`→舌戰三家、`11月#10`→填補赤字結算。另有兩條斷頭待該檔建好：`11月#2`→饕餮骰揭露、`11月#9`→乞丐結局。**交接處刻意不關背景**，關背景在結算場自己的尾格 |
  | 觸發點 | ✅ **劇本已寫：`劇情/填補赤字_觸發分流.md`。** ⛔ 沒有常駐對話點（作者裁示：玩家不必回箱庭回報）——就是兩次主線強制事件，各讀 `C2M1` entry 1／2。**JSON 已建**（`9月.json`／`11月.json`）；**Unity 端兩處都還沒建**。11 月同檔還有饕餮骰揭露（未建），結算排在它前面 |
  | 立繪 | ✅ **`MC11` 黑狼王＝五立繪**（作者 2026-08-30 確認）。`劇情/舌戰三家.md` 26 句已補 `SetPortrait(MC11,pic=1`–`5)`；`給AI看的指南/立繪指令轉換規則.md` §2／§4.3 已登記 |
  | 音樂 | 舌戰三家 §Ⅳ 4-0 賈詡起身那一格 `AudioControl(PlayMusic,＿＿)` **曲號作者自己處理**。獨家版與回報入口**都不設換曲點** |

- [ ] 黑鐵嶺分支條件確認。`Json/大地圖/黑鐵嶺礦坑.json` 各入口分流與三選一（殺伐／救贖／血刀）的 `Conditions`。要看分支裡任務有無更新：`CF16`（條目 1–5 收場）、`CF17` 寒鐵、`CF21` 血池等 `SetQuestState`／`SetQuestEntryState` 是否仍對得上現行分流。呂信線 `英豪府遴選/開場、子羽、呂信.json` `#4059`／`#4061` 也用 `CF16` 當條件，一併核。對照 `劇情/黑鐵嶺礦坑/黑鐵嶺礦坑.md`。對不上先記，不要默默改任務表。
- [ ] **趙王洞白虎道妖 · 待同步 Unity。** **✅ 2026-08-31 確認 JSON 已是新稿**：`#45`–`#48` 為「幫我／不開口」二問（舊三連問 `#49`–`#56` 已不在）。`#4243` 以上新增、**尚未進 Unity**，同步前先問「這個對話蓋過印記了嗎」（與遺跡秘籍場同一風險）。不要再當「確認舊稿還是新稿」重做一遍。

## 卡在作者

agent 不得自行決定。

- [x] **已裁示維持** 戰敗即 `ShowEnding(Ending_1)`（Game Over）。作者 2026-08-28 裁示**刻意這樣設計，全案不要改**——`宗緯家`、`小溪村`、`小溪村後山`、`水濂洞`、`破廟`、`雲中`、`黑鐵嶺礦坑`、`179年 支線事件`、`趙王洞/山賊窩` 都是同一個寫法。`趙王洞_白虎道妖_改動追蹤.md`〈複-1〉的改案不採納。記在本檔以免下一輪又去「修」。
- [x] **已裁示維持** [panel] 剩 23 格。三個多人同場檔案。作者已裁示維持現狀、不要改。記在本檔以免下一輪又去「修」。
- [ ] **第二章 · `C2M1` entry 4。** 雙投版寫它當「三家危險平衡成立」。要在 Unity 確認沒被佔，沒定就不要自創。
- [ ] **第二章 · 甄家 entry 2 還沒掛。** 水濂洞 `#208`／`#215`／`#219` → `success`、`#4292` → `failure`。不補則結算分流讀不到甄家那一路。
- [ ] **第二章 · 舌戰三家 14 個變數正式名。** JSON 佔位 `＿＿01`–`＿＿14`，用途在各節點 `Description` 與 `劇情/舌戰三家.md`〈附錄五〉。沒定名就不要自創。
- [ ] **第二章 · `舌戰三家.json` `#293` 曲號。** `AudioControl(PlayMusic,＿＿)`，作者自己填。
- [ ] **賈詡〈兩卷〉· 你要填的空。** 任務 ID 與 3 顆 entry＋`Description`；`#81`／`#88` 好感增減；`#88` 賞錢。對照 `劇情/賈詡/01 兩卷.md` 附錄二。沒定號就不要自創任務編號去建。
- [ ] **赫連娜娜 · 你要填的空。** 虎腹戰鬥 ID；背景 `Desert`／`Yard`／`Cave`、音效 `Others_StoneDoor` 核 ID；勸阻好感門檻（提案第 3 級，`IsFavorabilityLevelPass("MC22", 3)`）；元祖場要不要事件圖。
- [ ] **赫連娜娜 · 你要裁的。** 名號「玄妍」；「第四層＝她自己的決定」；放下線「天機盤不轉了」留不留；虎腹守門的東西；10 檔選 1 加不加好感。
- [ ] **郭嘉 · 01–04 任務 ID。** 未定（建議 `CF46`），15 entry。沒定號就不要自創任務編號去建。
- [ ] **郭嘉 · 你要填的空。** 06 兩個戰鬥 ID；07「府將散」掛哪個任務狀態／年月；各支好感數字。
- [ ] **郭嘉 · 你要裁的。** 那東西的本名、「更遠是哪裡」；05 §2「收人先問你家裡死了幾個」兩句留不留；05／06／07／08 正式檔名；04 檔名〈其所不見〉要不要改〈無人知〉。
- [ ] **郭嘉 · `01 逝者無聲` 放哪。** 三人共用支線，放郭嘉資料夾與 CLAUDE.md「跨角色共用不搬」相牴，要嘛加例外要嘛搬回根層。
- [ ] `BrokenWall_Right` 方向。`英豪府遴選/開場、子羽、呂信.json` #481（#484 收特效）。方向是作者選的 Right（不是 Left），待作者在遊戲裡複核畫面方向對不對。同 ID 也出現在 `大地圖/青霧林/不知名的野外.json`。對照 `給AI看的指南/畫面指令轉換規則.md`。
- [ ] `Json/主線事件/180年/6月.json` 是空陣列 `[]`。索引與 `劇情/180年事件/` 已註明無可回讀。刪或留或等內容，等作者裁示，不要自己填劇情。
