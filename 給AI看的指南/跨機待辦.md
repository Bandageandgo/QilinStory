# 跨機待辦

對話不會跟 git 走；兩地只認本檔。明細仍在 `劇情/索引.md`。做完刪列或打勾。

最後更新：2026-09-03

## 現在要做

- [ ] **Unity 要對呂信任務(2)〈第三百四十九次〉（2026-09-03 作者新增）。** 比照已進 Unity 的任務(1) `01仗義疏財`（節點 `title`：`呂信事件(1)仗義疏財/180年3月支線`）。**這支是第 2 支**：對話 `呂信線/02第三百四十九次`（234 節點、入口認 `JsonEntryID` 不要認 Inspector ID）。

  | 要在 Unity 做 | 說明 |
  | :--- | :--- |
  | 任務表 | 建「呂信事件(2)」／第三百四十九次那一筆，接到這份對話。**JSON 裡沒有 `SetQuestState`**（本支 0 個任務佔位），開／收任務在 Unity 端自己掛，**不要自創 `CF42` entry 寫進本檔**——`CF42` 的 8 顆 entry 是五原障／留信用的，見 `劇情/呂信/呂信.md` §7 |
  | 變數 `LuxianChose` | ✅ **已建、初值 `2`**（2026-09-03 作者確認）。本支 §4-1 寫 `+1`／`−2`（三套各一份） |
  | 對話 key | 標題／`JsonSource` 必須是 `呂信線/02第三百四十九次`（已從舊名改過）。對不到會另建一份 |
  | 場景 | 英豪府・演武場；JSON 已開 `EnableDialogueBG(TrainingGround)` |
  | ⚠ 先匯出 | 這支 Unity 曾手改過。**不要拿創作稿那 12 處還沒套上的 repo 去蓋 Unity**；要對任務就對任務，文案以 Unity／已匯出 JSON 為準 |

  明細：`劇情/呂信/02 第三百四十九次.md`、`劇情/呂信/呂信.md` 第 2 支。

- [ ] **Unity 要對妖林、幽冥禁地的對話（2026-09-03 作者新增）。** JSON **已匯入**（作者稍早灌過）：`大地圖/妖林.json`（12 節點、入口 `#1`，收 `CF45` entry 4、《異聞錄・化妖考》`WanderingNotesPage3`）、`大地圖/幽冥禁地.json`（13 節點、入口 `#1`，收 `CF45` entry 3、《遊方雜記・卷三》`WanderingNotesPage2`）。**接下來是箱庭裡把互動點接到這兩份對話**，不是再匯入一次。

  | 對話 key | 地圖 ID | 要對什麼 |
  | :--- | :--- | :--- |
  | `大地圖/妖林` | `DemonForest` | 掛在地城哪個物件（放置點在殷紅將那層，作者排） |
  | `大地圖/幽冥禁地` | `NetherForbiddenGrounds`（⚠ 有 s） | 掛在哪一重的哪個物件 |

  兩份都**不寫撿到的環境**，放置點可挪、文案不必改。其餘互動（雜兵、殷紅將、五重機關、份四～七的卷）要不要併進同一份 JSON，另裁。設定見 `劇情/妖林/@設定_妖林.md`、`劇情/幽冥禁地/@設定_幽冥禁地.md`。

  ⚠ 現行 `#1` 仍是 `ModifyData(Valuable,MC22,…)`；`ShowValuableMemo` 讀的是玩家手上那件。若對上之後便籤開不出來／甲式攔截卡死，先把 `#1` 改成 `Player` 再匯入一次（索引已記，JSON 還沒改）。

- [ ] **⛔ `主線事件/180年/11月.json`：四項中兩項已修，兩項還在（2026-09-03 第二輪回讀）。** ✅ 已修：`#4` 月份、`#4335` 孤兒（已刪，354 節點單一入口）。⏳ 未修：見下。 作者把 11 月從 12 節點改成 355 節點、自己收整場（不再跨對話呼叫 9 月）。
  1. **⚠⚠⚠ 「已經結算過就跳過補播」的判斷不見了。** 舊版 `#2` 掛 `CurrentQuestState("C2M1") == "success";` 讓已結算的玩家跳過；**現行全檔 `CurrentQuestState` 出現 0 次**，`#1` 直接照 9 月的樣子判 entry 1／2 → **9 月已經結算完的玩家，11 月會被抓去把整場再演一次。**
  2. **`#4` 還是九月的旁白**（「九月下旬。院裡的棗樹落了一地」）。舊版 11 月檔寫好的冬天版（「十一月下旬…廊下結著霜…兩卷帳…一卷一卷點過去」）在回讀稿〈疑點〉第 2 條。
  3. **`#8` 的文字貼錯**：它是「談成一家 → 進獨資版結算場」那條，卻寫著封門抄家的旁白（與 `#7` 逐字相同），下一句是小犀「三萬六千！湊齊啦！」，`Sequence` 還掛 `BGM_19`。冬天版原文在〈疑點〉第 3 條。
  4. **`#4335` 成了孤兒**（無連入、無連出），`Script` 卻會把 `C2M1` 設 `active` 並把 entry 1、2 設 `success`。**它有 `JsonEntryID` 印記，Bridge 的死節點清除掃不到。**
  - 另兩件小的：`#7` 缺 `[panel=6]`、`SetPortrait(MC4,pic=1)` 沒有對象；`#10` 主角那句由「先收著。回頭再掛。」變成「……」（是否刻意待確認）。
  - **⚠ 維護面加倍**：本檔 345 格與 `9月.json` 逐字重複，**改任何一句台詞都要兩檔各改一次**。另 `Variable["BreakAlliance"]` 兩檔共用，補播前要確認有沒有重設。
  - 明細見 `劇情/180年事件/11月(主)填補赤字補結算.md`〈回讀時發現的疑點〉。


- [x] **✅ 已完成（2026-09-03）：`呂信線/01仗義疏財.json` 四處修正已匯入 Unity，兩邊同步。** 未增刪節點（仍 108）、`links` 未動。

  | # | 改動 | 為什麼 |
  | :-: | :--- | :--- |
  | 1 | `#1001` **刪掉** `Conditions: IsPassFight() == true;` | 戰鬥拆掉後的殘骸，全檔 `BeginFight` 已是 0。`#1001` 是 `#99` 的**唯一**出口，留著會讓酒量檢定成功那條停在 `#99` |
  | 2 | `#981` `DisableCharacterExpression(1)` → `(2)` | `#98` 開的是位置 2 的 `Anger_3`，原本關位置 1（空轉），生氣表情會留到尾格 |
  | 3 | `#115` `BloodSplash_Middle,Stop` → `Sword_Multiple_ScreenRed,Stop` | `#114` 開的是後者；`BloodSplash_Middle` 全案沒有任何一處 Play 過 |
  | 4 | `#119` 補 `StopAllParticle();` 與 `OpenPanel(2, close)@1;` | 酒量失敗那條呂信站 `[panel=2]`，原本沒關；轉場前清特效見 `畫面指令轉換規則.md` §3.3 |

  **✅ 已複驗**：108 節點、無重複／斷鏈／孤兒；`IsPassFight`／`BeginFight` 皆 0；剩下的 `Conditions` 只有 `#39`／`#43`（洞悉）與 `#88`／`#110`（酒量）四個檢定分支；特效 Play/Stop 全配對；走完全檔沒有任何表情沒關。

  **⚠⚠ 匯入前必讀：Unity 的 Inspector `ID` ≠ JSON 的 `entryID`。** `AndyCursor用/DialogueJsonBridge.cs` 建立**新**對話時是依 JSON 陣列順序重發流水號（`GetNextDialogueEntryID`，START=0），JSON 的 `entryID` 存進節點的 **`JsonEntryID`** 欄位。**所以「砍掉本地對話重新匯入」會讓兩套號碼永久脫鉤**（本檔 2026-09-03 就踩過：repo `#104` 在 Unity 是 86）。
  - **在 Unity 找節點請認 Dialogue Text 或 `JsonEntryID` 欄位，不要認 ID。**
  - **這次直接匯入（不要先砍）**：Bridge 走「更新既有對話」那條路時是依 `JsonEntryID` 印記精準對應的，不會重新發號。


- [x] **✅ 已匯入 Unity（2026-09-03 作者完成）：`主線事件/180年/9月.json`。** 360 節點，本輪共動 15 處、**未增刪節點、`links` 未動**：
  - 作者改的 12 處：甄筠 `MC9` 由 `[panel=2]` 移到 `[panel=4]`（9 句）、`#4366` 補 `OpenPanel(2/5, close)`、`#291` 拿掉 `StopMusic`＋`BGM_47`、`#143` 拿掉 `BGM_38`。
  - 回讀時補的 3 處：**`#305` 補 `OpenPanel(4,close)@1;`**（甄筠移到 panel 4 後，三家離場沒人關她的立繪）、`#143` 連 `StopMusic` 一併拿掉（否則到 `#212` 有 23 格無音樂）、`#4356` `SetPortrait(MC9,pic=4)` 改回 `pic=1`（誤改）。
  - **`Variable["BreakAlliance"]` 已在 Unity 建好、初值 `0`**（作者 2026-09-03 確認）。**⏳ 只剩 `C2M1` entry 3 要確認號沒被佔**（`#325`）。
  - 回讀稿：`劇情/180年事件/09月(主)填補赤字結算.md`（§3 已於 2026-09-03 依併檔後新編號整段重生成，360 節點全對過）。

- [ ] **張寧線 · 設計端已建、劇本未寫（2026-09-03）。** `劇情/張寧/張寧.md` 第五版提案。第 1 支〈那一碗〉設計已拍板，其餘支仍提案；**一支 JSON 都沒有。** 不要當 Unity 要建去掃。

- [x] **✅ 已全部解決（2026-09-03）：三場的 repo 版與 Unity 已同步。** `呂信線/01仗義疏財`、`赫連娜娜線/08第一頁`、以及 `呂信線/02第三百四十九次`（已改名加序號、230→234 節點，**⏳ 創作稿還沒依新 JSON 回讀**）。原記錄——repo 那份還是舊稿。原記錄：這台 Unity 已修訂三場：①`呂信線/仗義疏財` ②`呂信線/第三百四十九次` ③`赫連娜娜線/08第一頁`。repo 仍是舊稿。**這台先從 Unity 匯出這三份再 commit**；另一台 pull 後匯入。**不要用 repo 舊 JSON 蓋 Unity。**
  - ✅ **2026-09-03（第二輪）：`08第一頁.json` 已照最新稿整份重寫，34 → 28 節點。**
    **這一份是正本，Unity 端請直接匯入它。** ⚠ **代價講明**：Unity 那邊若還有未匯出的手改，匯入時會被蓋掉——要保留就先匯出比對再決定。新稿全文見 `劇情/赫連娜娜/08 第一頁（180年4月主線）.md`（檔頭列了作者這輪的九條指正）。

    **作者拍板的兩條前提**（整場骨架都從這裡長出來）：**①她在市集攤子上就認出爹的字**——發現不在畫面上，這場戲演的是「她怎麼開這個口」；**②卷二便籤在她把卷子攤給主角看的那一下開**——玩家先讀完全文，**此後任何角色都不得再複述卷子內容**。

    | # | 改動 | 節點 |
    | :-: | :--- | :--- |
    | 1 | **簡帛考據修正**（東漢仍以簡牘為主，「簡帛」是現代合稱、帛不論斤賣；北地漢簡多木簡）：擔子裡改成**簡**、她翻著那卷仍是**紙** | `#94`／`#95`／`#97`／`#106` |
    | 2 | **開卷二便籤**：`ModifyData(Valuable,...)` 掛在新的 `#126`（「你自己看」），緊接 `#125` 空節點跑 `ShowValuableMemo(WanderingNotesPage1);`（甲式攔推進） | **`#126` 改寫、`#125` 新增** |
    | 3 | **併成一場**：原入口二 `#115`–`#124`（隔日）整段刪除 | **刪 10 格** |
    | 4 | **刪 `#100`**（「接過去，只看了一眼，手就停住了」）——前提①，發現不能在畫面上再演一次。連帶 `#101` 拿掉 `DisableCharacterExpression(1);`（`#100` 的 Surprise 沒了） | **刪 1 格** |
    | 5 | **刪 `#105`**（「這一卷本姑娘收了」）——**東西本來就是她自己翻出來、自己買的，沒人跟她搶**。收卷與離開併進末尾 `#111` 旁白 | **刪 1 格** |
    | 6 | **刪 `#127`／`#128`**（南下晉陽、兩個地方一東一西）——玩家已從便籤讀到，角色不複述 | **刪 2 格** |
    | 7 | **地圖 ID 更正**：`NetherForbiddenGround` → **`NetherForbiddenGrounds`**（有 s）。`MapLock` 兩條現在掛在 **`#134`**（主角「帳先擱著」） | `#134` |
    | 8 | **小犀有立場了**：`#131` 由主角句改成她的（`actorID` `MC1`→`MC8`，晉陽來回六七天、這個月帳沒平）；`#106` 改問「上頭寫著卷二，那卷三呢」 | `#131`／`#106` |
    | 9 | **對白重寫**：開場旁白、`#102`（「他來過這種地方」無指涉 → 改問她是不是一路跑回來）、`#103`（拿掉驚嘆號，改成避而不答）、`#104`（她早讀過，改成指給他們看）、`#132`／`#133`（把手改輕）、`#107`／`#108`（卷是散佚的，她那一漏）、`#134` 改成主角自己的決定、`#111` 末尾旁白 | 全場 |

    **流程**：`#91`–`#97` → **`#126`** → **`#125`**（便籤）→ `#98`–`#99` → `#101`–`#104` → `#129`–`#133` → `#106`–`#108` → **`#134`**（`MapLock`）→ `#110` → `#109`（`CF45`）→ `#111` → `#114`（尾格）。**入口只剩 `#91`，Unity 端要收掉第二個入口。**
    **已驗**：28 格全有 `zh_CN`、`zh_TW` 與 `text` 一致、`[/em7]` 後都有標點、`title` 只有 `#91`（作者的，未動）、連線一條到底無孤兒。
    **✅ 貴重品 `WanderingNotesPage1`～`7` 在 Unity 已建檔**（作者 2026-09-03 確認）。
    **⚠ 連帶：`09 爹的筆記（CF45）.md`〈份一・拿到時〉已由六格砍到四格**，兩邊要一致。

  - ⚠⚠ **2026-09-03（第三輪）：作者又改了 `08第一頁.json`，28 → 31 節點，repo 版已回讀進 `08 第一頁（180年4月主線）.md` 與 `09 爹的筆記（CF45）.md`〈份一〉。**

    | # | 作者的改動 | 節點 |
    | :-: | :--- | :--- |
    | 1 | **主角收尾句換掉**：「……帳先擱著。」→「呼，果然路子還是在外頭啊，說不定找著寶貝，府裡這筆虧空就填上了呢。」（不正面表態，講府裡的窟窿） | `#134` |
    | 2 | **那道門拆成兩句演**：先講成聽來的傳聞、自己還不信，再由卷子坐實，並補「小犀，妳那本帳，說不定就在門後頭擱著」 | `#133` 改寫、**`#4068` 新增** |
    | 3 | **新增收尾兩格**：娜娜「那兩處都在晉陽那一帶」＋系統旁白「（發現：幽冥禁地、妖林。）」；`MapLock` 兩條移到後者 | **`#4069`／`#4066` 新增** |
    | 4 | **卷二轉手**：`#4066` 把 `WanderingNotesPage1` 從 `Player` −1、`MC22` +1（東西最後在她手上） | `#4066` |
    | 5 | **小犀的問句全部重寫**；全場補齊表情特效 | `#129`／`#131`／`#106` 等 |

    **✅ 已驗**：31 格一條直線無分支、單入口 `#91`（`title` 作者的，未動）、單結尾 `#114`、無重複 entryID／斷鏈／孤兒；**表情特效 17 組開關全部配對**；便籤走甲式且排在 `ModifyData(Valuable,…)` 之後（合 `貴重品指令轉換規則.md`）；`NetherForbiddenGrounds`／`DemonForest` 兩個 ID 全案都在用。

    **✅ 三件已裁示維持現狀（作者 2026-09-03）。⛔ 下一輪不得當成錯誤去「修」：**
    1. **尾格 `#114` 只寫 `SetQuestState("CF45", "active");`，刻意不收 entry 1／2。** 前一版 JSON 有這兩行，作者已決定拿掉。`09 爹的筆記（CF45）.md` 那張 entry 表僅供對照，不是補回來的依據。
    2. **`#4068` 的 `SetPortrait(MC22,pic=11)` 維持。** `立繪指令轉換規則.md` §4.4 說 `11` 是別名格、疑惑建議寫 `14`——**本格是刻意的例外，不適用那條建議。**
    3. **卷二在 `#4066` 提早轉回娜娜身上（早於主角「先給妳」三格）維持。** 不要挪到 `#109`／`#111`。

  - ⚠ **2026-09-03 補：`呂信線/第三百四十九次.json` 也已改名並整批修過**（作者裁示「都改吧」）。**檔名 → `02第三百四十九次.json`；228 → 234 節點。** ⚠ **Bridge 用相對路徑當對話 key**，`呂信線/第三百四十九次` → `呂信線/02第三百四十九次`，**Unity 端的對話標題（或 `JsonSource` 印記）要一起改**，否則下次匯入會多長一份。改動：①12 個選項節點 `actorID` `0`→`MC1`（`#32`／`#33`／`#34` 與 2xxx／3xxx／4xxx 三份的 `#x020`／`#x030`／`#x040`）②補 `zh_TW` 215 格 ③`#1` 開 `EnableDialogueBG(TrainingGround)`、六條結尾各補尾格（`#2072`／`#2082`／`#3072`／`#3082`／`#4072`／`#4082`）④**呂信 60 格補 `[panel=1]`**（原本全檔沒有，表情卻用位置 1）⑤`#31`／`#4080` 兩處文字修正 ⑥`#19` 去掉被覆蓋的 `SetPortrait(MC2,pic=4)`。**⚠ 這一檔 Unity 也已修訂未匯出，同樣要先比對。**

  - ⚠ **2026-09-03 補：`呂信線/仗義疏財.json` 的 repo 版已改名並修過**（作者指出的三個問題）。**同樣不可直接匯入 Unity**——先從 Unity 匯出，再把下列改動套到匯出檔上：

    | # | 改動 | 節點 |
    | :-: | :--- | :--- |
    | 1 | **檔名改成 `01仗義疏財.json`**（比照 `賈詡線/01兩卷`、`赫連娜娜線/08第一頁`）。⚠ **Bridge 是用相對路徑當對話 key**（`ResolveConversationKey`），`呂信線/仗義疏財` → `呂信線/01仗義疏財`；**key 含「/」時對不到就直接當新對話另建一份**，所以 **Unity 端的對話標題（或 `JsonSource` 印記）要一起改成 `呂信線/01仗義疏財`**，否則下次匯入會多長一份 | — |
    | 2 | **選項節點掛回主角的 `actorID`**：`0` → `MC1` | `#51`／`#53`／`#55`／`#67`／`#70`／`#85` |
    | 3 | **補 `zh_TW`**（原本 99 個有文案的節點一個都沒有；繁體正文＝`text`） | 全檔 99 格 |
    | 4 | **補對話背景圖**（原本全檔沒有 `EnableDialogueBG`）：`#1` 開 `Lnn`；`#881` 的 `@1` 槽加 `EnableDialogueBG(Yunzhong)@1;`（客棧內→門外） | `#1`／`#881` |
    | 5 | **新增 2 個節點（鐵則 6）**：`#119` 轉場回客棧（`EnableDialogueBG(Lnn)@1`，三條路 `#104`／`#109`／`#117` 匯合前，因為 `#120` 起是算酒錢、後院刷碗）；`#127` 尾格（`DisableAllCharacterExpression();DisableDialogueBG();Continue();`，`#126` links 改 `[127]`） | **111 → 113 節點** |

    **未動**：`role_Farmer`／`role_ZhaiZhu` 兩個暫名 actorID（待作者定號）。

  - ⚠⚠ **2026-09-03（第二輪）：作者又改了 `01仗義疏財.json`，repo 版已回讀進 `劇情/呂信/01 仗義疏財.md`。** 108 節點、入口 `#4314`（作者新建，帶 `title`）、單一結尾 `#127`；全部可達、無斷鏈、無重複 entryID。

    | # | 作者的改動 | 節點 |
    | :-: | :--- | :--- |
    | 1 | **戰鬥整段拿掉**：`BeginFight(Combat,78)` 那格改寫成 `StopAllParticle();Continue();`，戰鬥緩衝格與**整條戰敗分支**刪除，改用刀光特效帶過 | `#99` 改寫；刪 `#100`／`#105`–`#109`／`#1061` |
    | 2 | **NPC `actorID` 佔位填掉** | `role_Farmer`→`role84`、`role_ZhaiZhu`→`role87` |
    | 3 | **門外那段換 panel**：呂信 `1`→`2`、寨主 `2`→`1`，表情位置參數跟著改 `2` | `#90`–`#98`、`#111`–`#116` |
    | 4 | **改字一處、拿掉 `[em7]` 前綴兩處** | `#681` 改字；`#41`／`#123` 去前綴 |
    | 5 | **加刀光特效**、`#101` 加 `SetPanel(MC2, 1, immediate)` | `#981`／`#114`／`#115`／`#101` |

    **⚠⚠ 這四件事在確認之前就跟著匯進 Unity 了（2026-09-03）——現在是線上的問題，不只是 repo 的問題。第 1 項會斷線，見〈現在要做〉第一條：**
    1. **`#1001` 還掛 `Conditions: IsPassFight() == true;`，但全檔已無任何 `BeginFight`。** 它是 `#99` 的**唯一**出口——條件不成立這一場就走不下去。**會斷線的硬傷。**
    2. **`#981` 關錯表情位置**：`#98` 開的是位置 `2`（`Anger_3`），`#981` 關的是位置 `1` → 生氣表情一路留到尾格 `#127` 才消。
    3. **`#115` 關錯特效 ID**：`#114` 開 `Sword_Multiple_ScreenRed`、`#115` 關 `BloodSplash_Middle`（從未開過）→ 酒量失敗那條的刀光沒關；`#119` 轉場也沒有 `StopAllParticle();`。
    4. **`#119` 只關 panel 0、1**，但酒量失敗那條呂信站 `[panel=2]` → 立繪留在畫面上，`#121` 又讓他在 panel 1 出現一次。


- [x] **✅ 已重新匯入 Unity（2026-09-03 作者完成）：`主線事件/180年/9月.json`。** 錯誤條件式已還原，兩邊同步。以下留作紀錄。

  **當初做錯的事：** AI 把 `#79`／`#147`／`#216` 當成「擲骰的成功分支」，補上 `Conditions: IsPassDice() == true;`。**它們其實是匯流點**，從好幾條路進來——

  | 節點 | 入口 |
  | :--- | :--- |
  | `#79` | `#78`（擲骰）＋ **`#768`**（`BreakAlliance` 那條，玩家**檢定失敗**才走到） |
  | `#147` | `#146`（擲骰）＋ **`#156`**（甄筠回應「認錯」選項，**沒擲骰**） |
  | `#216` | `#215`（擲骰）＋ **`#225`**（沒擲骰）＋ **`#231`／`#240`**（兩條都是**檢定失敗**） |

  掛上擲骰條件之後，那些非擲骰／擲骰失敗進來的路**全部被擋死**，玩家會卡在上一格。**同日發現、還原成空條件，作者已重新匯入，Unity 兩邊一致。**

  **教訓（寫給下一輪）：擲骰分支的成功那半留空條件，往往不是漏寫，是因為那一格同時是別條路的匯流點。動 `Conditions` 之前先查入口。**

  **同批另外 7 處是好的，不必動**（作者逐條核可）：

  | # | 節點 | 改動 |
  | :-: | :--- | :--- |
  | 1 | `#307` | 呂信「師弟」→「**小子**」（英豪府不排行，文風指南四之二硬規則） |
  | 2 | `#29` | 徐榮「資金」→「**銀錢**」（甲類） |
  | 3 | `#30` | 徐榮「報酬」→「**酬金**」（甲類） |
  | 4 | `#281` | 褚人飛「你還不夠格」→「**你壓不住這張案**」（「夠格」現代白話；換成他自己的案面意象） |
  | 5 | `#34` | 主角「又是這齣戲碼重演」→「**怕是照樣再來一遍**」（「重演」現代語） |
  | 6 | `#310` | 賈詡「將近三倍**有餘**」→「將近三倍**來**」（「將近」＋「有餘」打架） |
  | 7 | `#4384` | 半形括號 → **全形**「（看向郭嘉。）」 |

  三欄同步（`text`／`zh_TW`／`zh_CN`），節點數不變（360）。**回讀稿已同步**：`劇情/180年事件/09月(主)填補赤字結算.md` L254／257／268／430／438／974／1051／1060。
  **⛔ 作者裁示不動的**：`#4335` 測試節點（「登登登，登大人囉老鐵」，孤兒節點但有 `title`，會把 `C2M1` entry 1／2 設 success）；甄筠 `#143`–`#208`／黑狼王 `#212`–`#272` 共 43 格缺 `[panel]`；`#75`「一絲不易察覺的微笑」；`#4339`「做得漂亮」；panel 跳號。**下一輪不要再去「修」。**

- [ ] **`呂信線/02第三百四十九次`：創作稿已修 12 處，JSON 還沒動（2026-09-03）。**

  **⚠ 順序不能顛倒。** 這支目前是三層不同步：**`.md` ← 舊於 → `02第三百四十九次.json` ← 舊於 → Unity**（Unity 已修訂、JSON 未匯出）。所以**先從 Unity 匯出 JSON**，再把下表套上去，最後回讀進 `.md` 核對。**不可拿 repo 的 JSON 蓋 Unity。**

  **另外：檔案已改名 `第三百四十九次.json` → `02第三百四十九次.json`。** 比照 `01仗義疏財` 的前車之鑑——**Bridge 拿相對路徑當對話 key**（`ResolveConversationKey`），`呂信線/第三百四十九次` → `呂信線/02第三百四十九次`；**Unity 端的對話標題／`JsonSource` 印記要一起改**，否則下次匯入會多長一份。

  | # | 改動 | 節點 |
  | :-: | :--- | :--- |
  | 1 | **拿掉判語**：「有一圈被踩得比較實的痕跡——`[em2]`他等了不只一時半刻`[/em2]`」→「……有一圈被踩得比較實的痕跡，**繞著石墩轉了一整圈**」。7.1b 第 1 條：旁白擺痕跡，不宣布結論（更不該用 `[em2]` 把結論畫重點） | `#9` |
  | 2 | **四格併兩格**（作者在 7.1b 親判「他放碗放得很輕」該進 `[em7]`，但它隔三格才有台詞、併不過去；改依「旁白格太短就跟隔壁併格」處理）：`#2007`＋`#2008` 併成一格；`#2009`＋`#2010` 併成一格，並拿掉「**你忽然懂了：**」（明著替玩家宣布），改成「他不是在數贏了幾次——`[em2]`他是在數還剩幾次`[/em2]`」，讓整串推理有頭有尾（7.1b 例外：整串留可以，孤句判語要改） | `#2007`–`#2010`／`#3007`–`#3010`／`#4007`–`#4010`，**三套各減 2 格** |
  | 3 | **分支一致性**：`§6` 共用旁白列了「倒了一半的兵器架、地上的沙」——**兵器架只有 3-1 成功才倒、沙只有 3-2 才有、3-3 根本沒打**。改成「＊（他掃了一眼場上——兩個空酒罈，和坐在石墩上的兩個人。）＊」 | `§6` 那格 |
  | 4 | **選項 2 沒有依據**：「三百四十八次**都贏**」——洞悉成功那條他根本沒答（「那種事有什麼好記的」），失敗那條也只說「當然是我」（贏得多≠都贏）。改成「**打了三百四十八次還嫌不夠**」（用他自己 `#14` 講過的話），選項與展開台詞兩處都改 | 選項 2＋展開 |
  | 5 | **同格重複 `SetPortrait`**：`SetPortrait(MC2,pic=4);SetPortrait(MC2,pic=1);` 前者立刻被蓋掉，等於沒有——而那格正是他「愣了半息」。刪掉 `pic=4`，留 `pic=1`＋`Proud`（愣已寫在 `[em7]` 裡） | `#19` 一帶 |
  | 6 | **謀略檢定難度 `5` → `10`**：原本武藝 `14`／謀略 `5`，一條幾乎必敗、一條幾乎必過，「兩種做法」等於只剩一種。10 仍比 14 好過，保住「不俠的那手比較管用」的設計 | `3-2` 擲骰格 |
  | 7 | **`.md` 回讀補上 JSON 已改的兩句**：`#31`「跟剛才看子羽那個角落**時一樣**」、`7-C`「**是找錯了。他當時是這麼說的。**」 | `#31`／`#4080` |
  | 8 | **檔頭過期註記**：「⚠ 支線7 目前凍結」→ 改為已解凍（同檔 `§6` 早就寫著 2026-08-25 解凍定稿，前後自打嘴巴）；`§4-1`「指令寫法待與程式確認」→ 已確認（照 `BreakAlliance` 體例）；`Script:` 加反引號的寫法改回全案體例 `**Script ...;**` | 僅 `.md` |

  **⏳ 還有一條沒改，等作者裁：`3-3`（我不是他）那條根本沒打架，可是 `§6` 子羽照樣說「第三百四十九次，算你的」。** 要嘛 `§6` 依 `§3` 分三條（多節點），要嘛改子羽那句讓三條都成立，要嘛 `3-3` 不接 `§6`。**這是設計決定，AI 不自行處理。**

- [ ] **180 年新增節日事件（2026-09-03）。** 掛哪個月、主線還是探險、寫什麼都還沒定。repo 目前沒有稿、沒有 JSON。不要自己發明劇情。

## 卡在作者

agent 不得自行決定。

- [x] **已裁示維持 · 本曲不做** 郭嘉線＝DLC（2026-09-03 作者確認）。`劇情/郭嘉/` 稿留著，**這次不要建 Unity、不要填任務 ID、不要裁、不要搬〈逝者無聲〉。** 日後 DLC 再開。`CF46` 留給那時候。
- [x] **已裁示維持** 戰敗即 `ShowEnding(Ending_1)`（Game Over）。作者 2026-08-28 裁示**刻意這樣設計，全案不要改**——`宗緯家`、`小溪村`、`小溪村後山`、`水濂洞`、`破廟`、`雲中`、`黑鐵嶺礦坑`、`179年 支線事件`、`趙王洞/山賊窩` 都是同一個寫法。`趙王洞_白虎道妖_改動追蹤.md`〈複-1〉的改案不採納。記在本檔以免下一輪又去「修」。
- [x] **已裁示維持** [panel] 剩 23 格。三個多人同場檔案。作者已裁示維持現狀、不要改。記在本檔以免下一輪又去「修」。
- [x] **已裁示維持 · 特例不寫進指南（2026-09-03）** `主線事件/180年/9月.json` §舌戰三家用到 `[panel=4]`（甄筠）與 `[panel=5]`（呂信），與 `文本創作指南`〈多名 NPC 同時對話〉「說話者只用 1、2、3」牴觸。**作者裁示：這一場六人同框是特例，指南不改、也不要去「修」JSON。⚠ 別的場次仍照指南只用 1、2、3，不要拿這一場當前例。** 記在本檔以免下一輪又被當成錯誤。
- [ ] **第二章 · `C2M1` entry 3。** 雙投版收尾 `9月.json` `#325` 寫它當「三家危險平衡成立」（**不是舊稿寫的 entry 4**，2026-09-03 查證）。要在 Unity 確認沒被佔，沒定就不要自創。
- [x] **已解決（2026-09-03 查證）** 第二章 · 甄家 entry 2 已掛：`大地圖/水濂洞.json` `#379`／`#380`／`#4338`／`#4341` → `success`、`#4344` → `failure`（同格一併收 `CF40`）。結算分流讀得到甄家那一路。
- [x] **已解決（作者 2026-09-02 改制＋2026-09-03 確認 Unity 已建）** 第二章 · 舌戰三家的 14 個變數 → 單一 `Variable["BreakAlliance"]`（初值 `0`）；換曲點 → `BGM_32`。
- [ ] **賈詡〈兩卷〉· 只剩 entry 的 `Description` 文字。** ✅ JSON 佔位全部填完（任務 `CF44`、3 顆 entry、蔡邕門檻 Lv2、`#81` 好感 `-20`、`#88` +50／賞錢 400）。**⏳ 三顆 entry 的 `Description`（Unity 任務表欄位，不在 JSON）待定** — 建議文字在 `劇情/賈詡/01 兩卷.md` 各處與附錄一。
- [ ] **赫連娜娜 · 你要填的空。** 虎腹戰鬥 ID；背景 `Desert`／`Yard`／`Cave`、音效 `Others_StoneDoor` 核 ID；勸阻好感門檻（提案第 3 級，`IsFavorabilityLevelPass("MC22", 3)`）；元祖場要不要事件圖。
- [ ] **赫連娜娜 · 你要裁的。** **感情線四事件提案**（`赫連娜娜.md`〈感情線〉：一百文／端藥／頭巾／下一處；任務與閘門作者自建，好感閘 1/2/3/4 級；R4＝伴侶＋天機盤信物＋後宮修羅場）；名號「玄妍」；「第四層＝她自己的決定」；放下線「天機盤不轉了」留不留；虎腹守門的東西；10 檔選 1 加不加好感。
- [ ] `BrokenWall_Right` 方向。`英豪府遴選/開場、子羽、呂信.json` #481（#484 收特效）。方向是作者選的 Right（不是 Left），待作者在遊戲裡複核畫面方向對不對。同 ID 也出現在 `大地圖/青霧林/不知名的野外.json`。對照 `給AI看的指南/畫面指令轉換規則.md`。
- [x] **已解決（作者 2026-09-02 已刪）** `Json/主線事件/180年/6月.json`。空檔已不在 repo；索引盤點那句「空陣列無可回讀」過期，重跑盤點時再改，不要自己填劇情。
