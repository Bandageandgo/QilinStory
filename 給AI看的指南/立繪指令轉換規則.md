# 立繪指令轉換規則指南

本文件旨在提供一套清晰的規則，用於將《劇情/舊創作稿/初出茅廬之一.md》等劇本文件中針對特定角色的**立繪描述**和**情緒表達**，轉換為遊戲對話系統所需的`SetPortrait`（換立繪）和`EnableCharacterExpression`/`DisableCharacterExpression`（啟用/停用表情特效）指令。這份指南將作為後續將Markdown文件內容轉換為JSON格式數據時的依據。

## 1. 目標指令格式

### 1.1 換立繪 (`SetPortrait`)

根據《對話系統指令.csv》，指令格式如下：

`SetPortrait(角色ID,pic=圖片名稱);`

| **角色ID** | 代表遊戲引擎中角色的唯一識別碼。 |
| **圖片名稱 (pic)** | 對應角色立繪圖片的數字ID。 |

### 1.2 表情特效 (`EnableCharacterExpression` / `DisableCharacterExpression`)

根據目前專案實作，指令格式如下：

-   **啟用:** `EnableCharacterExpression(位置,角色版本ID,表情名稱);`
-   **停用:** `DisableCharacterExpression(位置);`

-   **位置**: 通常指角色在畫面上的位置，對於當前說話者，慣用 `1`，如果是作用在主角的則是固定為`0`。
-   **角色版本ID**: 用於區分角色不同時期或服裝的ID，例如 `MC1-1`、`MC8`。
-   **表情名稱**: 表情英文名稱，例如 `Anger`、`Surprise`、`Proud`。

### 1.3 指令組合使用範例


**重要規則**：
0.  **立繪張數逐角色不同**（以作者提供的 `pic=` 比對表為準；`角色立繪/` 資料夾只有四個角色，其餘角色的圖不在 repo 裡）：
    -   **每個角色的立繪張數與有效 `pic` 都不一樣，寫之前先查第 4 節該角色的表**（2026-08-27 作者提供實際比對表後全面改寫，舊版「多立繪角色一律套十五格總表」是錯的）：

        | 角色 | 角色ID | 有效 `pic` | 查哪一節 |
        | :--- | :--- | :--- | :--- |
        | 燕不凡 | `MC1` | `1`–`19`（全滿） | §3、4.1、**4.1.1** |
        | 赫連娜娜 | `MC22` | `1`–`5`、`7`–`16`（**缺 `6`**；12 張圖攤在 15 格） | **4.4** |
        | 蕭靈犀 | `MC8` | `1`–`6`、`9`–`14`（**缺 `7`／`8`，也沒有 `15`**） | **4.2.1** |
        | 菈沙 | `MC19` | `1`–`11` | 4.3 附表 |
        | 五立繪角色 | `MC2`–`MC10`、`MC13`、`MC15`、`MC24`、`role105` | `1`–`5` | 4.3 |
        | 卞喜 | `role125` | `1`–`7` | **4.5** |
        | 河童三兄弟 | `role120`／`role121`／`role122` | `1`–`4` | **4.5** |
        | 李大山 `role128`、廖淳 `role127` | | 只有 `1`、`2` | **4.5**、§2 備註 |
        | 蔡琰 | `MC23` | **張數未確認**，現有用量 `1`–`3`，勿寫 `4` 以上 | 4.3 附注 |
        | **雍仔** | `MC20` | **只有 `1`、`2`** | 4.3 附表 |
    -   **五立繪角色**：`MC2`～`MC10`、`MC13`、`MC15`、`MC24`、`role105`（含**甄筠 `MC9`**、張寧 `MC6`、褚人飛 `MC10`、張角 `MC13`、**曹操 `MC15`**、**蔡邕 `MC24`**、**茶博士 `role105`** 等）目前**都只有 5 張立繪**：1.一般 2.生氣 3.閉眼無奈 4.無奈傻眼 5.大傻眼（見 4.3）。**切勿**對這些角色使用 15 格總表（例如 `pic=10` 哭泣），對應圖檔並不存在。
    -   **所有角色都可完整使用**通用的 13 種表情特效疊加圖示（`EnableCharacterExpression`，見 5.1.1），因為那是疊加貼圖，不受立繪張數限制——**不要因為某角色只有 5 張（或雍仔只有 2 張）立繪，就誤以為他不能切表情特效**。立繪張數只管 `SetPortrait` 的 `pic` 範圍。
1.  燕不凡 (MC1)、蕭靈犀 (MC8) 每個對話行都應包含 `SetPortrait` 指令。**甄筠 (MC9) 身為劇情重要角色，也建議逐行切換立繪／表情**（僅是 pic 值限制在 1～5，而非因為她不重要就不切）。
2.  如果該對話情境需要額外的表情特效，則在 `SetPortrait` 後緊跟 `EnableCharacterExpression`。
3.  表情特效**必須**在該句對話文字結束後，下一個對話立即使用 `DisableCharacterExpression(1);` 關閉，以確保特效只在當前行生效。
4.  由於每行對話都會重新設定立繪 (`SetPortrait`)，不需要手動將立繪恢復到「預設」狀態。
5.  **其餘五立繪角色**（呂信 `MC2`、子羽 `MC3`、賈詡 `MC4`、徐榮 `MC5`、張寧 `MC6`、郭嘉 `MC7`、褚人飛 `MC10`、張角 `MC13` 等次要／客串角色）：因戲份較少，**並非強制規則 1 的對象**，舊有對話多半沒有切換立繪。但只要情境需要（例如生氣、傻眼、無奈等明顯情緒轉折），仍**建議**依 4.3 對照表加上 `SetPortrait`，不必等到「每一行都切換」；平淡陳述可略過不寫。
6.  **多角色同場景時的位置(位置參數)紀律**：只要對話行使用了 `[panel=N]`，該角色當次的 `EnableCharacterExpression` 第一參數（位置）就必須填 `N`；等到該角色的表情該結束時（通常是下一個對話節點開頭），**務必**呼叫對應的 `DisableCharacterExpression(N);` 把同一個位置關閉，否則特效會殘留在畫面上不會自動消失。同一場景若有多名角色（例如主角 `panel=0`、配角 `panel=2`）同時掛著表情，每個位置都要各自成對地 Enable／Disable，不能只關主角、漏了配角的位置。

## 2. 角色ID定義

立繪切換（`SetPortrait`）使用的角色 ID 總表如下（以引擎／立繪資源為準）：

> **⛔ 只有本表登記有案的角色有立繪（已定，2026-08-27 作者確認）。** 沒登記的代號——`role127`、`role42`、`NPC33`、`NPC10`、`NPC_Thug`、`Monster`、`MBoss13` 這一類——**不是立繪角色，不會有立繪切換**，因此**不可以**替他們寫 `SetPortrait` 或 `EnableCharacterExpression`：沒有立繪就沒有東西可換、也沒有臉可以疊圖示，那些指令是空轉。
> **⚠ 有立繪不等於 `MC` 開頭**：茶博士的代號就是 `role105`（作者確認**真的有五張**），寫成 `MC105`、`NPC36` 都是錯的。**代號一律用該角色的 `actorID`**，不要自己造一個。
> 這種角色說話時，該格 `Sequence` 就**只寫別的事**（`DisableCharacterExpression(N);` 關掉前一位角色的特效、`ModifyData`、音效…），沒有別的事就整欄留 `""`。
> **2026-08-27 全案清查：`Json/` 原有 182 條掛在非立繪角色身上的 `SetPortrait`／`EnableCharacterExpression`（30 個舊代號、26 個 JSON），已全數移除。**

| 角色 | 角色ID |
| :--- | :--- |
| 主角（燕不凡） | `MC1` |
| 呂信 | `MC2` |
| 子羽 | `MC3` |
| 賈詡 | `MC4` |
| 徐榮 | `MC5` |
| 張寧 | `MC6` |
| 郭嘉 | `MC7` |
| 蕭靈犀 | `MC8` |
| 甄筠 | `MC9` |
| 褚人飛 | `MC10` |
| 黑狼王 | `MC11` |
| 董卓 | `MC12` |
| 張角 | `MC13` |
| 絲蒂娜 | `MC14` |
| 曹操 | `MC15` |
| 張遼 | `MC16` |
| 高順 | `MC17` |
| 典韋 | `MC18` |
| 菈沙 | `MC19` |
| 雍仔 | `MC20` |
| 饕餮 | `MC21` |
| 赫連娜娜 | `MC22` |
| 蔡琰 | `MC23` |
| 蔡邕 | `MC24` |
| 茶博士 | `role105` |
| 卞喜 | `role125` |
| 李大山 | `role128` |
| 河童大哥 | `role120` |
| 河童二哥 | `role122` |
| 河童三弟 | `role121` |
| 廖淳 | `role127` |

備註：
-   **張角 (`MC13`)**：江湖行醫時亦作「張仲景」；舊稿可能寫 `NPC3` / `role113`，新劇本與立繪請統一 `MC13`。
-   **菈沙 (`MC19`)**：舊稿常寫「阿SA／阿莎」，立繪 ID 不變，仍為 `MC19`。
-   **蔡琰 (`MC23`)**：字文姬；設定檔／劇本可寫蔡文姬，`actorID` 用 `MC23`。
-   **`role` 系立繪角色（2026-08-27 作者確認）**：茶博士 `role105`、卞喜 `role125`、李大山 `role128`、河童三兄弟 `role120`／`role122`／`role121`、廖淳 `role127` **都有立繪切換**，代號就是他們的 `actorID`，**不要**改寫成 `MC` 開頭或自己造 `MC105`、`NPC36` 這種代號。
    -   **✅ 張數已於 2026-08-27 由作者補齊**：`role125` 卞喜 **7 張**（`1`–`7`，那 2 處 `pic=7` **是對的，不是超範圍**）、`role105` 茶博士 **5 張**、`role120`／`role121`／`role122` 河童三兄弟 **各 4 張**、`role128` 李大山 **2 張**、`role127` 廖淳 **2 張**。逐項與現有用量見 **4.5**。
    -   **廖淳 `role127`：只有兩張（已定，2026-08-27 作者確認）**——`1`＝**戴著眼罩**（常態），`2`＝**拿掉眼罩**。有效值只有 `1`、`2`，寫 `3` 以上一律是錯的（同雍仔 `MC20` 的處境）。
        **⚠ `2` 不是情緒、是身分**：眼罩是他裝凶的偽裝（見 `@角色設定/廖化.md`），只有在他當著自己人摘下眼罩之後才用 `2`——目前全案只有 `大地圖/天下第一武道大會.json` `#600`／`#750`／`#751` 那一段（`#599` 旁白寫眼罩歪了、`#750` 自己說破「這眼罩是裝給外人看的」）。破廟、趙王洞時期他還戴著，一律 `1`。
        2026-08-27 已校正：`趙王洞/山賊窩.json` `#50`／`#104`／`#4160` 的 `pic=8`（超範圍）→ `1`；同檔 `#64`／`#97` 的 `pic=2`（趙王洞時期他還沒摘眼罩，是動怒不是揭示）→ `1`。
-   **茶博士 (`role105`)**：**五張**，`pic` 值套 4.3 的 1～5 編號。舊稿寫成 `MC105`（`SetPortrait`）或 `NPC36`（`EnableCharacterExpression`）都是錯的，2026-08-27 已全案改為 `role105`（`SetPortrait` 11 處、`EnableCharacterExpression` 4 處）。

### 2.1 沒有立繪的角色：**不要寫 `SetPortrait`**

**⚠ 只有上表列到的 `actorID` 才有立繪。** 其餘路人、雜魚、單場客串（山賊頭子、黑血屍、村婦之類）**沒有畫立繪**，對他們下 `SetPortrait` 一定叫不到圖。（⚠ **茶博士 `role105` 是有立繪的**，別看他 ID 長得像路人就把他歸到這一節——他在上表裡，五張，用 4.3 的 1～5 編號。）

> **作者裁定（2026-08-27，沿用 2026-08-26 `NPC_DaughterInLaw` 那次的同一裁定）：「這些角色都沒有立繪切換。」**
> 遇到這種指令**不是想辦法換算成有效的 `pic` 值，而是整行移除**——把 `SetPortrait(...)` 從 `Sequence` 裡刪掉即可（該節點的 `actorID` 不動，`text` 不動）。若整條 `Sequence` 只有這一個指令，就把 `Sequence` 清空。
> 已依此處理過的：`Json/大地圖/黑鐵嶺礦坑.json` `#64`／`#66`／`#73` 的 `NPC_DaughterInLaw`（2026-08-26）、`劇情/赫連娜娜.md` 的 `NPC_Boss` 23 處／`NPC_Thug` 7 處／`NPC11` 1 處（2026-08-27）。

**怎麼判斷**：**以第 2 節總表為唯一依據**——`actorID` 不在那張表上（`NPC_Boss`、`NPC_Thug`、`NPC11`、`NPC_DaughterInLaw` 這種）就是沒有立繪。**不要用 ID 長相判斷**：`role105`（茶博士）在表上、有立繪，而 `MC` 開頭的 `MC11`／`MC12`／`MC14`／`MC16`／`MC17`／`MC18`／`MC21` 雖然在表上卻還沒有實際立繪資源，寫之前先確認。**不確定就問作者，不要自己給格號。**

**重要概念區分:**
-   **角色ID (`actorID`)**: 這是用於 `SetPortrait` 指令的核心角色識別碼，例如 `MC1`。它代表角色本身。
-   **立繪版本ID**: 這是用於區分角色不同時期或服裝的ID，例如 `MC1-1`。它是 `EnableCharacterExpression` 的第二個參數。
    -   **主角有兩個版本（已定，2026-08-27 作者指出）**：`MC1-1` ＝**少年**燕不凡、`MC1-2` ＝**青年**燕不凡。現行劇情（179–180 年）全是少年期，第二參數一律 `MC1-1`；寫成 `MC1`（漏了版本號）是錯的，引擎抓不到版本。`Json/` 原有 80 處 `MC1` 已於 2026-08-27 全數改為 `MC1-1`。
-   **表情名稱**: `EnableCharacterExpression` 的第三個參數，例如 `Anger`、`Surprise`。

## 3. 立繪 pic 參數數字ID總表

`SetPortrait`指令中的`pic`參數現已全面採用數字ID，取代舊有的文字ID。這份總表定義了每個數字ID的**名目**表情。

> **⚠ 2026-08-27 更正：本表只是「名目編號」，並不是每個角色都照這樣對圖。**
> 作者當日提供 `MC1-1`／`MC8`／`MC22` 的實際 `pic=` 比對表後確認：**`MC8` 有三格沒圖（7／8／12）、也沒有 15**；**`MC22` 有一格沒圖（6），且 4／11／12／15 與名目不符**；**`MC1` 另有 16–19 四格**。
> **動筆前一律先查第 4 節的角色專屬對照表**（4.1 `MC1`、4.2 `MC8`、4.3 五立繪角色、4.4 `MC22`）；本表只當作「還沒有專屬比對表的角色」的預設參考。

| 數字ID | 表情名稱 | 常見情境/情緒描述 |
| :--- | :--- | :--- |
| 1 | 一般 | 較平靜的對話、陳述事實 (通常對應 `serious` 或默認立繪) |
| 2 | 生氣 | 憤怒、不滿、吐槽、跺腳 |
| 3 | 難過 | 悲傷、沮喪、失落、同情 |
| 4 | 驚訝 | 震驚、驚愕、意想不到 |
| 5 | 笑 | 開心、得意、計謀得逞、期待、眼睛一亮 |
| 6 | 白眼 | 無奈、不屑、對他人的言行感到無語 |
| 7 | 害羞 | 臉紅、被誇獎、尷尬 |
| 8 | 嚴肅 | 認真、下定決心、講述重要事情、行禮 |
| 9 | 蛤 | 傻眼、狀況外、聽不懂對方的話 |
| 10 | 哭 | 哭泣、流淚 |
| 11 | 受傷 | 身體或心裡受創、痛苦、難受 |
| 12 | 思考 | 若有所思、分析、觀察、回憶、苦惱 |
| 13 | 緊張 | 焦慮、不安、心虛、被戳穿 |
| 14 | 疑惑 | 不解、提問、好奇、懷疑 |
| 15 | 疲勞 | 疲憊、有氣無力、想睡覺 |
| 16 | 受傷 | `MC1`（`Hurt`）重傷、嘔血、挨打後的狼狽；**`MC22` 亦有此格**（`Hurt`，2026-08-27 新增） |
| 17 | 狡黠 | **`MC1` 專屬**（`Cunning`）。賊笑、陪笑套話、打小算盤、想撈好處、討好前輩 |
| 18 | 算計 | **`MC1` 專屬**（`Scheming`）。城府、盤算得失、沉住氣談條件、下定決心要贏 |
| 19 | 絕望 | **`MC1` 專屬**（`Despair`）。驚呼慘叫、嚇壞、崩潰、欲哭無淚 |

**`17`／`18`／`19` 只有主角有**（2026-08-27 作者補齊檔名，見 4.1.1）；**`16` 除主角外，赫連娜娜 `MC22` 也有一格**（同日新增，`Hurt`，見 4.4）。其餘角色寫 `pic=16`～`19` 一律是錯的，圖檔不存在。

## 4. 立繪描述與`pic`參數對照表 (`SetPortrait`)

以下表格詳細列出了在《劇情/舊創作稿/初出茅廬之一.md》中使用的自然語言立繪描述，及其對應的`角色ID`和`SetPortrait`指令中`pic`參數應使用的數字ID。

### 4.1 少年燕不凡 (MC1)

| Markdown中的描述立繪文字 | 對應數字ID (`pic`值) | 產生的指令範例 |
| :--- | :--- | :--- |
| `[嘗試英雄式站姿，眺望遠方立繪]` | `5` | `SetPortrait(MC1,pic=5);` |
| `[嘗試英雄式立繪]` | `5` | `SetPortrait(MC1,pic=5);` |
| `[尷尬/臉紅]` | `7` | `SetPortrait(MC1,pic=7);` |
| `[尷尬立繪]` | `7` | `SetPortrait(MC1,pic=7);` |
| `[趾高氣昂]` | `5` | `SetPortrait(MC1,pic=5);` |
| `[拱手行禮立繪]` | `8` | `SetPortrait(MC1,pic=8);` |
| `[通常]` (用於洞悉選項) | `8` | `SetPortrait(MC1,pic=8);` |
| `[平靜立繪]` | `8` | `SetPortrait(MC1,pic=8);` |
| `[嚴肅立繪]` | `8` | `SetPortrait(MC1,pic=8);` |
| `[手持黃金魚立繪]` | `8` | `SetPortrait(MC1,pic=8);` |
| `[準備收回魚]` | `8` | `SetPortrait(MC1,pic=8);` |
| `[收回魚的立繪]` | `8` | `SetPortrait(MC1,pic=8);` |
| `[接過，拱手立繪]` | `8` | `SetPortrait(MC1,pic=8);` |
| `[抱拳請教]` | `8` | `SetPortrait(MC1,pic=8);` |
| `[手持殘頁]` | `8` | `SetPortrait(MC1,pic=8);` |
| `[略顯平靜]` | `8` | `SetPortrait(MC1,pic=8);` |
| `[無奈/被吐槽立繪]` | `11` | `SetPortrait(MC1,pic=11);` |
| `[無奈跟上立繪]` | `12` | `SetPortrait(MC1,pic=12);` |
| `[苦惱皺眉]` | `12` | `SetPortrait(MC1,pic=12);` |
| `[皺眉]` (村長告知規矩時) | `12` | `SetPortrait(MC1,pic=12);` |
| `[低聲碎碎念立繪]` | `15` | `SetPortrait(MC1,pic=15);` |
| `[表情略微緩和立繪]` | `15` | `SetPortrait(MC1,pic=15);` |
| `[嚴肅，若有所思立繪]` | `12` | `SetPortrait(MC1,pic=12);` |
| `[觀察棋局，若有所思]` | `12` | `SetPortrait(MC1,pic=12);` |
| `[觀察黃金魚，若有所思]` | `12` | `SetPortrait(MC1,pic=12);` |
| `[收回殘頁]` (保留棋譜) | `12` | `SetPortrait(MC1,pic=12);` |
| `[高深莫測]` | `12` | `SetPortrait(MC1,pic=12);` |
| `[挑眉]` | `5` | `SetPortrait(MC1,pic=5);` |
| `[頷首立繪，努力維持高手風範]` | `5` | `SetPortrait(MC1,pic=5);` |
| `[瀟灑遞出]` (贈送棋譜) | `5` | `SetPortrait(MC1,pic=5);` |
| `[努力維持大俠風度]` | `5` | `SetPortrait(MC1,pic=5);` |
| `[強裝鎮定但得意]` | `5` | `SetPortrait(MC1,pic=5);` |
| `[憐憫立繪]` (對小花) | `8` | `SetPortrait(MC1,pic=8);` |
| `[試探性]` (口才試探棋譜價格) | `5` | `SetPortrait(MC1,pic=5);` |
| *無描述，雍仔逃跑時燕不凡的反應* | `8` | `SetPortrait(MC1,pic=8);` |
| *燕不凡結局A反應* | `11` | `SetPortrait(MC1,pic=11);` |

*注意：為確保完整性，未使用的立繪 `surprise`(4), `anger`(2), `pain2`(11) 也已標註其對應數字ID。*

#### 4.1.1 MC1 專屬的 16–19（2026-08-27 作者提供檔名）

主角是全案立繪最多的角色（`角色立繪/少年燕不凡/` 共 19 張），1–15 沿用第 3 節名目總表，另有這四格是**主角獨有**：

| `pic` | 圖檔（`UI_Portrait_MC1-1_xxx.png`） | 表情 | 什麼時候用 |
| :--- | :--- | :--- | :--- |
| `16` | `Hurt` | 受傷 | 重傷、嘔血、挨打後的狼狽（例：`大地圖/黑鐵嶺礦坑.json` `#62`） |
| `17` | `Cunning` | 狡黠 | 賊笑、陪笑套話、想撈好處、討好前輩（例：`小溪村/華陀、老學究 .json` `#155` 開口討盤纏） |
| `18` | `Scheming` | 算計 | 城府、盤算得失、沉住氣談條件（例：`大地圖/天下第一武道大會.json` `#47`「這一場我得贏」） |
| `19` | `Despair` | 絕望 | 驚呼慘叫、嚇壞、崩潰（例：`大地圖/黑鐵嶺礦坑.json` `#3940`「呃啊啊啊——！」） |

`Json/` 現有用量：`16` 30 處、`17` 44 處、`18` 30 處、`19` 44 處，語氣與上表相符，**維持原樣不必改**。

### 4.2 蕭靈犀 (MC8)

#### 4.2.1 MC8 實際 `pic=` 比對表（2026-08-27 作者提供，**以此為準**）

> **⚠ MC8 不是十五格齊全。`7`／`8` 沒有圖，也沒有 `15`。** 舊版本檔把 MC8 當成「套用第 3 節十五格總表」是錯的，寫這兩格會叫不到立繪。（`12` 原本也是空的，**2026-08-27 作者裁定填入 `Hurt`**。）
> 另外 `6` 與 `9` 的意思與名目總表**對調得不完全一樣**：`6` 是「嫌棄（dislike）」，翻白眼要用 `9`。

| `pic` | 圖檔（Unity 資源名） | 表情 | 什麼時候用 |
| :--- | :--- | :--- | :--- |
| `1` | `UI_Portrait_MC8`（無後綴） | 一般 | 平常說話、陳述、預設立繪 |
| `2` | `UI_Portrait_MC8_Anger` | 生氣 | 氣鼓鼓、跺腳、叉腰吐槽、癟嘴 |
| `3` | `UI_Portrait_MC8_Sad` | 難過 | 傷心、失望、擔憂、同情、沒精神 |
| `4` | `UI_Portrait_MC8_Surprise` | 驚訝 | 震驚、驚喜、垮臉、捂嘴 |
| `5` | `UI_Portrait_MC8_Happy` | 笑 | 開心、雀躍、期待、眼睛一亮、星星眼 |
| `6` | `UI_Portrait_MC8_dislike` | 嫌棄 | 不屑、嫌棄、拿對方沒辦法的無奈 |
| `7` | **無圖（NONE）** | — | **⚠ 不可使用**（名目總表的「害羞」，MC8 沒畫） |
| `8` | **無圖（NONE）** | — | **⚠ 不可使用**（名目總表的「嚴肅」，MC8 沒畫）；要她認真講事情就用 `1` |
| `9` | `UI_Portrait_MC8_RollingEyes` | 翻白眼 | 無語、翻白眼、懶得理你 |
| `10` | `UI_Portrait_MC8_Cry` | 哭 | 哭泣、流淚 |
| `11` | `UI_Portrait_MC8_Pain` | 痛苦 | 心痛、被戳到痛處、難受（**2026-08-27 作者裁定：這格是 `Pain` 不是 `Hurt`**） |
| `12` | `UI_Portrait_MC8_Hurt` | 受傷 | 委屈、挨了一記、身上或心裡帶傷（**2026-08-27 作者從 `11` 挪到這格**）。名目總表這格是「思考」，**MC8 沒有思考臉**，要「在想事情」請用 `14` |
| `13` | `UI_Portrait_MC8_Nervous` | 緊張 | 焦慮、心虛、被戳穿 |
| `14` | `UI_Portrait_MC8_Confused` | 疑惑 | 不解、好奇、歪頭發問、想不通 |
| `15` | **不存在** | — | **⚠ MC8 只到 14**，沒有「疲勞」格 |

有效值只有 `1`～`6`、`9`～`14` 共 12 格（沒圖的只剩 `7`、`8`，且沒有 `15`）。

> **✅ 2026-08-27 結案：`Pain` 與 `Hurt` 兩張都配到格號了**——`11`＝`Pain`（痛苦），`Hurt` 從 `11` 挪到原本空著的 `12`。⚠ **`Json/` 現有 3 處 `MC8,pic=11`**（`劇情/蕭靈犀.md` 那幾句心痛）**維持原樣**，它們本來就是「心痛」，換成 `Pain` 臉正合適。
>
> 舊命名（`UI_Portrait_MC8_angry`、`UI_dialogue_MC8_RollingEyes-6` 這種帶數字後綴的）已於 2026-08-27 一併換掉，**舊檔名不再有效**，`RollingEyes-6` 的 `-6` 更不是 `pic` 值（它在 `9`）。

#### 4.2.2 描述用語 → `pic` 對照

| Markdown中的描述立繪文字 | 對應數字ID (`pic`值) | 產生的指令範例 |
| :--- | :--- | :--- |
| `[有氣無力立繪]` | `3` | `SetPortrait(MC8,pic=3);`（⚠ 原寫 `15`，MC8 沒有這格） |
| `[擔憂，同情立繪]` (對小花) | `3` | `SetPortrait(MC8,pic=3);` |
| `[擔憂立繪]` (小花害怕) | `3` | `SetPortrait(MC8,pic=3);` |
| `[眼睛一亮立繪]` | `5` | `SetPortrait(MC8,pic=5);` |
| `[期待立繪]` | `5` | `SetPortrait(MC8,pic=5);` |
| `[若有所思立繪]` | `14` | `SetPortrait(MC8,pic=14);`（⚠ 原寫 `12`，MC8 沒有這格） |
| `[眼睛更亮立繪]` | `5` | `SetPortrait(MC8,pic=5);` |
| `[小聲計算立繪]` | `14` | `SetPortrait(MC8,pic=14);`（⚠ 原寫 `12`，MC8 沒有這格） |
| `[握拳]` (為了綠豆糕) | `5` | `SetPortrait(MC8,pic=5);` |
| `[好奇張望]` (棋局) | `14` | `SetPortrait(MC8,pic=14);` |
| `[上前一步，禮貌但直接]` (對村長) | `14` | `SetPortrait(MC8,pic=14);` |
| `[震驚/垮臉立繪]` | `4` | `SetPortrait(MC8,pic=4);` |
| `[驚喜立繪]` | `4` | `SetPortrait(MC8,pic=4);` |
| `[震驚又心疼]` (送出棋譜) | `4` | `SetPortrait(MC8,pic=4);` |
| `[驚訝捂嘴]` (棋譜賣400文) | `4` | `SetPortrait(MC8,pic=4);` |
| `[癟嘴立繪]` | `2` | `SetPortrait(MC8,pic=2);` |
| `[扯衣袖，無奈立繪]` | `6` | `SetPortrait(MC8,pic=6);` |
| `[叉腰吐槽立繪]` | `2` | `SetPortrait(MC8,pic=2);` |
| `[失望立繪]` | `3` | `SetPortrait(MC8,pic=3);` |
| `[不解]` (不賣棋譜) | `14` | `SetPortrait(MC8,pic=14);` |
| `[點頭立繪]` | `5` | `SetPortrait(MC8,pic=5);` |
| `[星星眼立繪]` | `5` | `SetPortrait(MC8,pic=5);` |
| `[開心雀躍立繪]` | `5` | `SetPortrait(MC8,pic=5);` |
| `[蹦跳立繪]` | `5` | `SetPortrait(MC8,pic=5);` |
| `[噗嗤笑出聲立繪]` | `5` | `SetPortrait(MC8,pic=5);` |
| `[氣鼓鼓跺腳立繪]` | `2` | `SetPortrait(MC8,pic=2);` |
| `[氣鼓鼓立繪]` | `2` | `SetPortrait(MC8,pic=2);` |
| *蕭靈犀結局A反應* | `3` | `SetPortrait(MC8,pic=3);` |
| *蕭靈犀結局B反應* | `5` | `SetPortrait(MC8,pic=5);` |
| *蕭靈犀結局C反應* | `5` | `SetPortrait(MC8,pic=5);` |
| *(金錢接近目標時)* | `5` | `SetPortrait(MC8,pic=5);` |
| *(時間不多時)* | `13` | `SetPortrait(MC8,pic=13);`（⚠ 原寫 `15`，MC8 沒有這格） |
| *(若你選傻笑，阿婆互動)* | `2` | `SetPortrait(MC8,pic=2);` |
| *(驚訝，阿婆給錢)* | `4` | `SetPortrait(MC8,pic=4);` |
| *(耶！又賺到錢了！阿婆拿到石頭)* | `5` | `SetPortrait(MC8,pic=5);` |
| *(表哥你看，那裡有好多羊！)* | `5` | `SetPortrait(MC8,pic=5);` |
| *(表哥你好聰明！算對羊的數量)* | `5` | `SetPortrait(MC8,pic=5);` |
| *(表哥，這裡有釣竿！)* | `5` | `SetPortrait(MC8,pic=5);` |
| *(哇！表哥你看！釣到黃金魚)* | `4` | `SetPortrait(MC8,pic=4);` |
| *(歪頭，問雍仔茅山代數)* | `14` | `SetPortrait(MC8,pic=14);` |
| `[傷心立繪]` | `3` | `SetPortrait(MC8,pic=3);` |
| `[翻白眼立繪]` | `9` | `SetPortrait(MC8,pic=9);`（⚠ 原寫 `6`；`6` 是嫌棄，翻白眼在 `9`） |
| `[困惑立繪]` | `14` | `SetPortrait(MC8,pic=14);` |
| `[緊張立繪]` | `13` | `SetPortrait(MC8,pic=13);` |
| `[哭泣立繪]` | `10` | `SetPortrait(MC8,pic=10);` |
| `[心痛立繪]` | `11` | `SetPortrait(MC8,pic=11);` |

### 4.3 五立繪角色（`MC2`～`MC10`、`MC13`、`MC15`、`MC24`、`role105`，含甄筠）

以下角色**目前只有 5 張立繪**，數量遠少於「3. 立繪 pic 參數數字ID總表」列出的 15 種通用表情。**請勿**直接套用該 15 值總表（例如寫出 `pic=10` 哭泣），因為對應圖檔並不存在；這 5 張立繪改用**專屬的 1～5 局部編號**，依序如下：

> **✅ 2026-08-27 作者再次確認：甄筠 `MC9`、徐榮 `MC5` 就是只有 5 張。** 當日已把 `Json/` 裡超出範圍的 16 處改回 1～5（`MC9` 15 處在 `探索事件/180年/3月.json`、`MC5` 1 處在 `觀心.json`），見 4.3.1 下方的〈已校正〉。

| 數字ID | 表情名稱 | 常見情境/情緒描述 |
| :--- | :--- | :--- |
| 1 | 一般 | 平靜對話、陳述事實、談生意（預設立繪） |
| 2 | 生氣 | 憤怒、不滿、吐槽、動怒 |
| 3 | 閉眼無奈 | 無語、嘆氣、不屑、拒絕、拿對方沒辦法 |
| 4 | 無奈傻眼 | 意外、挑眉、輕微的難以置信 |
| 5 | 大傻眼 | 強烈震驚、完全出乎意料、哭笑不得 |

**適用角色與 `actorID` 對照**（沿用第 2 節總表）：

| 角色 | 角色ID |
| :--- | :--- |
| 呂信 | `MC2` |
| 子羽 | `MC3` |
| 賈詡 | `MC4` |
| 徐榮 | `MC5` |
| 張寧 | `MC6` |
| 郭嘉 | `MC7` |
| **甄筠** | **`MC9`** |
| 褚人飛 | `MC10` |
| 張角 | `MC13` |
| **曹操** | **`MC15`**（2026-08-27 作者確認，5 張） |
| **蔡邕** | **`MC24`**（2026-08-27 作者確認，5 張） |
| **茶博士** | **`role105`**（唯一不是 `MC` 開頭的，見 §2 備註） |

> ⚠ **蔡琰 `MC23` 的張數尚未確認**（作者 2026-08-27 表示先不處理）。`Json/` 現有 21 處都用 `1`–`3`，落在任何合理範圍內，**維持原樣**；在作者給出張數之前，**不要對 `MC23` 寫 `4` 以上的格號**。
| 曹操 | `MC15` |
| 蔡琰 | `MC23` |
| 蔡邕 | `MC24` |

> **⚠ 曹操 `MC15`、蔡琰 `MC23`、蔡邕 `MC24` 是 2026-08-27 補登記的（暫定）。** 三人 `Json/` 現行用量分別是 `3`–`4`、`1`–`3`、`1`–`4`，都落在本節 1～5 範圍內，**先按五立繪處理**；實際張數請作者確認後回填，若其中誰其實有十五格，本行要改。

**⚠ 不屬於本節的兩個角色**（2026-08-27 作者確認，各有各的張數，**不要當成五立繪角色**）：

| 角色 | 角色ID | 張數 | 有效 `pic` | 說明 |
| :--- | :--- | :--- | :--- | :--- |
| **雍仔** | `MC20` | **只有 2 張** | `1`、`2` | `1`＝正常、`2`＝傷痕累累。**這是全案立繪最少的角色**，除了打完架、挨揍、狼狽的場合用 `2`，其餘一律 `1`。寫 `3` 以上一律是錯的 |
| **菈沙** | `MC19` | **11 張** | `1`～`11` | 依第 3 節名目總表的前 11 格（`12`～`15` 沒有）。**檔名尚未回填**（`角色立繪/` 底下還沒有她的資料夾），`EnableCharacterExpression` 第三參數仍只能從 5.1.1 那 13 種挑 |

**指令範例**：`SetPortrait(MC2,pic=2);`（呂信・生氣）

**表情特效（`EnableCharacterExpression`）**：這些角色**同樣可以完整使用表情特效**，套用的是跟燕不凡（`MC1-1`）相同的通用 13 種特效圖示（見 5.1.1），因為這些是疊加在立繪上的**通用圖示貼圖**（怒氣符號、汗滴、驚嘆號、燈泡等），並非每個角色各畫一套，所以**不受「只有 5 張立繪」的限制**——立繪張數只影響 `SetPortrait` 的 `pic` 值範圍，不影響能不能掛表情特效：

`Anger`、`Anger_2`、`Anger_3`、`Nervous`、`Nervous_2`、`Pain`、`Proud`、`Shock`、`Surprise`、`Surprise_2`、`Meditate`、`Idea`、`Question`

**角色版本ID（第二參數）**：這些角色皆為單一版本，直接用其 `actorID` 本身即可，**不需要**像 `MC1-1` 那樣加 `-1` 後綴。例如：

`SetPortrait(MC2,pic=2);EnableCharacterExpression(位置,MC2,Anger);`（呂信・生氣＋怒氣特效）

#### 4.3.1 甄筠 (MC9) 範例

甄筠是本節五立繪角色中戲份較重的一位，**建議像 MC1／MC8 一樣逐行切換**，只是 `pic` 值限制在 1～5。位置對應其 `[panel=N]`，甄府對談時多為 `1`。

| 台詞語氣 | 對應數字ID (`pic`值) | 建議表情特效 |
| :--- | :--- | :--- |
| 談生意、平靜陳述、算計、好奇提問 | `1` 一般 | `Proud` / `Meditate` / `Idea` / `Question`（依語氣，可省略） |
| 生氣、不滿 | `2` 生氣 | `Anger` |
| 不屑、搖頭拒絕、嘆氣 | `3` 閉眼無奈 | `Meditate`（可省略） |
| 挑眉、意外（程度較輕） | `4` 無奈傻眼 | `Surprise` |
| 大感意外、完全沒料到、深受震撼 | `5` 大傻眼 | `Surprise` / `Shock` |

**指令範例**：`SetPortrait(MC9,pic=1);EnableCharacterExpression(1,MC9,Proud);`

**✅ 已校正（2026-08-27）**：`Json/探索事件/180年/3月.json` 的甄府對談原本有 15 處超出 1～5——`pic=6`（白眼）5 處 → `3` 閉眼無奈（那幾句都是「似笑非笑」「搖頭，不置可否」的不屑）、`pic=8`（嚴肅）7 處 → `1` 一般（談生意、講條件、陳述傳說）、`pic=12`（思考）3 處 → `1` 一般（估價、盤算，思考感交給 `Meditate` 特效）。同批另修 `觀心.json` `#362` 徐榮 `MC5,pic=8` → `1`、`古墓演出劇情.json` 與 `主線事件/179年/7月.json` 雍仔 `MC20` 6 處（`pic=11` 4 處 → `2` 傷痕累累，該段旁白就寫著「很快便傷痕累累」；`pic=12` 2 處 → `1`）。

**注意：**
-   此 1～5 編號**僅適用於五立繪角色**，與「3. 立繪 pic 參數數字ID總表」（`MC1`、`MC8` 適用的 1～15 編號）是兩套獨立的數字系統，切勿混用。
-   若日後新增其他角色也只做了 5 張同規格立繪（一般/生氣/閉眼無奈/無奈傻眼/大傻眼），可直接套用本節編號。
-   **位置參數務必對應 `panel` 編號**：例如該角色台詞是 `[panel=2]...`，`EnableCharacterExpression` 第一參數就要填 `2`；等到該表情該結束時（通常是下一個對話節點開頭），要記得補上 `DisableCharacterExpression(2);` 關閉同一位置，避免特效卡在畫面上不消失（完整規則見 5.2、5.3）。

### 4.4 赫連娜娜 (MC22)

**2026-08-27 作者提供實際 `pic=` 比對表，以下為準。**

> **⚠ 本節前兩版都寫錯，這是第三版。** 第一版寫「`6`／`9`／`12`／`15` 沒圖，勿用」，第二版（2026-08-26）改成「十五格全部都有圖」——**兩版都不對**。
> 實情是：娜娜只有 **11 張圖**攤在 15 格上，`6` 真的沒圖，其餘幾格是**重複套用同一張圖**，而且 `4`／`11`／`12`／`15` 的內容**與第 3 節名目總表不一樣**。

| `pic` | 圖檔（`UI_Portrait_MC22_xxx.png`） | 實際表情 | 什麼時候用 |
| :--- | :--- | :--- | :--- |
| `1` | 無後綴（預設立繪） | 一般 | 平常說話、講述事實 |
| `2` | `Anger` | 生氣 | 生氣、不滿 |
| `3` | `Sad` | 難過 | 難過、沮喪、失落 |
| `4` | `Nervous` | **緊張** | ⚠ **不是驚訝**。緊張、不安、心虛（與 `13` 同一張圖） |
| `5` | `Laugh` | 笑 | 開心、得意、期待、眼睛一亮 |
| `6` | **無圖（none）** | — | **⚠ 不可使用**（名目總表的「白眼」，娜娜沒畫） |
| `7` | `Shy` | 害羞 | 臉紅、被誇獎、尷尬 |
| `8` | `Serious` | 嚴肅 | 認真、焦急、講正事 |
| `9` | `Surprise` | **驚訝** | ⚠ **娜娜的「驚訝」在這格**，不在 `4`。震驚、傻眼、驚呼 |
| `10` | `Cry` | 哭 | 哭泣、流淚 |
| `11` | `Confused` | **疑惑** | ⚠ **不是受傷**（與 `12`／`14` 同一張圖） |
| `12` | `Confused` | 疑惑 | ⚠ 名目是「思考」，實際仍是疑惑臉（同 `11`／`14`） |
| `13` | `Nervous` | 緊張 | 焦慮、不安、被戳穿（同 `4`） |
| `14` | `Confused` | 疑惑 | 不解、提問、好奇（同 `11`／`12`） |
| `15` | `Sad` | 難過 | ⚠ 名目是「疲勞」，實際是難過臉（同 `3`） |
| `16` | `Hurt` | 受傷 | **2026-08-27 新增**（作者當日在 Unity 端一併新增這一格）。身上或心裡受創、痛苦 |

**新寫對白時的建議寫法**（避開重複格與無圖格）：驚訝寫 `9`、緊張寫 `13`、疑惑寫 `14`、難過寫 `3`、受傷寫 `16`；`4`／`11`／`12`／`15` 視為別名，不要新寫，`6` 一律不寫。

扣掉 `6`，有效值是 `1`～`5`、`7`～`16` 共 15 格，背後是 **12 張圖**。

> **✅ 2026-08-27 結案：`UI_Portrait_MC22_Hurt.png` 配到 `16`**（作者當日裁定並在 Unity 端新增該格）。娜娜是**除主角外唯一有 `16` 的角色**——`17`／`18`／`19` 仍然只有主角有。

**注意：**
-   **`EnableCharacterExpression` 第三參數不能照抄上表的圖檔名。** 立繪檔名（`Confused`、`Laugh`、`Sad`、`Cry`、`Serious`、`Shy`、`Hurt`…）與表情特效是兩套資源：特效是疊加圖示，**只能從 5.1.1 的 13 種**（`Anger`、`Anger_2`、`Anger_3`、`Nervous`、`Nervous_2`、`Pain`、`Proud`、`Shock`、`Surprise`、`Surprise_2`、`Meditate`、`Idea`、`Question`）挑。兩邊剛好同名的只有 `Anger`、`Nervous`、`Surprise`。
-   `@角色設定/赫連娜娜.md` 的〈立繪指令參照表〉已同步為本表，兩邊要一起改。

### 4.5 `role*` 配角的立繪張數（2026-08-27 作者確認）

這幾個 `actorID` 雖然是 `role` 開頭，**都有立繪**，張數各不相同——**不要因為 ID 長得像路人就套 2.1「沒有立繪」那條**：

| 角色 | 角色ID | 張數 | 有效 `pic` | `Json/` 現有用量 |
| :--- | :--- | :-: | :--- | :--- |
| **卞喜** | `role125` | **7** | `1`–`7` | 17 處，用到 `1`–`5`、`7` |
| **茶博士** | `role105` | **5** | `1`–`5`（套 4.3 的五立繪編號） | 11 處，用到 `2`–`5` |
| **河童大哥** | `role120` | **4** | `1`–`4` | 10 處，用到 `1`–`4` |
| **河童二哥** | `role122` | **4** | `1`–`4` | 4 處，用到 `1`／`3`／`4` |
| **河童三弟** | `role121` | **4** | `1`–`4` | 10 處，用到 `1`–`3` |
| **李大山** | `role128` | **2** | `1`、`2` | 4 處，用到 `1`／`2` |
| **廖淳** | `role127` | **2** | `1`、`2` | 見 §2 備註——`1` 戴眼罩、`2` 摘眼罩，**是身分不是情緒** |

**⚠ 除了 `role105`（確認套 4.3 的 1 一般／2 生氣／3 閉眼無奈／4 無奈傻眼／5 大傻眼）與 `role127`（1 戴眼罩／2 摘眼罩）之外，其餘五人「幾號是什麼表情」作者尚未給對照。** 現有用量全都落在各自範圍內，**一律維持原樣**；新寫時若要指定表情，**先問作者**，不要照第 3 節名目總表推。

## 5. 表情特效指令轉換規則 (`EnableCharacterExpression`)

本節定義如何根據劇本中的情緒線索，選用合適的`角色版本ID`與`表情名稱`並生成`EnableCharacterExpression`指令，用於強化角色的情緒狀態，需配合`SetPortrait`指令一起使用。**第二參數（角色版本ID）可由立繪推得，第三參數（表情名稱）不行——它只能從 5.1.1 的 13 種白名單挑，與立繪檔名無關。**

### 5.1 檔名與參數拆解

> **⚠ 2026-08-27 更正：不要從立繪檔名拆出第三參數。** 本節原本教人「把 `UI_Portrait_{角色ID}_{表情名稱}.png` 的表情段拿來當第三參數」，那會寫出 `Confused`、`Laugh`、`Sad`、`Cry`、`Serious`、`Shy`、`Hurt`、`Cunning` 這類**不存在的特效名**——立繪與表情特效是兩套資源，**第三參數只能從 5.1.1 的 13 種挑**。兩邊剛好同名的只有 `Anger`、`Nervous`、`Surprise`。

-   **第二參數（角色版本ID）**：主角用 `MC1-1`（**少年**燕不凡）——不是 `MC1`（漏版本號），也不是 `MC1-1_Cunning` 這種併寫；其餘角色直接用其 `actorID`（`MC8`、`MC22`、`MC9`…）。
    -   **`MC1-2` 是青年版主角**（已定，2026-08-27 作者指出）。現行劇情是少年期，除非該段明寫是青年燕不凡，否則一律 `MC1-1`。
-   **第三參數（表情名稱）**：一律查 5.1.1，與該角色的 `pic` 值、立繪檔名都無關。
-   **範例**:
    -   `EnableCharacterExpression(0,MC1-1,Anger);`
    -   `EnableCharacterExpression(2,MC22,Surprise);`

-   **參數格式（易錯提醒）**: 第二、三參數必須**分開**，以逗號寫成 `角色版本ID,表情名稱`。  
    -   正確：`EnableCharacterExpression(0,MC1-1,Anger);`  
    -   錯誤：`EnableCharacterExpression(0,MC1-1_Anger);`（把版本與表情併成一段底線字串，引擎無法辨識）

### 5.1.1 通用表情特效 ID 一覽（目前專案可用，適用所有角色）

以下 13 種`表情名稱`（第三參數）是**通用圖示貼圖**（怒氣符號、汗滴、驚嘆號、燈泡等疊加圖示），並非為特定角色臉部繪製，因此**所有角色皆可共用**——不論是多立繪角色（`MC1-1`、`MC8`、`MC22`）還是五立繪角色（`MC2`～`MC10`、`MC13`，含甄筠 `MC9`，見 4.3）。撰寫 `Sequence` 時請僅從此表選用，避免引用不存在的檔名：

| 表情名稱 | 圖示描述 |
| :--- | :--- |
| `Anger` | 紅色生氣 |
| `Anger_2` | 黃色圓圈生氣（道具靈氣） |
| `Anger_3` | 白色怒吼生氣（怪物吼叫） |
| `Nervous` | 滴汗緊張（一滴汗） |
| `Nervous_2` | 大量滴汗緊張（滿頭大汗） |
| `Pain` | 紫色痛苦 |
| `Proud` | 閃星星自豪 |
| `Shock` | 打雷震驚 |
| `Surprise` | 不規則驚嘆號（外框不規則） |
| `Surprise_2` | 圓形驚嘆號（外框是圓的） |
| `Meditate` | 點點點驚嘆號（外框裡是點點點） |
| `Idea` | 燈泡 |
| `Question` | 圓形問號 |

> **⚠ 大小寫必須完全一致**：`proud`、`anger`、`surprise` 這種小寫寫法**不算**在白名單內，一律寫成 `Proud`、`Anger`、`Surprise`。第三參數也**不可以填數字**（`EnableCharacterExpression(0,MC1-1,4)` 是錯的，數字只用在 `SetPortrait` 的 `pic=`）。

#### 5.1.1a 常見錯名 → 正解（2026-08-27 全案清查所得，寫之前先對一遍）

上表以外的名稱**都不存在**，引擎掛不出特效。全案清查在 `Json/` 與劇情稿裡實際抓到過這些錯名，對應改法如下：

| 寫錯的名稱（來源多半是立繪檔名或自創） | 改成 | 說明 |
| :--- | :--- | :--- |
| `Happy`、`Laugh`、`Joyful`、`proud`（小寫） | `Proud` | 開心、得意笑、閃亮登場 |
| `Hopeful`、`Confident`、`Determined`、`Flirt` | `Proud` | 期待、有把握、放電；若語氣偏沉著改 `Meditate` |
| `Sad`、`Cry`、`Crying`、`Mindpain`、`Despair`、`Hurt`、`hurt` | `Pain` | 難過、哭泣、頭痛心累、絕望（極度震撼可改 `Shock`） |
| `Shy`、`Embarrassed`、`Sleepy` | `Nervous` | 害羞、尷尬、心虛（滿頭大汗用 `Nervous_2`） |
| `Forbearance`、`Thinking`、`Talk`、`Sigh`、`Serious`、`Cunning` | `Meditate` | 隱忍、思考、無奈、若有所思、平淡陳述 |
| `Surprised`、`Shocked`、`Moved` | `Surprise`／`Surprise_2`／`Shock` | 依震驚程度挑，感動到說不出話也可用 `Pain` |
| `Confused`、`Qution` | `Question` | 困惑、不解 |
| `Ideal` | `Idea` | 純拼錯 |
| `Anger_3r` | `Anger_3` | 純拼錯 |
| `Provocative` | `Anger_2` | 挑釁、嘲諷 |
| 數字（如 `4`） | 查 5.2 挑一個 | 數字只屬於 `SetPortrait(…,pic=N)`，不是特效名 |

> **平淡沒有情緒的句子，正解是整條 `EnableCharacterExpression` 不要寫**（見 5.3），不必硬塞一個 `Meditate`。

### 5.2 情緒描述與表情名稱對照表

以下根據《劇情/舊創作稿/初出茅廬之一.md》中的情境及 `Dialog` 資料夾內的檔名，列出了一些常見情緒描述與建議使用的`表情名稱`。

| 情緒/情境描述 (或立繪文字提示) | 建議的`表情名稱` | 產生的指令範例 (以MC1-1為例) |
| :--- | :--- | :--- |
| 驕傲、自豪、閃亮登場 | `Proud` (閃星星) | `EnableCharacterExpression(0,MC1-1,Proud);` |
| 挑釁、嘲諷、欠打 | `Anger_2` (黃圈) | `EnableCharacterExpression(0,MC1-1,Anger_2);` |
| 腹黑、陰險、狡詐、壞笑 | `Anger` / `Meditate`（視語氣） | `EnableCharacterExpression(0,MC1-1,Meditate);` |
| 調情、拋媚眼、放電 | `Proud` / `Surprise`（視語氣） | `EnableCharacterExpression(0,MC1-1,Proud);` |
| 隱忍、咬牙切齒、沉住氣 | `Meditate` / `Anger_2` | `EnableCharacterExpression(0,MC1-1,Meditate);` |
| 絕望、崩潰、眼神死 | `Pain` / `Shock` | `EnableCharacterExpression(0,MC1-1,Pain);` |
| 尷尬、臉紅、緊張、心虛 | `Nervous` (一滴汗) | `EnableCharacterExpression(0,MC1-1,Nervous);` |
| 極度緊張、滿頭大汗 | `Nervous_2` (大量汗) | `EnableCharacterExpression(0,MC1-1,Nervous_2);` |
| 痛苦、難受、受傷 | `Pain` (紫色痛苦) | `EnableCharacterExpression(0,MC1-1,Pain);` |
| 頭痛、懊惱、心累 | `Pain` (紫色痛苦) | `EnableCharacterExpression(0,MC1-1,Pain);` |
| 生氣、憤怒、不滿 | `Anger` (紅色) | `EnableCharacterExpression(0,MC1-1,Anger);` |
| 狂怒、爆氣 | `Anger_2` (黃圈) / `Anger_3` (白吼) | `EnableCharacterExpression(0,MC1-1,Anger_2);` |
| 震驚、驚愕、驚喜 | `Surprise` (不規則) | `EnableCharacterExpression(0,MC1-1,Surprise);` |
| 強烈震驚、難以置信 | `Surprise_2` (圓形) / `Shock` (打雷) | `EnableCharacterExpression(0,MC1-1,Shock);` |
| 開心、得意笑 | `Proud` (閃星星) | `EnableCharacterExpression(0,MC1-1,Proud);` |
| 思考、碎碎念、若有所思、認真 | `Meditate` (點點點) | `EnableCharacterExpression(0,MC1-1,Meditate);` |
| 疑問、不解、困惑 | `Question` (圓形問號) | `EnableCharacterExpression(0,MC1-1,Question);` |
| 靈光一閃、想到辦法 | `Idea` (燈泡) | `EnableCharacterExpression(0,MC1-1,Idea);` |
| 哭泣 | `Pain` / `Nervous_2` | `EnableCharacterExpression(0,MC1-1,Pain);` |

**注意：**
- 資料夾中存在 `UI_Portrait_MC1-1_Qution.png`，這是 `Question` 的拼寫錯誤，建議修正檔名。
- 資料夾中存在 `UI_Portrait_MC1-1_腹黑.png`，建議修正為英文檔名 `UI_Portrait_MC1-1_Cunning.png` 以避免潛在錯誤。


**注意：**
-   燕不凡（`MC1-1`）請優先依 **5.1.1** 白名單選用表情名稱；舊 CSV 或過往劇本若出現 `MC1-1_表情` 單一 token，應改為 `MC1-1,表情` 兩參數。
-   最終使用哪個`表情名稱`，**只取決於 5.1.1 的 13 種白名單**，與 `Dialog` 資料夾裡有哪些立繪檔名無關（2026-08-27 更正：本行原本寫「取決於檔名」，那正是 `Happy`／`Talk`／`Sad`／`Cry`／`Laugh` 這些錯名的來源）。
-   並非所有對話都需要表情特效，如果情境平和或無特別強調的情緒，可以省略`EnableCharacterExpression`。
-   `EnableCharacterExpression` 的第二參數（角色版本ID）需與`SetPortrait`中的角色一致（例如 `MC1` 對應 `MC1-1`）。
-   `位置`參數規則如下：
    -   `EnableCharacterExpression` 指令中的 `位置` 參數，必須對應角色對話框 `panel` 的編號。
    -   例如，若 `蕭靈犀` 的對話為 `[panel=2]...`，則後續指令應為 `EnableCharacterExpression(2,MC8,Surprise)`。
    -   主角 `MC1` (燕不凡) 是特例，其 `位置` 參數固定為 `0`。
    -   `DisableCharacterExpression` 指令中的 `位置` 參數應與其對應的 `EnableCharacterExpression` 指令的 `位置` 參數保持一致。

### 5.3 使用時機與關閉規則

由於指令（存放於 `Sequence` 欄位）與對話文字（存放於 `text` 欄位）在最終資料結構中是分開處理的，因此指令與文字的相對位置描述需基於此結構理解。

-   **啟用 (`EnableCharacterExpression`)**:
    在包含此表情的對話節點的指令序列 (`Sequence`) 中，`EnableCharacterExpression` 指令應緊跟在對應的 `SetPortrait` 指令之後。

-   **停用 (`DisableCharacterExpression(位置);`)**:
    為了避免表情特效持續播放，必須手動關閉。`DisableCharacterExpression` 指令應放置在**緊隨**啟用該表情特效的對話節點之後的**下一個對話節點（或系統訊息、事件節點等）的指令序列 (`Sequence`) 的最開頭**。
    其`位置`參數應與配對的`EnableCharacterExpression`指令一致。如果下一個節點沒有其他指令，則其 `Sequence` 將僅包含此 `DisableCharacterExpression` 指令。

**範例:**
**蕭靈犀:** `[震驚/垮臉立繪]`SetPortrait(MC8,pic=4);EnableCharacterExpression(1,MC8,Surprise);`：「三…三百？！表哥！三百文！我們連昨天的餅錢都還沒付呢！」;`

**[自動洞悉檢定]** (難度 2)
[TUTORIAL：洞悉檢定] // 此處彈出教學提示：當特定條件滿足或劇情推進時，可能會自動進行「洞悉」檢定。成功時，你可能會發現隱藏的細節或獲得額外的對話選項。

**(若洞悉檢定成功)**
    [panel=1]＊（你注意到老伯在說出價格時，眼神似乎閃爍了一下，嘴角有著一絲不易察覺的弧度，似乎這價格並非表面那般簡單。）＊DisableCharacterExpression(1)

**(若洞悉檢定失敗)**
    [panel=1]＊（三百文確實不是個小數目，老伯的表情看起來很正常，似乎這就是公道價。）＊DisableCharacterExpression(1)
## 6. 使用流程建議

1.  閱讀Markdown劇本文件（如《劇情/舊創作稿/初出茅廬之一.md》）。
2.  定位到燕不凡(MC1)、蕭靈犀(MC8)或甄筠(MC9)的對話行。
3.  **處理立繪**: 根據對話行中的立繪描述文字（`[...]`內）或上下文，判斷該角色屬於「多立繪角色」（MC1／MC8／MC22，查 4.1／4.2／4.4，pic 1～15）還是「五立繪角色」（甄筠 MC9 及 MC2～MC10、MC13，查 4.3，pic 僅 1～5），再生成`SetPortrait(角色ID,pic=數字ID);`指令。甄筠 `EnableCharacterExpression` 的角色版本ID直接用 `MC9`（無需 `-1` 後綴），位置對應其 `[panel=N]`（甄府對談時多為 `1`）。
4.  **處理表情特效**: 
    a.  評估當前對話的情緒和語氣。
    b.  參考「5.2 情緒描述與組合ID對照表」以及 `Dialog` 資料夾中的可用立繪檔名，判斷是否需要添加表情特效。
    c.  如果需要，從檔名中提取`角色版本ID`與`表情名稱`，生成`EnableCharacterExpression(位置,角色版本ID,表情名稱);`指令，緊跟在`SetPortrait`指令之後。
    d.  **緊隨其後**，在對話文字的**末尾**添加 `DisableCharacterExpression(位置);` 指令。
5.  將生成的完整指令序列（`SetPortrait;` 可能伴隨 `Enable...;` 和 `Disable...;`）嵌入到最終輸出的JSON或其他格式數據中，與對話文字關聯。

本指南旨在提供一個標準化的轉換基礎，可根據實際項目需求進行調整和擴充。 

## 7. 特殊情境處理

### 7.1 玩家選擇回應中的立繪與表情

在劇本中，如果「玩家（選擇回應）」的選項文字內部包含了立繪描述（如 `[尷尬/臉紅]`）和對應的對話內容，這些描述和伴隨的 `SetPortrait` 及 `EnableCharacterExpression` / `DisableCharacterExpression` 指令，**不應**直接附加到選項文字本身或作為選項節點的 `Sequence`。

**正確處理方式**：
1.  選項文字本身應保持純淨，不包含 `[...]` 立繪描述，也不觸發任何 `Sequence` 指令。
2.  當玩家選擇了某個選項後，如果該選項代表主角（或其他指定角色）會說出一句話，那麼這句話將作為一個獨立的對話節點出現 **緊接在選項分支的開頭**。此時，原選項中描述的立繪和表情指令，應應用到這個**因選擇而觸發的、新插入的對話節點**上。

**範例**：

**原始劇本格式**：
```markdown
**玩家**（選擇回應）：
    1.  `[魅力 難度6]` `[尷尬/臉紅]`：「咳咳！區區三百文…對我輩江湖兒女而言……」
```

**修改後劇本結構** (示意，具體 `Sequence` 行遵循本文檔其他規則)：
```markdown
**玩家**（選擇回應）：
    1.  `[魅力 難度6]`：「三百文？小意思！」 // 選項文本修改為精簡、口語化版本

**(若選擇1：吹squeeze)** // 假設這是選擇選項1後進入的分支
    **燕不凡** `[尷尬/臉紅]`：「咳咳！區區三百文…對我輩江湖兒女而言……」 // 新增的發言節點，使用原始完整對話及立繪描述
    **Sequence SetPortrait(MC1,pic=7);EnableCharacterExpression(0,MC1-1,Nervous);**
    
    // 原本選擇1後的老伯對話等內容
    **茶攤老伯:** ：「哦？小哥兒口氣不小啊...」
    **Sequence DisableCharacterExpression(0);** // 關閉燕不凡的表情
    // ...後續內容...
```

這樣處理可以確保立繪和表情是在角色實際「說話」時才發生變化，而不是在「選擇要說什麼」的階段，同時也為對話文本的潤色處理（如果需要的話）提供了空間，避免選項文本和實際發言文本完全一致可能帶來的重複感。 