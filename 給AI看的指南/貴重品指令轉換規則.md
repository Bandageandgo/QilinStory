# 貴重品指令轉換規則

本文件說明如何在劇情 Markdown／JSON 中給予玩家物品。轉 JSON 見 `劇情/轉成json指南.md` §3.6。

## 目前沒有一般道具

劇情裡給玩家的東西**一律是貴重品**。沒有「一般道具／背包道具」這套系統，**不要**寫 `Inventory` 或 `AddItem`。

## 正確指令

```
ModifyData(Valuable,Player,DragonHorn,1);
```

-   `Valuable`：資料類別（貴重品）。
-   `Player`：**持有者**——不是只能填主角。**娜娜 `MC22` 也能持有**（`大地圖/幽冥禁地.json` `#6`、`大地圖/妖林.json` `#6`、`赫連娜娜線/08第一頁.json` `#4066`、`大地圖/白鹵澤.json` `#4` 都把爹的卷轉到 `MC22` 名下）。主角一律寫 `Player`。
    > **⛔ 主角一律寫 `Player`，`MC1` 是錯的（已定，2026-09-07 作者確認為 BUG）。** 兩者不是同一個 key，混用會讓扣除**靜默失敗**——東西還留在玩家身上，連帶讓 `IsValuablesObtained("Player", …)` 的判定繼續成立。
    > **✅ 全案 8 處已於同日改正**：`大地圖/水濂洞.json` 4 處（`#215`／`#4332` 蛟龍角扣除、`#4335` 測試節點 2 筆）、`古墓演出劇情.json`（`ImmortalJing`）、`小溪村/華陀、老學究 .json`（`HealingPotion`）、`小溪村/赫連娜娜.json`（`Rivertear`）、`小溪村後山/赫連娜娜、張寧.json`（`HealingPotion`）。
    > 當時的實害：蛟龍角 `#135` 用 `Player` 發、成交卻用 `MC1` 扣，而 `#4179`「回去交差」的條件是 `IsValuablesObtained("Player","DragonHorn")`——**賣掉之後那條依然成立**。
-   `DragonHorn`：貴重品 ID。**依該件實際 ID 替換**，必須是英文 ID，不可寫中文名。
-   `1`：數量（或該貴重品系統要求的數值）。

可與立繪／表情寫在同一條 Sequence：

```
**旁白:**：[panel=6]＊（…… **【獲得貴重品：蛟龍角】**）＊
**Sequence DisableCharacterExpression(3);ModifyData(Valuable,Player,DragonHorn,1);**
```

## 禁止的舊寫法

| 錯誤 | 正確 |
| :--- | :--- |
| `ModifyData(Inventory,AddItem,DragonHorn,1);` | `ModifyData(Valuable,Player,DragonHorn,1);` |
| `ModifyData(Inventory,AddItem,RiverGodsTear,1);` | `ModifyData(Valuable,Player,Rivertear,1);` |
| `AddItem(FanZhenCao,1);` | `ModifyData(Valuable,Player,FanZhenCao,1);` |

CSV「特殊標籤」亦以 `ModifyData(Valuable,...)` 開頭，但**沒有數量參數**（例如 `ModifyData(Valuable,MC1,SwordProficiency);`），那是標籤不是物品，不要拿來發蛟龍角。

## 開啟貴重品便籤 `ShowValuableMemo(ValuablesID);`

在對話中把該件貴重品的**便籤（備忘錄）**叫出來給玩家看。**與獲得指令 `ModifyData(Valuable,...)` 分開寫。**

```
ShowValuableMemo(Oddlist);
```

-   `ValuablesID`：同一組英文 ID，與發放、`IsValuablesObtained` 用的完全一致，**不可寫中文名**。
-   **用途：** 長篇文字（名冊、紙條、手記、卷）**不寫進對話節點**，放在貴重品資料裡，由這道指令呼叫。對話那一格只留一句「……」或空白，內容交給便籤演。

### ⚠ 鐵則一：一定排在「已經發出去」之後

**便籤讀的是那件東西的資料，東西還沒發就開不出來。** 所以 `ShowValuableMemo` 必須排在該件的 `ModifyData(Valuable,<持有者>,<ID>,1);` **之後**——不論是同一段 Sequence 的下一行，還是下一個節點。

> **⚠ 持有者是誰不影響開啟（2026-09-07 作者實測更正）。** 本條原本寫「便籤讀的是**玩家手上**那件東西」，因而衍生出一套三步流程：**先發給 `Player` → 開便籤 → 尾格再扣掉、轉給 `MC22`**（娜娜線的卷就是這樣寫的）。實測證明**一開始就發給娜娜（`ModifyData(Valuable,MC22,<ID>,1);`）也開得起來**，那段「先給主角再轉手」是多餘的。
> **新寫一律直接發給該收的人**；既有的三步寫法要不要簡化，見下方〈娜娜的卷：三步流程可簡化〉。

> **定樁事故：** `Json/破廟/阿傑、阿佑、阿偉、水井.json` `#3274` 原本自己給一次名冊、又在同段 Sequence 把 memo 排在 `ModifyData` 前面，等於既重複發物、又依賴尚未持有的東西。2026-08-26 作者裁定：**`#3272` 負責給、`#3274` 只負責開。** 見 `劇情/破廟/00 總覽.md`。

### 兩種現行寫法（都在遊戲裡）

**甲、獨立空節點＋攔住推進（要玩家讀完才走）**

```
**旁白:**：[panel=6]＊（…… **【獲得貴重品：教眾名冊】**）＊
**Sequence ModifyData(Valuable,Player,Oddlist,1);**

**Sequence SetContinueMode(false);SetContinueMode(original)@Message(EndMemo);Continue()@Message(EndMemo);ShowValuableMemo(Oddlist);**
```

（`Json/破廟/阿傑、阿佑、阿偉、水井.json` `#3272`→`#3274`）

-   `SetContinueMode(false);` 先關掉點擊推進。
-   `@Message(EndMemo)` ＝**等便籤關閉才觸發**：關閉的當下恢復推進模式，並自動 `Continue()`。
-   效果：**便籤沒關掉，對話就停在那裡。** 這是「非讀不可」的正規寫法。該節點 `text` 留空。

**乙、掛在一句台詞上（讀不讀隨玩家）**

```
**你:** 「……」
**Sequence DisableCharacterExpression(0);ShowValuableMemo(FollowersLeftNote);**
```

（`Json/趙王洞/山賊窩.json` `#4146`）

-   台詞只留 `「……」`——主角沒話說，內容全在便籤裡。
-   沒有攔截，玩家關掉便籤後照常點擊推進。

**怎麼挑：** 那段文字**是不是後續劇情的前提**？是（名冊、線索、任務憑據）→ 用甲；只是氣氛與補充 → 用乙。

## 娜娜的卷：三步流程可簡化（2026-09-07 作者實測）

赫連娜娜爹的《遊記》四卷目前都寫成三步：

```
#1 首格   ModifyData(Valuable,Player,WanderingNotesPageN,1);      ← 先發給主角
#3 空節點 ShowValuableMemo(WanderingNotesPageN);                  ← 開便籤
#6 尾格   ModifyData(Valuable,Player,WanderingNotesPageN,-1);
          ModifyData(Valuable,MC22,WanderingNotesPageN,1);        ← 扣掉、轉給娜娜
```

**實測證明第一步直接發給 `MC22` 也開得起來**，所以中間那次轉手是多餘的；簡化後只要**首格發給 `MC22`、空節點開便籤**兩步，尾格不必再扣再給。

| 檔案 | 節點 | 狀態 |
| :--- | :--- | :--- |
| `大地圖/幽冥禁地.json`（卷二） | `#1`／`#6` | **作者手動處理（2026-09-07）** |
| `大地圖/妖林.json`（卷三） | `#1`／`#6` | **作者手動處理（2026-09-07）** |
| `赫連娜娜線/08第一頁.json`（卷一） | `#126` 改發 `MC22`／`#4066` 移除轉手兩行 | ✅ **已簡化（2026-09-07）** |
| `大地圖/白鹵澤.json`（卷七） | `#1` 改發 `MC22`／`#4` 移除轉手兩行 | ✅ **已簡化（2026-09-07）** |

> ⚠ 簡化之後，**該卷從頭到尾都在娜娜名下**，主角不曾持有。若有任何 `Conditions` 寫 `IsValuablesObtained("Player", "WanderingNotesPageN")`，要一起改成 `"MC22"`，否則那條分支永遠不成立。（2026-09-07 掃描：全案目前**沒有**任何一條這樣的判定，四卷都只有發放與轉手，改起來是安全的。）

## 讀取持有狀態（寫在 `Conditions`，不寫進 `Sequence`）

```
IsValuablesObtained("Player", "Rivertear") == true;
```

-   ID 與發放指令用同一組英文 ID（`Rivertear`、`FadedTearstone`…），**不可寫中文名**。
-   **⚠ 和別的條件串接時，第二段起一律用小括號包住整段**（全案體例，見 `劇情/轉成json指南.md` §2）：
    -   ✅ `CurrentQuestEntryState("CF40", 6) == "success" and (IsValuablesObtained("Player", "FadedTearstone") == true);`
    -   ❌ `CurrentQuestEntryState("CF40", 6) == "success" and IsValuablesObtained("Player", "FadedTearstone") == true`
    -   少了括號引擎判不出來，**該分支永遠不成立、也不會報錯**——玩家明明帶著東西卻看不到那個選項。2026-08-27 作者在 `大地圖/水濂洞.json` `#4276` 抓到一筆，同檔另有 `#4275`／`#4283`／`#4284` 三筆同錯，已一併改正。

## 水濂洞

| 物品 | ID | 正確指令 |
| :--- | :--- | :--- |
| 蛟龍角 | `DragonHorn` | `ModifyData(Valuable,Player,DragonHorn,1);` |
| 河神的眼淚（活淚） | `Rivertear` | `ModifyData(Valuable,Player,Rivertear,1);`（拿走：`-1`） |
| 褪色的河神眼淚（吸乾後） | `FadedTearstone` | `ModifyData(Valuable,Player,FadedTearstone,1);`（拿走：`-1`） |
| 《河伯的贈禮》 | `HeBoKeepsake` | `ModifyData(Valuable,Player,HeBoKeepsake,1);` |
| 古舟遺物 | `AncientVesselRelic` | `ModifyData(Valuable,Player,AncientVesselRelic,1);` |

> **⚠ 活淚 ID 是 `Rivertear`，不是舊稿的 `RiverGodsTear`。** 吸乾發生在 `Json/大地圖/水濂洞.json` `#4252`：先 `-1` 活淚，再 `+1` 褪色的河神眼淚。交差賣褪色淚走 `#4290` `-1`。

## 黑鐵嶺礦坑（2026-08-27 自創）

| 物品 | ID | 指令 |
| :--- | :--- | :--- |
| 惡神碎片 | `EvilGodFragment` | `ModifyData(Valuable,Player,EvilGodFragment,1);`（拿走：`-1`） |

> **⚠ 這是新貴重品。** 顯示名「惡神碎片」、英文 ID `EvilGodFragment` 是 2026-08-27 為黑鐵嶺三條路收尾自創的，**Unity 貴重品表裡還沒有這一筆**。
>
> - **發放：** `Json/大地圖/黑鐵嶺礦坑.json` `#53`／`#59`／`#76`（殺伐／救贖／血刀都會給）
> - **收回：** 2026-08-28 已進 JSON——`Json/趙王洞/遺跡.json` `#4357`（惡神碎片夢．遞出去）走 `ModifyData(Valuable,Player,EvilGodFragment,-1);`＋`ModifyData(AbilityMaxLevel,Player,Strength,5);`。那一場原本規劃成獨立對話 `惡神碎片(夢).json`，現已整合進趙王洞四條夢的結尾（見 `劇情/趙王洞/`b）。
> - **同步對話之前，必須先在 Unity 貴重品資料建檔**，否則指令會落空。
> - 同一顆東西三種稱呼：顯示名／對白「惡神碎片」＝菈沙口中的「赤眼殘片」＝本土「凶戾核」（`給AI看的指南/世界觀.md` L242）。**不要再另造 ID。**
