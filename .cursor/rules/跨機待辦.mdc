---
description: 兩地 git 交接：拉完必讀跨機待辦，commit 前先更新清單
alwaysApply: true
---

# 跨機待辦

清單在 `給AI看的指南/跨機待辦.md`。對話不會跟 git 走；兩地只認該檔。明細仍在 `劇情/索引.md`，**不要**把〈待進 Unity〉整表抄進跨機待辦，也**不要**掃整份索引當開場報告。

## 「拉一下 git」／ pull 完之後

必須讀 `給AI看的指南/跨機待辦.md`，把**未勾項**唸出來，再問要做哪條。已勾的「已裁示維持」只提醒不要改，不當待辦。卡在作者的未勾項只報、不得自行決定。

## 「同步到 git」／ commit 前

先更新 `給AI看的指南/跨機待辦.md`（做完刪列或打勾，並改最後更新日期），再 commit。使用者沒說 commit 就不要 commit。
