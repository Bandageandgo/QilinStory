# 英豪府 §11　遴選結束（箱庭入口）

> **本檔由 `Json/英豪府遴選(後)/遴選結束.json` 回讀產生，不是創作稿；遊戲內文案的唯一事實來源是該 JSON。**
> 改字請指名錨點（例：「`英豪府遴選(後)/遴選結束.json` #N 這句改成…」）→ AI 改 JSON → Unity 同步 → 重新回讀本檔。直接編輯本檔的文字**不會進遊戲**。
> 讀法、說話者對照（actorID）、本箱庭收錄的全部 JSON 見 `../00 總覽.md`；本章在其他總覽稿的收錄關係見 `劇情/索引.md`。

> **⚠ 本檔只寫這個對話獨有的「大廳門口守門」四句。** 玩家在箱庭走到英豪府大廳門口時 Unity 觸發的是本 JSON（91 節點，入口 `#631`）。
> **戲的本體不在這裡**——`#628` 之後那一整場遴選結束，與 `../10 遴選結束（主線180年2月）.md` **逐欄相同**（2026-08-28 逐欄查證：兩檔可一對一對齊的 77 句在 `text`／`zh_TW`／`zh_CN`／`actorID`／`Sequence`／`Script`／`Conditions`／`Description` 八欄全同，另 4 句是兩組刻意重複的節點，也全同）。**差異只有本檔多出的 4 個守門節點**，正文請讀該檔，錨點換算見下方對照表。

---

### 10-2　箱庭那條路：`英豪府遴選(後)/遴選結束.json`（**另一個 Unity 對話**）

> ⚠ **這一小節記的不是本章的 JSON。** 箱庭走到英豪府大廳門口時，Unity 觸發的是 `Json/英豪府遴選(後)/遴選結束.json`（91 節點，entryID 525–3908），入口 `#631`（Title：英豪府大廳門口）。該檔內容與本章 §10-1 逐句相同（同一場戲的兩個副本），只是錨點自成一套。**2026-08-27 已把本章的文風修正整批補進該檔**，兩邊文案一致。

> ⚙ `#631` 箱庭入口
> - Sequence: `Continue();`
> - Script: `SetQuestEntryState("C1M1", 4, "success")`　⚠ 見章末〈疑點〉一的舊 bug 說明
> - 註記：英豪府大廳門口
> - 分流：`links` 順序 `#567` → `#632`

**▶ 已通過遴選、期限還沒到**

**燕不凡:**`#567`　「離遴選期限，還有段時間……」
- Conditions: `CurrentQuestEntryState("C1M1", 4) == "success"`

**◆ 玩家選擇**

1. `#568`　「時間寶貴，再多加善用吧。」 → 結束對話，回箱庭繼續玩（`links` 為空）
2. `#569`　「罷了，也無它事，就在這待到[em2]二月上旬[/em2]遴選結束吧。」 → 接 `#628`（推進日期＋轉場，＝本章 `#4072` 的對應節點）→ 進該檔自己那一整場遴選結束

**▶ 還沒獲得任何一名考官的認可**

**蕭靈犀:**`#632`　「表哥，你還沒獲得任何一名考官的認可呢，要是耽誤了時間，我們這趟可就白跑了！」

> 錨點對照（本章 ⇄ 遴選結束.json）：`#4072`⇄`#628`、`#4017`⇄`#570`、`#4001`⇄`#525`、`#4002`⇄`#526`、`#4061`⇄`#614`、`#4062`⇄`#616`、`#4071`⇄`#627`、`#4087`⇄`#3908`。

**一、⛔ 2026-08-27：箱庭入口併入本 JSON 一案，同日全案撤銷**

- **作者 2026-08-27 裁示**：這場戲有兩種觸發方式——主線推進到 180 年 2 月上旬會強制播，玩家在箱庭走到英豪府大廳門口也能觸發；**箱庭這條路要留**。
- 當日**先誤判**成「舊檔 `英豪府遴選(後)/遴選結束.json` 已退役，把它的 5 個入口節點抄進本 JSON」，寫成了 `#4088`–`#4092`。
- **作者隨後重新從 Unity 匯出 `英豪府遴選(後)/遴選結束` 對話，證實前提是錯的：**

| | `英豪府遴選(後)/遴選結束` | `主線事件/180年/2月`（本章） |
| :--- | :--- | :--- |
| 觸發 | **箱庭走到英豪府大廳門口** | **主線推到 180/2 上旬（強制播）** |
| 入口節點 | `#631`（Unity Title：英豪府大廳門口） | `#4072`（Unity Title：遴選結束） |
| 節點數 | 91（entryID 525–3908） | 86（entryID 4001–4087） |

- **這是兩個不同的 Unity 對話資產、各有自己的呼叫點。** 箱庭觸發點呼叫的是 `遴選結束` 那個對話，**不會走到本 JSON 裡的複製品**——`#4088` 沒有任何節點或外部觸發指向它，整組是死節點。內容雖是同一場戲的兩份，**誰也取代不了誰，刪任何一邊都會斷一條路**。

**✅ 2026-08-27 當日的收尾（作者核可四項）**

1. **本 JSON 移除 `#4088`–`#4092`**（91 → 86）。查證確認無外部指向、移除後無斷鏈，`#4072` 恢復為唯一入口；**保留的 86 節點一個欄位都沒動**。
2. **`英豪府遴選(後)/遴選結束.json` 復位進 `Json/`**，並補上本章已審過的全部修正：**68 節點的 `text`／`zh_TW`／`zh_CN`**（67 節點補「」引號、16 句文風與禁詞修正）＋**12 處立繪 ID**（賈詡 `SetPortrait(MC11)`→`MC4` 8 處、徐榮 `SetPortrait(MC10)`→`MC5` 4 處）。`entryID`／`links`／`Conditions`／`Script`／`Description`／節點數全未動。兩邊文案現已一致。
3. `劇情/索引.md` 的退役結論撤銷、該檔移回正常收錄。
4. 本章、`劇情/180年事件/02月(主)遴選結束.md` 主線 §1、`劇情/精簡盤點.md` 的相關段落一併改寫。

- ⏳ **兩個檔都尚未同步進 Unity**，登記見 `劇情/索引.md`〈待進 Unity〉；依鐵則 7，同步前先 commit。
- **START 連線不必管**——作者 2026-08-27 明示「不要連 START，本就都沒有作用」。

**⚠ `#631`／`#4088` 的 `Script` 會白送一張「通過遴選」（2026-08-27 查證；作者裁示保留原樣，自行檢查）**

- 本章的 `#4088` 已隨死節點移除，但**同樣的 `Script` 仍在 `遴選結束.json` 的 `#631`**（那才是真正會跑到的節點）。
- 全庫掃過：**`C1M1` 第 4 項＝「拿到任一考官的認可」**，只有四個地方會設成 `success`，就是四位考官各自的過關收尾——

| 設定處 | 考官 |
| :--- | :--- |
| `探索事件/179年/12月.json` `#2267` | 呂信（古墓） |
| `英豪府遴選(後)/羽之演武.json` `#4087` | 子羽 |
| `英豪府遴選(後)/賈之嗜好.json` `#564` | 賈詡 |
| `英豪府遴選(後)/徐之交際 .json` `#685` | 徐榮 |

- 而**讀**它的是本章 `#4017`（＝`遴選結束.json` 的 `#570`），判「通過」還是走壞結局。
- 所以 `#631` 那句 `SetQuestEntryState("C1M1", 4, "success")` 等於**玩家只要走到英豪府大廳門口，就自動算通過遴選**，四位考官一個都不必理。連帶兩個後果：
  1. `#632` 蕭靈犀那句永遠播不到——而那句正是要講「你還沒獲得任何一名考官的認可」。
  2. 二月上旬時間到，`#570` 讀到 `success` 直接走通過路線，壞結局 `#525` 進不去。
- **玩家不會被困住**：`#568`「時間寶貴，再多加善用吧。」`links` 為空＝對話結束、回箱庭繼續玩，隨時走得掉。問題不是卡死，是門口白送通行證。
- **✅ 2026-08-27 作者裁示：「這個留下來，我再去檢查。」** `Script` 原樣留在 JSON 裡。**不要再把它列為待辦或自行修改**——作者會自己查。上面的分析保留在此供他查證時參考。

