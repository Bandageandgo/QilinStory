# 180年 11月事件（養成模式）

> **本檔由 `Json/` 回讀產生，不是創作稿；遊戲內文案的唯一事實來源是各章節對應的 JSON。**
> 改字請指名錨點（例：「`主線事件/180年/11月.json` #N 這句改成…」）→ AI 改 JSON → Unity 同步 → 重新回讀本檔對應章節。直接編輯本檔的文字**不會進遊戲**。
> **本檔＝這個月的全部戲。** 11 月只有主線，沒有支線事件（`探索事件/180年/11月.json` 未建）。
> 讀法、年表、說話者對照（actorID）、本年收錄的全部 JSON 見 `00 總覽.md`；本章在其他總覽稿的收錄關係見 `劇情/索引.md`。
>
> **⚠ 這一天是第二章的死線，順序不可倒：先補播、再判生死。**
> 1. 已經結算過 → 跳過補播，直接接饕餮骰揭露。
> 2. 沒結算過、但 `C2M1` entry 1 或 2 有 `success`（錢已到）→ **補播結算場**，播完再接饕餮骰揭露。
> 3. entry 1、2 都不是 `success` → **乞丐 BD**，當天到此為止，不播饕餮骰揭露。
>
> **⚠ 判的是 entry，不是總任務。** 總任務 `C2M1` 的 `success` 寫在結算場收尾——10 月才談成的玩家當天還沒結算過，判總任務會把他誤判成失敗。
> **⚠ 結算場本體不在本月檔。** `填補赤字結算.json`、`舌戰三家.json` 是獨立對話，9 月下旬已經呼叫過一次；本月只提供**冬天那一版的首格旁白**再交接過去，**內容不重貼**，見 `09月(主)填補赤字結算.md` §2、§3。
>
> **創作稿：** `劇情/填補赤字_觸發分流.md` §二。

---

## 收錄 JSON

| JSON | 節點 | zh_CN | 回讀 | 亦收錄於 |
|:--|:-:|:-:|:--|:--|
| `主線事件/180年/11月.json` | 10（有文案 2） | 有 | 2026-08-30 | — |
| `主線事件/180年/填補赤字結算.json` | 37 | 有 | 2026-08-30 | **正本在 `09月(主)填補赤字結算.md` §2**（本檔只指路） |
| `主線事件/180年/舌戰三家.json` | 333 | 有 | 2026-08-30 | **正本在 `09月(主)填補赤字結算.md` §3**（本檔只指路） |
| `主線事件/180年/饕餮骰揭露.json` | ⏳ 未建 | — | — | 創作稿 `劇情/張仲景_饕餮骰揭露.md` |
| `雲中/乞丐結局.json` | ⏳ 未建 | — | — | 創作稿 `劇情/雲中/@結局_乞丐.md` |

## 說話者對照

| actorID | 寫作名稱 | 依據 |
|:--|:--|:--|
| `2` | 旁白 | 轉成json指南 §6 |
| `0` | （系統節點，不標說話者） | 同上 |
| `MC5` | 徐榮 | 立繪指令轉換規則 §2 |
| `MC8` | 蕭靈犀 | 同上 |

---

## 主線｜1. 主線事件/180年/11月.json

> 對應 JSON：`Json/主線事件/180年/11月.json`｜節點 10（有文案 2）｜entryID 1 – 10｜zh_CN：有｜回讀：2026-08-30
> 內容：180 年 11 月下旬，「填補赤字」死線。整份幾乎都是分流節點——先判有沒有結算過，沒結算但錢已到就補播結算場（首格換成冬天那一版），一家都沒談成就走乞丐 BD。
> 入口：**1 個**——`#1`（180 年 11 月下旬強制觸發，無視主角位置）。
> ⚠ **有文案的只有兩格**（`#6`、`#10`），都是季節首格旁白，與 9 月那兩格只差窗外的景。

### 1-1　入口 `#1`：先判有沒有結算過

> ⚙ `#1` 入口
> - Sequence: `Continue();`
> - 註記：11 月下旬入口：先判有沒有結算過（順序不可倒：先補播、再判生死）
> - 分流：已結算 → `#2`；未結算（else）→ `#3`

> ⚙ `#2` 已結算過，跳過補播
> - Sequence: `Continue();`
> - Conditions: `CurrentQuestState("C2M1") == "success";`
> - 註記：已結算過 → 跳過補播，接對話「張仲景・饕餮骰揭露」〈前置：藏書庫〉（Unity 端接跨對話連線）
> - `links: []`（交接點）

> ⚙ `#3` 未結算（else），改判 entry
> - Sequence: `Continue();`
> - 註記：未結算（else）→ 判 C2M1 entry 1／2
> - 分流：赤龍幫已出資 → `#4`；否則 → `#5`

> ⚙ `#4` 赤龍幫已出資
> - Sequence: `Continue();`
> - Conditions: `CurrentQuestEntryState("C2M1", 1) == "success";`
> - 註記：分流：赤龍幫已出資
> - 分流：甄家也出資 → `#6`；否則 → `#7`

> ⚙ `#5` 赤龍幫未出資（else）
> - Sequence: `Continue();`
> - 註記：分流：赤龍幫未出資（else）
> - 分流：甄家出資 → `#8`；兩家都沒 → `#9`

### 1-2　`#6`：兩家都出資（補播舌戰三家）

**旁白:**`#6`　[panel=6]＊（十一月下旬。院裡那棵棗樹早光了，廊下結著霜。正廳的案上攤著兩卷帳，徐榮一卷一卷點過去，點得很慢；小犀跪坐在案邊，一把算籌撥一下、記一筆，已經撥了大半個時辰。）＊
- Sequence: `EnableDialogueBG(HallOfHeroes); SetPortrait(MC5,pic=1); SetPortrait(MC8,pic=14);`
- Conditions: `CurrentQuestEntryState("C2M1", 2) == "success";`
- 註記：交接點：兩家都出資 → 補播對話「主線事件/180年/舌戰三家」（Unity 端接跨對話連線；背景不關）

### 1-3　`#10`：只有一家出資（補播填補赤字結算）

> ⚙ `#7` 只有赤龍幫（else）／`#8` 只有甄家——兩條都併到 `#10`
> - `#7` Sequence: `Continue();`｜註記：分流：只有赤龍幫（else）｜→ `#10`
> - `#8` Sequence: `Continue();`｜Conditions: `CurrentQuestEntryState("C2M1", 2) == "success";`｜註記：分流：只有甄家｜→ `#10`

**旁白:**`#10`　[panel=6]＊（十一月下旬。院裡那棵棗樹早光了，廊下結著霜。正廳的案上攤著一卷帳，徐榮一行一行點過去，點得很慢；小犀跪坐在案邊，一把算籌撥一下、記一筆，已經撥了大半個時辰。）＊
- Sequence: `EnableDialogueBG(HallOfHeroes); SetPortrait(MC5,pic=1); SetPortrait(MC8,pic=14);`
- 註記：交接點：只有一家出資 → 補播對話「主線事件/180年/填補赤字結算」（Unity 端接跨對話連線；背景不關）

> ⚠ `#6`／`#10` 與 `9月.json` 的 `#4`／`#8` **只差開頭那句季節**：九月是落棗、十一月是結霜。**一個字也不提玩家晚了兩個月**——沒有人抱怨、沒有人鬆一口氣。

### 1-4　`#9`：一家都沒談成 → 乞丐 BD

> ⚙ `#9` 走乞丐結局
> - Sequence: `Continue();`
> - 註記：entry 1、2 都沒 success（else）→ 接對話「雲中/乞丐結局」（ShowEnding(Ending_1)）；當天不播饕餮骰揭露
> - `links: []`（交接點）

---

## 回讀時發現的疑點（只記錄，未動 JSON；要改請指名）

1. **`#2`、`#9` 是交接點，`links: []` 且沒有尾格。** 兩者都沒開過背景，所以不必 `DisableDialogueBG()`；`#6`／`#10` 開了背景但**刻意不關**，關背景在結算場自己的尾格。
2. **`饕餮骰揭露`、`乞丐結局` 兩份 JSON 都還沒建**，`#2`／`#9` 目前是斷頭。創作稿分別是 `劇情/張仲景_饕餮骰揭露.md`、`劇情/雲中/@結局_乞丐.md`，兩份都還沒過稿。
3. **`C2M1` entry 2（甄家）在全案沒有任何一處被寫入**——同 `09月(主)填補赤字結算.md` 疑點 1。**不補的話 `#4`／`#5`／`#8` 的甄家那一路等於不存在，錢談成了也會走 `#9` 吃乞丐 BD。**
4. **`#2` 的條件只讀總任務。** 雙投版收尾已於 2026-08-30 補寫 `SetQuestState("C2M1", "success");`（原本只寫 entry 4），否則雙投版玩家 9 月結算完，11 月會被抓去再播一次。
