# 180年 11月事件（養成模式）

> **本檔由 `Json/` 回讀產生，不是創作稿；遊戲內文案的唯一事實來源是各章節對應的 JSON。**
> 改字請指名錨點（例：「`主線事件/180年/11月.json` #N 這句改成…」）→ AI 改 JSON → Unity 同步 → 重新回讀本檔對應章節。直接編輯本檔的文字**不會進遊戲**。
> **本檔＝這個月的全部戲。** 11 月只有主線，沒有支線事件（`探索事件/180年/11月.json` 未建）。
> 讀法、年表、說話者對照（actorID）、本年收錄的全部 JSON 見 `00 總覽.md`；本章在其他總覽稿的收錄關係見 `劇情/索引.md`。
>
> **⚠⚠ 2026-09-03 作者大改：`11月.json` 從 12 節點變成 355 節點，改成「自己收整場」。** 舊版只放冬天的首格旁白、再用**跨對話連線**呼叫 `9月.json` 的兩版結算場；新版把**整個結算場複製進本檔**，不再需要跨對話連線。`#4` → `#4351`（雙投版）、`#8` → `#4336`（獨資版）現在都是**檔內連線**。
> **⚠⚠ 代價：本檔 354 格裡有 349 格與 `9月.json` 逐字相同**（同 entryID、同內容；只有 `#4`／`#7`／`#8`／`#10`／`#15` 五格不同，另 9 月的 `#9`／`#11`–`#14`／`#4335` 本檔沒有）。**兩版結算場現在有兩份正本——改任何一句台詞都必須兩檔各改一次**，只改一邊會讓 9 月與 11 月演出不一致。
>
> **⚠ 這一天是第二章的死線，順序不可倒：先補播、再判生死。**
> 1. 已經結算過 → 跳過補播，直接接饕餮骰揭露。**⚠⚠ 這一條現在在 JSON 裡不存在了，見〈回讀時發現的疑點〉第 1 條。**
> 2. 沒結算過、但 `C2M1` entry 1 或 2 有 `success`（錢已到）→ **補播結算場**，播完再接饕餮骰揭露。
> 3. entry 1、2 都不是 `success` → **英豪府倒閉 → 乞丐 BD**（`#7`→`#10`→`#15`），當天到此為止，不播饕餮骰揭露。
>
> **⚠ 判的是 entry，不是總任務。** 總任務 `C2M1` 的 `success` 寫在結算場收尾——10 月才談成的玩家當天還沒結算過，判總任務會把他誤判成失敗。
>
> **創作稿：** `劇情/舊創作稿/填補赤字_觸發分流.md` §二。

---

## 收錄 JSON

| JSON | 節點 | zh_CN | 回讀 | 亦收錄於 |
|:--|:-:|:-:|:--|:--|
| `主線事件/180年/11月.json` | **354**（有文案 289） | 有 | **2026-09-03（第二次：作者修過一輪後重新匯出）** | — |
| `主線事件/180年/9月.json` | 360 | 有 | 2026-09-03 | **⚠ 本檔 `#16` 以後與該檔逐字重複**，逐句正本在 `09月(主)填補赤字結算.md` §2／§3 |
| `主線事件/180年/饕餮骰揭露.json` | ⏳ 未建 | — | — | 創作稿 `劇情/張仲景_饕餮骰揭露.md` |
| `雲中/乞丐結局.json` | ⏳ 未建 | — | — | 創作稿 `劇情/雲中/@結局_乞丐.md` |

## 說話者對照

| actorID | 寫作名稱 | 依據 |
|:--|:--|:--|
| `role2` | 旁白 | 轉成json指南 §6 |
| `0` | （系統節點，不標說話者） | 同上 |
| `MC1` | 燕不凡 | 立繪指令轉換規則 §2 |
| `MC4` | 賈詡 | 同上 |
| `MC5` | 徐榮 | 同上 |
| `MC8` | 蕭靈犀 | 同上 |
| `MC9` | 甄筠 | 同上（`[panel=4]`） |
| `MC10` | 褚人飛 | 同上（`[panel=1]`） |
| `MC11` | 黑狼王 | 同上（`[panel=3]`） |
| `MC2`／`MC3`／`MC7` | 呂信／子羽／郭嘉 | 同上 |

---

## 主線｜1. 分流門與倒閉 BD（`#1`–`#15`，本檔獨有的部分）

> 對應 JSON：`Json/主線事件/180年/11月.json`｜本節 `#1`–`#8`、`#10`、`#15`｜zh_CN：有｜回讀：2026-09-03
> 入口：**`#1`**（180 年 11 月下旬強制觸發，帶 `title`「赤字結算(前)」）——**單一入口、單一入口**，`#4335` 孤兒已由作者刪除（2026-09-03）。

### 1-1　入口 `#1`：開背景、依 `C2M1` 分流

> ⚙ `#1` 入口（`title`：赤字結算(前)）
> - Sequence: `EnableDialogueBG(HallOfHeroes); Continue();`
> - 分流：赤龍幫已出資 → `#2`；否則 → `#3`

> ⚙ `#2` 赤龍幫已出資
> - Sequence: `Continue();`
> - Conditions: `CurrentQuestEntryState("C2M1", 1) == "success";`
> - 分流：甄家也出資 → `#4`；否則 → `#5`

> ⚙ `#3` 赤龍幫未出資（else），換音樂
> - Sequence: `AudioControl(PlayMusic,BGM_23); Continue();`
> - 分流：甄家出資 → `#6`；兩家都沒 → `#7`

### 1-2　`#4`：兩家都出資（接雙投版結算場）

**旁白:**`#4`　[panel=6]＊（十一月下旬。院裡的棗樹落了一地，沒人掃。正廳的案上攤著兩卷帳，徐榮一卷一卷點過去，點得很慢；小犀跪坐在案邊，一把算籌撥一下、記一筆，已經撥了大半個時辰。）＊
- Sequence: `SetPortrait(MC5,pic=1); SetPortrait(MC8,pic=14);`
- Conditions: `CurrentQuestEntryState("C2M1", 2) == "success";`
- 註記：交接點：兩家都出資 → `#4351`（檔內）
- ✅ **2026-09-03 已改成十一月**（原本誤留九月）。作者只換了月份、保留「棗樹落了一地，沒人掃」，沒有採用舊版那句「棗樹早光了，廊下結著霜」——**這是作者的選擇，不要再改回去。**

### 1-3　`#8`：只有一家出資（接獨資版結算場）

> ⚙ `#5` 只有赤龍幫（else）／`#6` 只有甄家——兩條都併到 `#8`
> - `#5` Sequence: `Continue();`｜註記：分流：只有赤龍幫（else）｜→ `#8`
> - `#6` Sequence: `Continue();`｜Conditions: `CurrentQuestEntryState("C2M1", 2) == "success";`｜註記：分流：只有甄家｜→ `#8`

**旁白:**`#8`　[panel=6]＊（十一月下旬。院裡那棵棗樹早光了，廊下結著霜。正廳的案上攤著兩卷帳，徐榮一卷一卷點過去…）＊
- Sequence: `SetPortrait(MC5,pic=1); SetPortrait(MC8,pic=14); AudioControl(PlayMusic,BGM_19);`
- 註記：交接點：只有一家出資 → `#4336`（檔內）
- ⚠⚠ **2026-09-03 改過一次，但改成了「兩卷帳」版，而且句子沒寫完。** 這一條是**只有一家出資** → 獨資版結算場（下一句是小犀「三萬六千！湊齊啦！」），帳應該是**一卷**、點法應該是**一行一行**；現在寫的是「兩卷帳／一卷一卷」，與上面 `#4`（兩家都出資）撞在一起。而且結尾停在「點過去…」就收了，後半句（「點得很慢；小犀跪坐在案邊……已經撥了大半個時辰」）不見了。見〈疑點〉第 2 條。

### 1-4　`#7` → `#10` → `#15`：一家都沒談成 → 英豪府倒閉 → 乞丐 BD

**旁白:**`#7`　＊（十一月三十，霜還沒化。差役來封門，一張封條壓著一張。英豪府那塊匾是最後抬下來的，抬下來也沒人要，就靠在階前的牆邊。）＊
- Sequence: `SetPortrait(MC4,pic=1);`
- ⚠ **缺 `[panel=6]`**（旁白必標，`文本創作指南` §2）。⚠ `SetPortrait(MC4,pic=1)` 是賈詡——但這一格的旁白裡沒有他（舊版九月那格是「賈詡把你叫到正廳」才需要），見〈疑點〉第 4 條。

**你:**`#10`　「……」
- Sequence: `SetPortrait(MC1,pic=6);`
- ⚠ 舊版 11 月檔這一句是「[em7]把那塊匾抱起來[/em7]，……先收著。回頭再掛。」，現在只剩「……」。是刻意改成更冷的收法，還是複製 `9月.json` `#10`（「先生，我——」）時只換了字？見〈疑點〉第 6 條。

> ⚙ `#15` 尾格（`links: []`）：關背景、失敗結局
> - Sequence: `DisableAllCharacterExpression(); DisableDialogueBG(); ShowEnding(Ending_1); Continue();`
> - 註記：尾格：關背景，失敗結局

> ⚠ **既有的長版〈抄家〉創作稿仍在** `劇情/雲中/@結局_乞丐.md`（討債班頭、賈詡想溜、雍仔的葫蘆、娜娜抱著藏寶圖），**那份還沒轉 JSON**。現行 JSON 是三格短版直接 `ShowEnding(Ending_1)`；長版要不要接在這三格之後、還是取代它們，**待作者裁示**。

---

## 主線｜2／3. 兩版結算場（`#16` 以後，與 `9月.json` 重複）

> **⚠ 本節不重貼逐句。** `#16`–`#37`、`#38`–`#333`、`#763`、`#766`–`#775`、`#4336`–`#4385` 共 345 格，**與 `9月.json` 同 entryID、逐字相同**。
> **逐句正本在 [`09月(主)填補赤字結算.md`](09月(主)填補赤字結算.md) §2（獨資版）與 §3（雙投版）**，號碼可以直接對照。
>
> | 這一節 | 進入點 | 結尾 |
> | :--- | :--- | :--- |
> | §2 獨資版結算場 | `#8` → `#4336` | `#37` |
> | §3 雙投版結算場（舌戰三家，`title` 在 `#4351`） | `#4` → `#4351` | `#326`（危險平衡）／`#766`（離席 BD） |
>
> **⚠⚠ 改字要改兩檔。** 這 345 格在 `9月.json` 與 `11月.json` 各有一份，**只改一邊會讓九月與十一月演出不一致**。
> **⚠ `Variable["BreakAlliance"]` 兩檔共用同一個變數。** 九月播過一次、十一月再播一次的話，變數不會自動歸零——**Unity 端要確認補播前有沒有重設**，否則十一月一開場就可能直接判離席。

---

## 回讀時發現的疑點（只記錄，未動 JSON；要改請指名）

> **2026-09-03 第二輪：作者已修掉兩項**——`#4` 的月份、`#4335` 孤兒節點（已刪，本檔現在單一入口）。以下是**還沒處理的**。

1. **⚠⚠⚠ 「已經結算過就跳過補播」的判斷還是不見了。** 舊版 `#2` 掛 `CurrentQuestState("C2M1") == "success";` 讓已結算的玩家跳過、去接饕餮骰揭露；**現行全檔 `CurrentQuestState` 仍是 0 次**，`#1` 直接照 9 月的樣子判 entry 1／2。結果：**9 月已經結算完的玩家，11 月會被抓去把整場結算再演一次。** 檔頭那條「1. 已經結算過 → 跳過補播」目前在 JSON 裡沒有對應的格。
2. **⚠⚠ `#8` 改成「兩卷帳」版了，而且句子沒寫完。** 這一條是**只有一家出資** → 獨資版（下一句小犀喊「三萬六千！湊齊啦！」），應該是**一卷帳／一行一行**。現行文字是「兩卷帳／一卷一卷」，跟 `#4`（兩家都出資）重複；結尾又停在「點過去…」，後半句沒了。**照 `#4` 的體例，這一格應該是：**
   > ＊（十一月下旬。院裡的棗樹落了一地，沒人掃。正廳的案上攤著**一卷帳**，徐榮**一行一行**點過去，點得很慢；小犀跪坐在案邊，一把算籌撥一下、記一筆，已經撥了大半個時辰。）＊
3. **⚠ `#7` 缺 `[panel=6]`**（旁白必標，`文本創作指南` §2——全檔只有這一格漏）。另 `SetPortrait(MC4,pic=1)`（賈詡）在這一格沒有對象：旁白只寫差役封門、匾抬下來，賈詡不在場。那是從 `9月.json` `#7`（「賈詡把你叫到正廳」）帶過來的。
4. **`#10` 主角那句仍是「……」。** 舊版 11 月檔是「[em7]把那塊匾抱起來[/em7]，……先收著。回頭再掛。」——刻意改冷，還是複製 `9月.json` `#10`（「先生，我——」）時只換了字？**未確認。**
5. **`饕餮骰揭露`、`乞丐結局` 兩份 JSON 都還沒建。** 現行本檔沒有任何一格接去饕餮骰揭露（舊版 `#2` 那個交接點隨第 1 條一起沒了）；`#15` 直接 `ShowEnding(Ending_1)` 收掉倒閉那條。
6. **⚠ 維護面加倍**：本檔 349 格與 `9月.json` 逐字重複，**改任何一句台詞都要兩檔各改一次**。`Variable["BreakAlliance"]` 也是兩檔共用同一個變數——九月播過一次之後不會自動歸零，**補播前 Unity 端要確認有沒有重設**，否則十一月一開場就可能直接判離席。
7. **✅ `C2M1` entry 2（甄家）已寫入**（2026-09-03 查證）：`Json/大地圖/水濂洞.json` `#379`／`#380`／`#4338`／`#4341` 寫 `success`、`#4344` 寫 `failure`。
