# 180年 10月事件（養成模式）

> **本檔由 `Json/` 回讀產生，不是創作稿；遊戲內文案的唯一事實來源是各章節對應的 JSON。**
> **本檔＝這個月的全部戲**：`## 主線｜` 是主動觸發的主線事件，`## 支線｜` 是選「探險」指令後才觸發的支線事件。
> 讀法、年表、說話者對照（actorID）、本年收錄的全部 JSON 見 `00 總覽.md`；本章在其他總覽稿的收錄關係見 `劇情/索引.md`。
>
> **⚠ 本月的 JSON 沒有文案，本檔因此不貼對話。** 180 年 10 月的戲是**調用角色線的個人事件**——`探索事件/180年/10月.json` 只留一顆空節點當呼叫用的殼，實際內容在 `Json/赫連娜娜線/13頭巾.json`（體例同 `01月(支)儺隊過街.md`、`05月(主)端午(支)死地生綠.md`〈支線〉）。
> **⚠ 佔位節點照 `探索事件/180年/5月.json` 的體例建**（`role2`、無文案、無 `links`），匯入 Unity 之後不得改號、不得刪除。
> **⚠ 10 月沒有主線事件**（`主線事件/180年/10月.json` 未建）。

---

## 支線｜探索事件/180年/10月.json

> **✅ 2026-09-08 作者定：赫連娜娜〈頭巾〉掛本月支線（探索事件）。**
> 對應 JSON：`Json/探索事件/180年/10月.json`｜節點 1（**有文案 0**）｜entryID 1｜zh_CN：無（無文案可譯）｜建檔：2026-09-08
> 入口：**無可讀入口**——只有一顆佔位空殼。

| entryID | actorID | Description（原文照抄） | 實際演的是 |
| :--- | :--- | :--- | :--- |
| `#1` | `role2` | 這格無作用，會實際上使用「赫連娜娜線/13頭巾」 | `Json/赫連娜娜線/13頭巾.json`（入口 `#1`、終點 `#30`，30 節點）→ 正式稿 [`劇情/赫連娜娜/13 頭巾（白鹵澤之後）.md`](../赫連娜娜/13%20頭巾（白鹵澤之後）.md) |

**〈頭巾〉是什麼：** 娜娜線的**好感綁定事件**（感情線 R3）。白鹵澤回來之後，夜裡她在馬廄刷完馬、蹲在水槽邊洗白鹵澤的鹽，頭巾搭在欄上——他走進來，第一次看見她髮間那兩個小小的尖。她說「這一代，是我」，問他嚇著了沒。三選一：調情檢定（他先講自己的身世——故鄉燒了、兩個小孩子流浪、一頭白髮看盡臉色；她才交心事——怕自己不算人、胡漢兩邊都不算數；撩只有最後一句「不遮，我就這麼看著妳」）／「比我想的小」／不說話把頭巾遞回去。**三條都不扣分。** 收尾她讓出半邊乾草要他坐，頭巾被風掀起一角，她沒去拿。

- **⚠ 觸發條件由作者在 Unity 端設定。** 設計上＝白鹵澤走完（`CF45` entry 3 `success`）＋好感第 2 級（`IsFavorabilityLevelPass("MC22", 2)`）。
- **好感度：** 三處各 **+30**（調情成功 `#17`、「比我想的小」`#22`、遞頭巾 `#26`）；調情失敗 0。單輪 +30。
- **引擎：** 尾格 `#30` 收 **`CF45` entry 4 `success`**（作者 2026-09-08 定）。背景 `HorseFarm`（馬廄）。音樂未填。
- **⏳ 待辦：** 兩份 JSON（本月佔位檔＋娜娜線那份）都**尚未匯入 Unity**；沒有 `＿＿` 要填。

---

## 回讀時發現的疑點（只記錄，未動 JSON；要改請指名）

（無。）
