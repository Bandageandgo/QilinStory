# 180年 4月事件（養成模式）

> **本檔由 `Json/` 回讀產生，不是創作稿；遊戲內文案的唯一事實來源是各章節對應的 JSON。**
> **本檔＝這個月的全部戲**：`## 主線｜` 是主動觸發的主線事件，`## 支線｜` 是選「探險」指令後才觸發的支線事件，兩者各自對應一份 JSON。
> 讀法、年表、說話者對照（actorID）、本年收錄的全部 JSON 見 `00 總覽.md`；本章在其他總覽稿的收錄關係見 `劇情/索引.md`。
>
> **⚠ 本月兩份 JSON 都沒有文案，本檔因此不貼對話。** 180 年 4 月的戲全部是**調用角色線的個人任務**：`4月.json` 兩份（主線／探索）裡只有空的佔位節點（`text` 空、`links` 空、只有 `Description` 註明要調哪一支），實際要演的對白在**賈詡線、赫連娜娜線、呂信線各自的 JSON** 裡（主線兩支＋支線兩支）。**要改本月的字，去那四份 JSON 改，不要改這裡、也不要往佔位 JSON 裡塞文案。**
> **⚠ 佔位節點是作者在 Unity 建的，不得改號、不得刪除**（`給AI看的指南/Unity對話同步流程.md` 鐵則：entryID 一經同步不可改號）。

---

## 主線｜主線事件/180年/4月.json

> 對應 JSON：`Json/主線事件/180年/4月.json`｜節點 2（**有文案 0**）｜entryID 116、118｜zh_CN：無（無文案可譯）｜回讀：2026-09-06
> 入口：**無可讀入口**——兩顆節點都無 `links`、無 `Sequence`，只是 Unity 端掛呼叫用的空殼。

| entryID | actorID | Description（原文照抄） | 實際演的是 |
| :--- | :--- | :--- | :--- |
| `#116` | `0` | 此節點無作用，本對話會調用賈詡線的事件01兩卷 | `Json/賈詡線/01兩卷.json`（入口 `#347`）→ 回讀／創作稿 `劇情/賈詡/01 兩卷.md` |
| `#118` | `0` | 此節點無作用，本對話會調用娜娜線的08第一頁 | `Json/赫連娜娜線/08第一頁.json`（入口 `#91`）→ 回讀稿 `劇情/赫連娜娜/08 第一頁（180年4月主線）.md` |

- **賈詡〈兩卷〉**＝賈詡線第 1 支（解謎線四支之首，任務 `CF44`）。他派差事、每辦成一件就讀一點麒麟骰的東西給主角聽。**⏳ 已轉 JSON、未進 Unity。**
- **娜娜〈第一頁〉**＝她在市集攤子上認出爹的字，卷二便籤當場開，帶出幽冥禁地與妖林兩座地城（任務 `CF45`，貴重品 `WanderingNotesPage1`）。
- 這兩支**原本併在 `4月.json` 裡**（賈詡 `#1`–`#90`、娜娜 `#91`–`#114`），**2026-09-02 由作者拆成兩份獨立 JSON**，`4月.json` 只留下這兩顆空殼。舊稿寫的「同一份 JSON 兩個入口」已作廢。

## 支線｜探索事件/180年/4月.json

> **本月兩條支線：** 呂信〈第三百四十九次〉（`#1`）＋娜娜〈一百錢〉（`#2`）。兩顆都是佔位空殼，體例照 `主線事件/180年/4月.json` `#116`／`#118`。
> 對應 JSON：`Json/探索事件/180年/4月.json`｜節點 **2**（**有文案 0**）｜entryID 1–2｜zh_CN：無（無文案可譯）
> 入口：**無可讀入口**——同樣是空殼；`actorID` 掛著 `role2`（旁白），但 `text` 是空的。

| entryID | actorID | Description（原文照抄） | 實際演的是 |
| :--- | :--- | :--- | :--- |
| `#1` | `role2` | 這格無作用，會實際上使用「呂信線/02第四百三十九次」 | `Json/呂信線/02第三百四十九次.json`（121 節點）→ 回讀稿 `劇情/呂信/02 第三百四十九次.md` |
| `#2` | `role2` | 這格無作用，會實際上使用「赫連娜娜線/14一百錢」 | `Json/赫連娜娜線/14一百錢.json`（入口 `#1`、終點 `#25`，**25 節點**）→ 創作稿 `劇情/赫連娜娜/14 一百錢.md` ⚠ **待進 Unity** |

- **呂信〈第三百四十九次〉**＝演武場上有人來找他打第三百四十九次；掛好感度門檻＋地點觸發。編號是刻意的，`劇情/雙世冥劍.md` §12 子羽那句「第三百五十次」要留著不動。

**〈一百錢〉是什麼：** 娜娜感情線 R1（白鹵澤之前、好感 1 級）。英豪府門口糖炒栗子阿婆，主角沒帶錢、小犀吞口水，娜娜直接付；三選一都不扣分、各 +20。`#10` 收 `CF18` entry 5。阿婆 `role5` 無立繪。

- **觸發條件（作者在 Unity 設）：** 設計＝進駐英豪府後主線 `＿＿` ＋ `IsFavorabilityLevelPass("MC22", 1)`。感情線任務號尚未建。
- **背景／音樂：** `Yunzhong`；`BGM_30`。
- **⏳ 尚未匯入 Unity。** 4 月檔已在 Unity（原本 1 節點），本輪加了 `#2`——走更新、不要砍掉重匯。Unity 端要另掛一個 4 月探險入口。

## 回讀時發現的疑點

1. **支線佔位節點的 `Description` 把數字寫錯了**：寫的是「呂信線/02**第四百三十九次**」，`Json/呂信線/` 底下實際只有 `02第三百四十九次.json`，創作稿也是 `劇情/呂信/02 第三百四十九次.md`——**是 `Description` 手誤，指的就是那一支**。依回讀規矩（發現問題只記、不動 JSON）**未改**；作者要順手改的話，改 `探索事件/180年/4月.json` `#1` 的 `Description` 即可，那欄不是遊戲文案、不影響 Unity。
2. 主線兩顆空殼的 `actorID` 是 `0`（系統節點）、支線兩顆是 `role2`（旁白），兩邊不一致；四顆都沒有文案，**不影響遊戲**，記一筆備查。
