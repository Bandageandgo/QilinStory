# 赫連娜娜・一百錢（好感綁定事件・白鹵澤之前）

> **本檔由 `Json/` 回讀產生，不是創作稿；遊戲內文案的唯一事實來源是 `Json/赫連娜娜線/14一百錢.json`。** 改字請指名錨點（例：「`赫連娜娜線/14一百錢.json` #9 這句改成…」）→ AI 改 JSON（`text` ＋ **重翻 `zh_CN`**）→ Unity 同步 → 重新回讀本檔。規則：`給AI看的指南/Unity對話同步流程.md`〈文案總覽稿〉。
> **狀態：✅ 2026-09-09 依作者從 Unity 匯出的 JSON 回讀（25 節點）。** 設計端見同資料夾 [`赫連娜娜.md`](赫連娜娜.md)〈感情線〉R1；人物本體見 [`@角色設定/赫連娜娜.md`](../../@角色設定/赫連娜娜.md)。
> **✅ 作者拍板：** 感情線 R1，**排在白鹵澤之前**（2026-09-07）。入口 `#1` 帶 `title`「100錢」、`Description`「180年4月上旬」，**無 `Conditions`——觸發條件作者在 Unity 端設**（設計＝進駐英豪府後＋好感 1 級）。
> **⚠ 名稱：** 檔名與行文用〈一百錢〉；JSON 的 `title` 是作者寫的「100錢」，**`title` 不動**。全案 JSON 只用「錢」不用「文」（「文」當錢的單位是漢以後的說法）。
> **這一場是什麼：** 角色檔〈大方、豪爽〉那條的現場示範——**主角沒帶錢、小犀在吞口水，娜娜直接付**。情竇未開，先欠一百錢。她不邀功、說完就忘；記帳的是小犀。
> **阿婆：** 沿用 `大地圖/雲中/街道.json` 的糖炒栗子阿婆（`role5`）——**她全場只有那一句「糖炒栗子，一包一百錢。」，笑容從來不變**（`#53` 體例，`[em7]笑容不變重複道[/em7]` 照抄）；小犀早就覺得她走到哪都在（`劇情/蕭靈犀/蕭靈犀.md` `#210`）。本場把攤子擺到英豪府門口，就是那個笑話再演一次——**不解釋她是誰**。
> **在場：** 你、蕭靈犀、赫連娜娜；阿婆 `role5`（無立繪、無 panel）。地點：英豪府門外，傍晚。
> **稱呼：** 白鹵澤之前，她還喊「大俠」、自稱「本姑娘」；小犀喊她「娜娜姊姊」。

## 收錄 JSON

| § | JSON 檔（不可改名） | 節點 | 有文案 | entryID | zh_CN | zh_TW | 回讀日期 | 亦收錄於 |
| :-: | :--- | ---: | ---: | :--- | :-: | :-: | :--- | :--- |
| 1 | `赫連娜娜線/14一百錢.json`（Unity 對話名 `赫連娜娜線/14一百錢`） | 25 | 24 | 1–25 | **齊 24/24** | **無** | 2026-09-09 | — |

- 本檔 **1 個入口**：`#1`（帶 `title`「100錢」）。結尾 `#25`。**檔期 180 年 4 月支線**（月檔調用點 `探索事件/180年/4月.json` `#2`，與同月呂信〈第三百四十九次〉並列）。
- **一組三選一**（`#11`／`#13`／`#19`），三條各 **+20**、**沒有扣分的路**，匯合於 `#23`。
- **`[em7]` 全場一個**：`#8`（阿婆笑容不變重複道）——19 格台詞，合規。
- **任務：** `#10` 收 `CF18` entry 5 `success`（entry 1–4 已分別在礦坑／街道／遴選／武道大會）。

## 說話者對照（本檔用到的 actorID）

| actorID | 寫作 | 依據 |
| :--- | :--- | :--- |
| `MC1` | 你 | 主角標籤一律 `你`（文本創作指南 2.1a） |
| `MC22` | 赫連娜娜 | 立繪指令轉換規則 §2 |
| `MC8` | 蕭靈犀 | 立繪指令轉換規則 §2 |
| `role5` | 阿婆 | 糖炒栗子阿婆，沿用 `大地圖/雲中/街道.json` `#50`／`#53`／`#4139` |
| `role2` | 旁白 | 慣例 |
| `0` | （系統節點，不標說話者） | 慣例 |

**Panel：** 娜娜 `[panel=1]`、小犀 `[panel=2]`、旁白 `[panel=6]`；主角與阿婆不標。

---

## 1. 赫連娜娜線/14一百錢.json

> 對應 `Json/赫連娜娜線/14一百錢.json`｜25 節點（24 句文案）｜入口 `#1`、結尾 `#25`｜`zh_CN` 齊、`zh_TW` 無｜回讀 2026-09-09｜主角錢袋忘在屋裡，娜娜替他付了兩包糖炒栗子；小犀把這一百錢記進帳本。

### 1-1　英豪府・門外，傍晚

**旁白:**`#1`　[panel=6]＊（出了府門，一股糖炒栗子的甜香迎面撲來。門口那棵樹底下支著一口鐵鍋，阿婆在鍋邊翻炒，火光把她的臉照得通紅。）＊
- Sequence: `EnableDialogueBG(Yunzhong); AudioControl(PlayMusic,BGM_30);`
- 註記：180年4月上旬

**蕭靈犀:**`#2`　[panel=2]「表哥……她怎麼又在這兒？前天明明還在市集那頭呢。」
- Sequence: `SetPortrait(MC8,pic=14);`

**阿婆:**`#3`　「糖炒栗子，一包一百錢。」

**旁白:**`#4`　[panel=6]＊（你往腰間一摸。錢袋不在——今早換衣裳，擱在屋裡了。）＊

**你:**`#5`　「……錢袋落在屋裡了。」
- Sequence: `SetPortrait(MC1,pic=7);`

**赫連娜娜:**`#6`　[panel=1]「一百錢而已，阿婆，兩包栗子。」
- Sequence: `SetPortrait(MC22,pic=5);`

**旁白:**`#7`　[panel=6]＊（她把錢數到阿婆手裡。阿婆收了錢，眼睛在你和她中間停了一下，笑容還是那個笑容。）＊

**阿婆:**`#8`　「[em7]笑容不變重複道[/em7]：糖炒栗子，一包一百錢。」

**赫連娜娜:**`#9`　[panel=1]「拿去拿去，小犀也一包——別用那種看敗家子的眼神看我，草原上哪有人為這點事算帳的。」
- Sequence: `SetPortrait(MC22,pic=1); EnableCharacterExpression(1,MC22,Proud); ModifyData(Valuable,Player,SugarRoastedChestnut,5);`
- 註記：獲得糖炒栗子

**蕭靈犀:**`#10`　[panel=2]「一百錢也是錢！小犀記下了——表哥欠娜娜姊姊一百錢，栗子一包。」
- Sequence: `SetPortrait(MC8,pic=6); DisableCharacterExpression(1);`
- Script: `SetQuestEntryState("CF18", 5, "success");`
- 註記：CF18 entry 5：表哥欠娜娜一百錢（entry 1–4 已分別在礦坑／街道／遴選／武道大會）

> ⚠ **她不邀功。** 角色檔：「事後不記帳、不邀功，頂多隨口一句『大俠下次記得請回來』，說完就忘。」三條路她都不收錢、不提這件事；**記帳的是小犀**（帳房，盤纏算到銅板）——這場的笑點全在小犀的帳本上。

**◆ 玩家選擇**（`#10` → `#11`／`#13`／`#19`）

1. **`#11`** 「這錢我還妳，明日就還。」→ **分支 1**
2. **`#13`** 「那就記著吧——欠妳一包栗子。」→ **分支 2**
3. **`#19`** （不說話，剝了一顆栗子遞給她。）→ **分支 3**

> ⚙ 選項 1、2 各接一格發言；選項 3 是**不開口選項**，依 `劇情/轉成json指南.md` 豁免，不接發言格、直接接旁白 `#20`。三條都加分、都不扣分，各 **+20**。

**▶ 分支 1**

**你:**`#11`　「這錢我還妳，明日就還。」
- 註記：選項1：明日還錢

**你:**`#12`　「這錢我明日就還妳——一百錢，一個子兒都也不會少的。」
- Sequence: `SetPortrait(MC1,pic=8);`
- 註記：選項1的發言格

**赫連娜娜:**`#15`　[panel=1]「還什麼還——大俠下回請回來就是。」
- Sequence: `SetPortrait(MC22,pic=5); ModifyData(FavorabilityExp,MC22,20);`
- 註記：選項1：好感 +20

**蕭靈犀:**`#16`　[panel=2]「那小犀先記著，表哥可別想賴帳。」
- Sequence: `SetPortrait(MC8,pic=6);`

→ 接 `#23`

**▶ 分支 2**

**你:**`#13`　「那就記著吧——欠妳一包栗子。」
- 註記：選項2：欠一包栗子

**你:**`#14`　「一包栗子而已，連本帶利，回頭一次清。」
- Sequence: `SetPortrait(MC1,pic=5);`
- 註記：選項2的發言格

**赫連娜娜:**`#17`　[panel=1]「連本帶利？好，本姑娘記著——哪天沒了盤纏，就找大俠討這一包。」
- Sequence: `SetPortrait(MC22,pic=5); ModifyData(FavorabilityExp,MC22,20);`
- 註記：選項2：好感 +20

**蕭靈犀:**`#18`　[panel=2]「嘻嘻，表哥又再說大話了。」
- Sequence: `SetPortrait(MC8,pic=9);`

→ 接 `#23`

> ⚠ **豪爽不是有錢。** 她自己也會把盤纏花光（`劇情/雲中/01 街道.md` `#24`），「哪天沒了盤纏，就找大俠討這一包」是照那條寫的。

**▶ 分支 3**

**你:**`#19`　（不說話，剝了一顆栗子遞給她。）
- 註記：選項3：不開口選項，依轉成json指南豁免，不接發言格

**旁白:**`#20`　[panel=6]＊（你沒說話，剝了一顆栗子，遞到她面前。）＊

**赫連娜娜:**`#21`　[panel=1]「……本姑娘自己會剝。」
- Sequence: `SetPortrait(MC22,pic=7); ModifyData(FavorabilityExp,MC22,20);`
- 註記：選項3：好感 +20

**蕭靈犀:**`#22`　[panel=2]「娜娜姊姊，妳嘴上說不要，手倒是接得快。」
- Sequence: `SetPortrait(MC8,pic=5);`

→ 接 `#23`

> ⚠ **情竇未開。** 這是感情線第一場，好感 1 級就開；她跟他之間只留一筆「欠一包栗子」的小帳，往後可以拿出來還。**不調情、不害羞過頭**——選項三她那句「本姑娘自己會剝」＋小犀那句吐槽，就是全場最近的距離。

**◆ 合流**

**阿婆:**`#23`　「糖炒栗子，一包一百錢。」
- 註記：三條路匯流

**旁白:**`#24`　[panel=6]＊（你們往府裡走。走到門檻回頭，鍋還在翻，阿婆還在笑，笑容還是那一個。）＊
- Sequence: `OpenPanel(1, close); OpenPanel(2, close);`

> ⚙ `#25`　尾格：收立繪面板與背景
> - Sequence: `DisableAllCharacterExpression(); DisableDialogueBG(); Continue();`

> ⚠ **阿婆只有一句話。** 她在 `街道.json` 就是這麼寫的（`#50`／`#53`／`#4139` 三格同一句，笑容不變），小犀懷疑她是神仙化身也早在 `#210`。本場照抄，**一個字都不多給**——她看你們兩個那一眼，旁白只擺「停了一下」，不下判語。
> ⚠ **她付的是面子。** 「大俠你那張臉撐著就行」——她付錢是為了讓他把臉撐起來，這是角色檔「機略掛在他還想當大俠上」那一條的輕版。

### 孤兒節點

`#1`（入口，無人連入是正常的）。無缺號。

### 回讀時發現的疑點（只記錄，不改；要改走 JSON）

1. **入口沒有 `Conditions`。** 設計端寫的閘門（主線階段 ＋ `IsFavorabilityLevelPass("MC22", 1)`）在 JSON 裡是空的——**作者在 Unity 端設**。若要寫進 JSON，錨點是 `#1`。
2. **`title` 寫「100錢」（阿拉伯數字），與檔名〈一百錢〉、文案裡的「一百錢」不一致。** `title` 是作者的欄位，**AI 不動**；只是列出來讓作者確認 Unity 對話圖上要不要統一。
3. **小犀的立繪與舊創作稿全部不同**：`#10` `pic=6`（舊稿 2）、`#16` `pic=6`（舊稿 1）、`#18` `pic=9`（舊稿 14）、`#22` `pic=5`（舊稿 5，未動）。作者在 Unity 端調過，照抄。
4. **`#9`／`#10` 新增表情特效** `EnableCharacterExpression(1,MC22,Proud)` ＋ 下一格 `DisableCharacterExpression(1)`，開關成對，正常。
5. **貴重品掛 `Player`**（`#9` `ModifyData(Valuable,Player,SugarRoastedChestnut,5)`），與 2026-09-07「貴重品持有者 BUG（MC1→Player）」那一輪的口徑一致；一次給 5 份。
6. **感情線任務／entry 仍未建。** `#10` 只收既有的 `CF18` entry 5；感情線自己的任務號還是 `＿＿`（作者建，未自創）。
7. **`zh_TW` 全檔 0 筆**（`zh_CN` 24/24 齊）——**不是缺口，不必補**（Unity對話同步流程 鐵則 5；作者 2026-09-09 再確認：**匯出格式的關係，遊戲默認就是繁中，`text` 本身就是繁中，不必另填 `zh_TW`**）。
8. **節奏檢查（`文風節奏檢查.py`）：乾淨。** 零碎句、`[em7]` 1／19 格；半形標點、簡體字：**零**。

---

## 本段的引擎面

| 項目 | 內容 |
| :--- | :--- |
| 事件 | 養成模式**探索事件**（支線）；**檔期 180/4**（`探索事件/180年/4月.json` `#2` 調用）；觸發條件作者在 Unity 端設 |
| 任務 | 感情線任務 `＿＿`（作者建，未自創）；`#10` 收 `CF18` entry 5 `success` |
| 好感度 | 三條選項各 **+20**；**沒有扣分的路** |
| 貴重品 | `ModifyData(Valuable,Player,SugarRoastedChestnut,5);`（掛在 `#9`） |
| 背景 | `Yunzhong` |
| 說話者 | 阿婆 `role5`（無 panel）；小犀 `MC8` `[panel=2]`；娜娜 `MC22` `[panel=1]` |
| 音樂 | `BGM_30`（赫連娜娜出場曲，`#1`） |
