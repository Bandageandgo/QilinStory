# 赫連娜娜・端藥（好感綁定事件・白鹵澤之前）

> **本檔由 `Json/` 回讀產生，不是創作稿；遊戲內文案的唯一事實來源是 `Json/赫連娜娜線/15端藥.json`。** 改字請指名錨點（例：「`赫連娜娜線/15端藥.json` #21 這句改成…」）→ AI 改 JSON（`text` ＋ **重翻 `zh_CN`**）→ Unity 同步 → 重新回讀本檔。規則：`給AI看的指南/Unity對話同步流程.md`〈文案總覽稿〉。
> **狀態：✅ 2026-09-09 依作者從 Unity 匯出的 JSON 回讀（32 節點）。** 設計端見同資料夾 [`赫連娜娜.md`](赫連娜娜.md)〈感情線〉R2；人物本體見 [`@角色設定/赫連娜娜.md`](../../@角色設定/赫連娜娜.md)、[`@角色設定/張寧.md`](../../@角色設定/張寧.md)。
> **✅ 作者拍板：** 感情線 R2，**排在白鹵澤之前**（2026-09-07）。入口 `#1` 帶 `title`「端藥」、`Description`「180年七月下旬」，**無 `Conditions`——觸發條件作者在 Unity 端設**（設計＝主線階段 ＋ 好感 2 級）。**修羅場對象＝張寧**（作者 2026-09-02 定）。
> **⚠ 這一輪作者在 Unity 端改了三件事**（詳見檔末〈回讀時發現的疑點〉）：**①背景改成 `Clinic`**（舊創作稿寫 `Home`）、**②音樂全部填齊**（`BGM_31` → 吵起來 `BGM_38` → 張寧走後 `BGM_18`）、**③七夕分支的條件填上了 `Variable["CrossOverLover"] == 2`，並新增判斷節點 `#32`**。
> **修羅場怎麼寫（作者 2026-09-06 於七夕裁示，本場照辦）：不寫比較、不寫爭風。** 兩人吵的是**怎麼治傷**（漢藥對草原的羊湯）——那是她們「行事風格針鋒相對」的勁敵設定，不是爭他。**吃醋只准兩下：一句刀子話＋手上一個動作**，不寫「吃醋」兩個字、不寫她想什麼、不讓任何人點破。
> **這一場是什麼：** 主角從地底回來掛了彩（哪一座不點名，旁白只說「那底下」）；張寧照承包者的本分來換藥，娜娜端著羊湯闖進來。兩碗擱在他面前——**先喝哪一碗，是玩家的選擇**；好感依此分岔。
> **在場：** 你、張寧、赫連娜娜。小犀不在。地點：藥房（`Clinic`），午後。
> **稱呼：** 白鹵澤之前，娜娜還喊「大俠」、自稱「本姑娘」。**⚠ 不碰頭巾**（那是 13 檔的事；本場她戴著）。

## 收錄 JSON

| § | JSON 檔（不可改名） | 節點 | 有文案 | entryID | zh_CN | zh_TW | 回讀日期 | 亦收錄於 |
| :-: | :--- | ---: | ---: | :--- | :-: | :-: | :--- | :--- |
| 1 | `赫連娜娜線/15端藥.json`（Unity 對話名 `赫連娜娜線/15端藥`） | 32 | 30 | 1–32 | **齊 30/30** | **無** | 2026-09-09 | — |

- 本檔 **1 個入口**：`#1`（帶 `title`「端藥」）。結尾 `#31`。**檔期 180 年 7 月下旬支線**（`探索事件/180年/7月.json` `#1` 是調用點）。
- **一組三選一**（`#12`／`#14`／`#16`），三條匯合於 `#32`。
- **`#32` 是七夕條件的判斷節點**：`links` 同時指 `#25`（帶 `Conditions`）與 `#26`（else）。
- **`[em7]` 全場一個**：`#18`（把湯往案上一擱）——24 格台詞，合規。

## 說話者對照（本檔用到的 actorID）

| actorID | 寫作 | 依據 |
| :--- | :--- | :--- |
| `MC1` | 你 | 主角標籤一律 `你`（文本創作指南 2.1a） |
| `MC6` | 張寧 | 立繪指令轉換規則 §2 |
| `MC22` | 赫連娜娜 | 立繪指令轉換規則 §2 |
| `role2` | 旁白 | 慣例 |
| `0` | （系統節點，不標說話者） | 慣例 |

**Panel：** 娜娜 `MC22` `[panel=1]`、張寧 `MC6` `[panel=2]`、旁白 `[panel=6]`；主角不標。張寧離場後（`#26` 收 panel 2），`#29` 用 `SetPanel(MC22, 2, immediate)` 把娜娜搬到 `[panel=2]`。

---

## 1. 赫連娜娜線/15端藥.json

> 對應 `Json/赫連娜娜線/15端藥.json`｜32 節點（30 句文案）｜入口 `#1`、結尾 `#31`｜`zh_CN` 齊、`zh_TW` 無｜回讀 2026-09-09｜主角傷後躺在藥房，張寧來換藥、娜娜端羊湯闖進來，兩碗擱在他面前先喝哪一碗由玩家選；張寧走後，娜娜留下來。

### 1-1　藥房（`Clinic`），午後

**旁白:**`#1`　[panel=6]＊（你靠在榻上，肩頭的傷剛換過藥，布還是新的。張寧坐在案邊，把藥一包一包分開，一句話也沒有。）＊
- Sequence: `EnableDialogueBG(Clinic); AudioControl(PlayMusic,BGM_31);`
- 註記：180年七月下旬

**張寧:**`#2`　[panel=2]「藥三日一換，動作大了裂開，我不管。」
- Sequence: `SetPortrait(MC6,pic=1);`

**張寧:**`#3`　[panel=2]「在那底下逞英雄，回來躺三天——你這種傻子，我一年見七八個。」
- Sequence: `SetPortrait(MC6,pic=3);`

**你:**`#4`　「這回不是逞英雄，是躲不開。」
- Sequence: `SetPortrait(MC1,pic=6);`

**張寧:**`#5`　[panel=2]「躲不開就別衝那麼前頭。少廢話，把藥喝了。」
- Sequence: `SetPortrait(MC6,pic=1);`

**旁白:**`#6`　[panel=6]＊（門被撞開，赫連娜娜端著一碗東西進來，湯汁晃出來，燙得她直換手。）＊

**赫連娜娜:**`#7`　[panel=1]「大俠——本姑娘親手熬的羊湯！還費了我一番工夫呢，趕緊趁熱喝！」
- Sequence: `SetPortrait(MC22,pic=5);`

**張寧:**`#8`　[panel=2]「傷口剛合，你給他灌油湯？想讓他發熱，直說。」
- Sequence: `SetPortrait(MC6,pic=2); AudioControl(StopMusic); AudioControl(PlayMusic,BGM_38);`

**赫連娜娜:**`#9`　[panel=1]「你們漢人的藥苦得能把人苦死，喝了三天，臉還是白的。」
- Sequence: `SetPortrait(MC22,pic=2);`

**張寧:**`#10`　[panel=2]「白是失血，不是藥。再說了羊湯治不了失血，只能治嘴饞。」
- Sequence: `SetPortrait(MC6,pic=4);`

> ⚠ **勁敵，不是情敵。** 兩人吵的每一句都在「怎麼治傷」上：油湯發熱、藥苦、失血——沒有一句是「他該聽誰的」。角色檔寫她們「從行事風格到人生目標都能吵上半天」，本場只吵行事風格。

**旁白:**`#11`　[panel=6]＊（兩碗東西擱到你面前，一碗黑的，一碗白的，冒著一樣的熱氣。她們兩個都看著你。）＊
- Sequence: `SetPortrait(MC1,pic=12);`

**◆ 玩家選擇**（`#11` → `#12`／`#14`／`#16`）

1. **`#12`** 「先喝藥。」→ **分支 1**
2. **`#14`** 「先喝湯。」→ **分支 2**
3. **`#16`** （兩碗一起端起來。）→ **分支 3**

> ⚙ 選項 1、2 各接一格發言；選項 3 是**不開口選項**，依 `劇情/轉成json指南.md` 豁免，不接發言格、直接接旁白 `#22`。

**▶ 分支 1・先喝藥**

**你:**`#12`　「先喝藥。」
- 註記：選項1：先喝藥

**你:**`#13`　「先喝藥——湯擱著，涼了我再熱。」
- Sequence: `SetPortrait(MC1,pic=1);`

**張寧:**`#17`　[panel=2]「算你還有點腦子。」
- Sequence: `SetPortrait(MC6,pic=1); ModifyData(FavorabilityExp,MC6,20); ModifyData(FavorabilityExp,MC22,-30);`
- 註記：選項1：張寧 +20、娜娜 −30（尾格 #30 共通 +20，實收 −10）

**赫連娜娜:**`#18`　[panel=1]「[em7]把湯往案上一擱[/em7]，……涼了可別怪本姑娘。」
- Sequence: `SetPortrait(MC22,pic=2);`

> ⚠ 這兩格就是娜娜「吃醋」的全部：一句「涼了別怪我」＋湯往案上一擱，然後坐下不走。**不寫吃醋、不點破。**

→ 接 `#32`

**▶ 分支 2・先喝湯**

**你:**`#14`　「先喝湯。」
- 註記：選項2：先喝湯

**你:**`#15`　「先喝湯——藥苦，得有東西墊著。」
- Sequence: `SetPortrait(MC1,pic=1);`

**赫連娜娜:**`#19`　[panel=1]「這才對——大俠到底是有見識的。」
- Sequence: `SetPortrait(MC22,pic=5); ModifyData(FavorabilityExp,MC22,20); ModifyData(FavorabilityExp,MC6,-20);`
- 註記：選項2：娜娜 +20、張寧 −20

**張寧:**`#20`　[panel=2]「[em2]適寒溫，服一升。服已須臾，啜熱稀粥一升餘，以助藥力[/em2]；藥要溫服，服了再啜熱粥助藥力。」
- Sequence: `SetPortrait(MC6,pic=3);`

**張寧:**`#21`　[panel=2]「你倒好先灌一碗油，藥力走不到傷處——我才不在乎哩，反正傷是你的！」
- Sequence: `SetPortrait(MC6,pic=2);`

> ⚠ 這兩格就是張寧「吃醋」的全部：醫理訓完收在「反正傷是你的」。**不寫吃醋、不點破。**

→ 接 `#32`

**▶ 分支 3・兩碗一起**

**你:**`#16`　（兩碗一起端起來。）
- 註記：選項3：不開口選項，依轉成json指南豁免，不接發言格

**旁白:**`#22`　[panel=6]＊（你左手端藥，右手端湯，一口黑的、一口白的，誰也沒落下。）＊
- Sequence: `SetPortrait(MC1,pic=13);`

**張寧:**`#23`　[panel=2]「……你倒是會做人。藥三日一換，到時自己來拿。」
- Sequence: `SetPortrait(MC6,pic=1); ModifyData(FavorabilityExp,MC22,10); ModifyData(FavorabilityExp,MC6,10); SetPortrait(MC1,pic=16);`
- 註記：選項3：兩邊各 +10

**赫連娜娜:**`#24`　[panel=1]「這就叫兩全其美！大俠，往後就這麼辦。」
- Sequence: `SetPortrait(MC22,pic=5);`

→ 接 `#32`

**◆ 合流**

> ⚙ `#32`　判斷 `CrossOverLover` 變數
> - Sequence: `SetPortrait(MC1,pic=1); Continue();`
> - 分流：條件成立 → `#25`；否則 → `#26`
> - 註記：判斷CrossOverLover變數是否大於2

**▶ 七夕那句（條件成立）**

**張寧:**`#25`　[panel=2]「七月七日你提早走，我當府裡有事——該是跑去找她了。」
- Sequence: `SetPortrait(MC6,pic=3);`
- Conditions: `Variable["CrossOverLover"] == 2`

> ⚠ **一句說完就走，娜娜不接、你也不接。** 這是〈共通〉離場前另掛的七夕酸話（作者 2026-09-06 於七夕裁示），仍不點破。

→ 接 `#26`

**▶ 條件不成立**

→ 直接接 `#26`

**旁白:**`#26`　[panel=6]＊（張寧提起藥箱出去了。門一關，屋裡只剩羊湯的味道。）＊
- Sequence: `OpenPanel(2, close);`

**赫連娜娜:**`#27`　[panel=1]「……她的藥，真有那麼靈？」
- Sequence: `SetPortrait(MC22,pic=14); AudioControl(StopMusic); AudioControl(PlayMusic,BGM_18);`

**你:**`#28`　「靈是靈，就是苦。」
- Sequence: `SetPortrait(MC1,pic=3);`

**赫連娜娜:**`#29`　[panel=2]「那明兒本姑娘熬碗甜的——藥歸她，甜的歸我。」
- Sequence: `SetPortrait(MC22,pic=5); SetPortrait(MC1,pic=4); SetPanel(MC22, 2, immediate);`

**旁白:**`#30`　[panel=6]＊（湯碗見了底，窗外天色暗下來，她笑嘻嘻地看著你，還沒有走的意思。）＊
- Sequence: `OpenPanel(0, close); OpenPanel(1, close); OpenPanel(2, close); ModifyData(FavorabilityExp,MC22,20);`

> ⚠ **收尾歸娜娜。** 這是她的感情線事件，最後一拍給她：「藥歸她，甜的歸我」——她把兩碗的地盤分清楚了，這就是她的大方；「她還沒有走的意思」是旁白擺出來的事實，不是評語。

> ⚙ `#31`　尾格：收立繪面板與背景
> - Sequence: `DisableAllCharacterExpression(); DisableDialogueBG(); Continue();`

> ⚠ **張寧的病人腔。** 角色檔：對病人「刻意用嚴厲、甚至帶點恐嚇的語氣訓斥病人不愛惜身體、自找苦吃」——「動作大了裂開，我不管」「你這種傻子我一年見七八個」是那一條。**她說「行醫的」不說「作為一個醫者」**（聲口卡）；本場她一句自稱都沒有。
> ⚠ **娜娜的草原方子。** 羊是草原的東西，但她進門不講方子、不邀功親手熬，只說費工夫、趁熱喝——**不寫成她會醫**。湯是什麼，張寧下一句「灌油湯」會點，收尾旁白才寫羊湯的味道。「你們漢人的藥」是她胡漢兩邊都站一半的口吻（13 檔那句「兩邊都不算數」的前伏，但本場不點）。
> ⚠ **傷在肩頭、地方不點名。** 「那底下」可以是幽冥禁地也可以是妖林，戲不綁死。

### 孤兒節點

`#1`（入口，無人連入是正常的）。無缺號。

### 回讀時發現的疑點（只記錄，不改；要改走 JSON）

1. **背景改成 `Clinic`**（`#1`），舊創作稿寫的是 `Home`（英豪府・你的屋裡）。**戲的地點跟著變成藥房。✅ 作者 2026-09-09 裁示：維持，不處理。**
2. **音樂已填齊：** `#1` `BGM_31`（舊稿此處空著待作者填）→ `#8` 吵起來換 `BGM_38` → `#27` 張寧走後換 `BGM_18`。
3. **七夕分支的條件填上了。** `#25` 由舊稿的 `Conditions: false;`（佔位）改成 **`Variable["CrossOverLover"] == 2`**，並新增判斷節點 `#32`（`links` 同時指 `#25` 與 `#26`）。**這是作者在 Unity 端建的變數**，不是 AI 自創。`#32` 的註記寫「是否**大於** 2」、實際條件是 `== 2`，`#25` 的 `Conditions` 結尾也沒有分號——**✅ 作者 2026-09-09 裁示：維持，不處理**（`Description` 是作者的欄位，AI 不動）。
4. **`#4` 主角立繪 `pic=6`**（舊稿寫 12）；**`#23` 張寧 `pic=1`**（舊稿寫 4）並補了 `SetPortrait(MC1,pic=16)`；**`#22` 補 `SetPortrait(MC1,pic=13)`**。作者在 Unity 端調過，照抄。
5. **`#21` 台詞改長：** 舊稿「你倒好先灌一碗油，藥力走不到傷處…反正傷是你的。」→ 現行「你倒好先灌一碗油，藥力走不到傷處——**我才不在乎哩，反正傷是你的！**」。「哩」這個語尾詞全案張寧只有這一處。**✅ 作者 2026-09-09 裁示：維持，不處理。**
6. **✅ 已解決（作者 2026-09-09 裁示：讓娜娜淨 −10）。`#30` 新增的 `ModifyData(FavorabilityExp,MC22,20)` 是三條路共通的**，加在分支的加減分之外，原本會把「先喝藥」那條的娜娜拉回持平。**已把 `#17` 的娜娜由 `−20` 改成 `−30`**（只動這一條路，`#30` 的共通 +20 不動）。**現行實收**：先喝藥＝張寧 +20／娜娜 **−10**；先喝湯＝娜娜 **+40**／張寧 −20；兩碗一起＝娜娜 **+30**／張寧 +10。**⚠ 這一改要重進 Unity**（只動 `Sequence`，`text`／`zh_CN` 未動）。
7. **尾格 `#31` 仍沒有 `SetQuestEntryState`。** 感情線任務／entry 還是 `＿＿`（作者建，未自創）。
8. **`#29` 娜娜改成 `[panel=2]`**（`SetPanel(MC22, 2, immediate)`），但 `#26` 才剛 `OpenPanel(2, close)` 把張寧的面板收掉——順序上沒有問題（收完再進），只是 `#27`／`#28` 期間她還在 panel 1，到 `#29` 才搬家。**✅ 作者 2026-09-09 裁示：維持，不處理。**
9. **`zh_TW` 全檔 0 筆**（`zh_CN` 30/30 齊）——**不是缺口，不必補**（Unity對話同步流程 鐵則 5；作者 2026-09-09 再確認：**匯出格式的關係，遊戲默認就是繁中，`text` 本身就是繁中，不必另填 `zh_TW`**）。
10. **節奏檢查（`文風節奏檢查.py`）：** 碎句 2 句——`#12`「先喝藥。」`#14`「先喝湯。」**都是選單句**，依體例不算。`[em7]` 1／24 格，合規。半形標點、簡體字：**零**。

---

## 本段的引擎面

| 項目 | 內容 |
| :--- | :--- |
| 事件 | 養成模式**探索事件**（支線）；**檔期 180/7 下旬**（`探索事件/180年/7月.json` `#1` 調用）；觸發條件作者在 Unity 端設 |
| 任務 | 感情線任務 `＿＿`（作者建，未自創）；尾格 `#31` 未收 entry |
| 好感度 | 分支：先喝藥 `#17`＝張寧 +20、娜娜 **−30**；先喝湯 `#19`＝娜娜 +20、張寧 −20；兩碗一起 `#23`＝各 +10。**共通：`#30` 娜娜再 +20**。**實收：先喝藥 娜娜 −10／先喝湯 +40／兩碗一起 +30**（作者 2026-09-09 定「先喝藥那條讓娜娜淨 −10」） |
| 七夕分支 | `#32` 判斷 → `#25`（`Variable["CrossOverLover"] == 2`）／else `#26` |
| 背景 | `Clinic` |
| 說話者 | 張寧 `MC6` `[panel=2]`；娜娜 `MC22` `[panel=1]`（`#29` 起 `[panel=2]`） |
| 音樂 | `BGM_31`（`#1`）→ `BGM_38`（`#8`）→ `BGM_18`（`#27`） |
