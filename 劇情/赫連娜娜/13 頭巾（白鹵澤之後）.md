# 赫連娜娜・頭巾（好感綁定事件・白鹵澤之後）

> **本檔由 `Json/` 回讀產生，不是創作稿；遊戲內文案的唯一事實來源是 `Json/赫連娜娜線/13頭巾.json`。**
> 改字請指名錨點（例：「`13頭巾.json` `#58` 這句改成…」）→ AI 改 JSON → Unity 同步 → 重新回讀本檔。
> **⚠ 本檔原是正式稿（2026-09-12 作者審畢、依大綱「甲＋丙」整份重寫）。2026-09-16 作者在 Unity 改完並匯出，以 JSON 為準整份重生成**——作者改了台詞、刪掉 12 顆節點、換了場二的地點、補上立繪與表情特效、填了音樂。創作期的設計理由與紅線留在下面。

> 對應 JSON：`Json/赫連娜娜線/13頭巾.json`｜節點 **65**（有文案 59）｜entryID 1–77（**不連號**：`#23`／`#24`／`#25`／`#30`–`#35`／`#61`／`#69`／`#70` 已由作者刪除）｜**`zh_TW`／`zh_CN` 各 59 句全齊**｜回讀：2026-09-16（同日作者第二次匯出後重讀）
> 單一入口 `#1`（`title`：頭巾）、單一尾格 `#77`；無重複 entryID、無斷鏈、無孤兒。
> **觸發門檻寫在 `#1` 的 `Description`**：「必須要完成CF45,3 ，娜娜好感度超過3Lv」——即白鹵澤走完（`CF45` entry 3 `success`）＋娜娜好感 **3 級**（舊案寫 2 級，作者改高了一級）。條件由作者在 Unity 端掛，節點不寫 `Conditions`。
> **本場不動任何任務狀態**：`#77` 的 `Script` 是空的，全檔沒有 `SetQuestEntryState`。**作者 2026-09-16 確認是刻意的**——任務與門檻都照現況，不再報。
> **兩場**：①**雲中市集**（`#1`–`#45`，晌午，`Yunzhong`）——回敬布販（威嚇→經濟兩關）→ 挑布；②**府裡・入夜**（`#47`–`#76`，`Home_Night`）——她教他草原的包法。`#46` 是黑幕轉場。
> **音樂與音效**：`#1` 開 `BGM_19`＋人聲 `Others_Talking`，`#3` `StopSFX`；`#45` `StopMusic`；`#47` 換 `BGM_18` 管整個夜戲；`#77` 關背景圖。
> **擲骰四處**：`#10` 威嚇 8（`Manual`）、`#13` 經濟 8（**`Auto`**）、`#36` 調情 **14**、`#62` 調情 **12**。（創作稿四處都寫 8，作者調高了兩個調情檢定。）
> **好感**：市集三段各 `+10`（`#12` 威嚇過、`#15` 經濟過、`#17` 收尾）；`#41` 調情成功 `+30`；夜戲 `#65`／`#68` 各 `+30`。**全場沒有扣分**（`#44` 的 −20 作者第二次匯出時拿掉了，`#27` 的「扣好感」註記也一併刪除）。專長：威嚇 `+10`、經濟 `+10`、調情 `+10`×2。**單場最高 ＋90。**
> **立繪**：市集她戴著借來的灰布，用**有帽版**；夜戲那塊灰布已經取下（`#71` 明寫），整段用**露角版** `pic=17`–`26`（`立繪指令轉換規則.md` §4.4.1）。**兩邊沒有混用。**
> **在場**：你、赫連娜娜。**布販 2026-09-16 起有 `actorID`：`role15`**（創作稿時期是無 ID 的 `[panel=6]` 路人）。

> **以下是創作期的設計理由與紅線，作者 2026-09-12 定，仍然有效：**
> **選定大綱：甲＋丙**——白天在雲中市集挑一塊新的（她那塊埋在白鹵澤了），夜裡她教他草原的包法。**前半是「交付」，後半是「互換」。**
> **為什麼是這兩場：** 洞窟已經把「他看見」演完，13 不能再演一次被看見。挑布把遮蓋物的所有權從她手上鬆開一次（**伏虎石那一拍她解下來扔掉的，就是他挑的這塊**）；教包法把方向倒過來——**她先替他遮**，因為他也有一個走到哪都被人盯著看的記號。
> **她能說的上限：** 胡漢兩邊都算她外人、她怕自己不算人（09-08 作者定向）。角、末卷、斬心訣、伏虎石去不去，**這一場都不碰**——洞窟講完了，這裡只講「算不算人」這一層。
> **⚠ `#73`「往後大概是不用按了。至少在你跟前不用。」只說「在你跟前」**——對天下解頭巾那一拍是伏虎石的，這裡不越界（`@設定_伏虎石.md`）。
> **⚠ `#2` 種下「按頭巾」這個小動作**（她隔一會兒就伸手按一下），`#71`–`#73` 手抬到一半停住就是它的收束。
> **⚠ `#68`「那咱們倆湊一塊，算個什麼？」是 R4〈下一處〉的引信**——她那時候會自己把答案說出來。
> **⚑ 稱呼：** 創作稿原本讓「本姑娘」在市集回來一次（舊 `#35`），**那一格已被作者刪掉，現行全檔沒有「本姑娘」**，兩場都用 `[var=PlayerFirstName]` 或直接對話。

---

## 全場逐格（依 `Json/赫連娜娜線/13頭巾.json` 回讀，2026-09-16）

**旁白:**`#1`　[panel=6]＊（雲中的市集，晌午。布攤一張挨著一張，麻的、葛的、染過色的，一匹一匹立在架上。）＊
- Sequence: `EnableDialogueBG(Yunzhong); AudioControl(PlaySFX,Others_Talking); AudioControl(PlayMusic,BGM_19);`
- 註記：必須要完成CF45,3 ，娜娜好感度超過3Lv

**旁白:**`#2`　[panel=6]＊（她在同一攤前站了半天了。頭上那塊灰布是府裡借的，短了半尺，鬢角壓不住，她隔一會兒就伸手按一下。）＊

**赫連娜娜:**`#3`　[panel=1]「這塊太薄，風一吹就掀。這塊花得像要去嫁人——你們漢地的布，怎麼沒一塊是能戴出門的。」
- Sequence: `SetPortrait(MC22,pic=9); EnableCharacterExpression(1,MC22,Nervous_2); AudioControl(StopSFX);`

**你:**`#4`　「妳原來那塊，是什麼樣的？」
- Sequence: `DisableCharacterExpression(1); SetPortrait(MC1,pic=12); EnableCharacterExpression(0,Player,Question);`

**赫連娜娜:**`#5`　[panel=1]「粗麻，靛青的，我阿母縫過一道邊。戴了七年——結果在白鹵澤給弄丟了……」
- Sequence: `DisableCharacterExpression(0); SetPortrait(MC22,pic=3); EnableCharacterExpression(1,MC22,Pain);`

**布販:**`#6`　[panel=1]「姑娘是胡人吧？我這幾匹都是好貨，可姑娘要的不過是塊遮頭的，隨便扯一塊也就夠了。」
- Sequence: `DisableCharacterExpression(1); SetPanel(MC22, 2, immediate);`

**旁白:**`#7`　[panel=6]＊（她把手上那塊放回架上，指頭在邊沿壓了壓，沒回嘴。）＊

### ◆ 玩家選擇

1. `#8`　[em2][威嚇檢定][/em2]「你再說一遍。」
   - 註記：選項一：威嚇檢定（難度 8）→ 過了再接經濟檢定
2. `#9`　（不作聲。）
   - 註記：選項二：不作聲（不開口選項）

### ▶ 選 `#8`

**你:**`#8`　[em2][威嚇檢定][/em2]「你再說一遍。」
- 註記：選項一：威嚇檢定（難度 8）→ 過了再接經濟檢定

> ⚙ `#10`　威嚇檢定擲骰節點（難度 8）
> - Sequence: `SetContinueMode(false); SetContinueMode(original)@Message(EndRoll); Continue()@Message(EndRoll); BeginDiceRoll(Manual,IntimidationCheck,8);`

**你:**`#11`　「你再說一遍——方才那句話，是衝著誰說的？」
- Sequence: `DisableCharacterExpression(1); SetPortrait(MC1,pic=13); EnableCharacterExpression(0,Player,Anger);`
- 註記：發言格＝擲骰後的過渡格

- 分流：`#12`（IsPassDice() == true）／`#19`（IsPassDice() == false）

### ▶ 分支 `#12`　條件：`IsPassDice() == true`

**旁白:**`#12`　[panel=6]＊（那人往後縮了半寸，手在布上抹了兩下，話沒接上。）＊
- Sequence: `DisableCharacterExpression(0); ModifyData(FeatExp,Player,Intimidation,10); ModifyData(FavorabilityExp,MC22,10);`
- Conditions: `IsPassDice() == true;`

> ⚙ `#13`　經濟檢定擲骰節點（難度 8）
> - Sequence: `SetContinueMode(false); SetContinueMode(original)@Message(EndRoll); Continue()@Message(EndRoll); BeginDiceRoll(Auto,EconomicDevelopmentCheck,8);`

**你:**`#14`　「這麻布起毛，經緯也歪。這種貨在雲中賣幾個錢，我剛巧知道。」
- Sequence: `SetPortrait(MC1,pic=10);`
- 註記：發言格＝第二關的過渡格

- 分流：`#15`（IsPassDice() == true）／`#18`（IsPassDice() == false）

### ▶ 分支 `#15`　條件：`IsPassDice() == true`

**旁白:**`#15`　[panel=6]＊（他臉上那副笑換了一張。價錢從嘴裡滾出來，一路砍到一半，還說是給姑娘算便宜的。）＊
- Sequence: `ModifyData(FeatExp,Player,EconomicDevelopment,10); ModifyData(FavorabilityExp,MC22,10);`
- Conditions: `IsPassDice() == true;`

**你:**`#16`　「不必了——隔壁也有一樣的貨。」
- Sequence: `SetPortrait(MC1,pic=18); EnableCharacterExpression(0,Player,Proud);`

**赫連娜娜:**`#17`　[panel=1]「……沒想到你還有這一面。不過我倒是喜歡。」
- Sequence: `DisableCharacterExpression(0); SetPortrait(MC22,pic=5); ModifyData(FavorabilityExp,MC22,10);`

**赫連娜娜:**`#21`　[panel=1]「……走吧，這攤沒有我要的。」
- Sequence: `DisableCharacterExpression(1); SetPortrait(MC22,pic=3);`

**旁白:**`#22`　[panel=6]＊（往前走過兩攤。這家布堆得少，老闆蹲在後頭打盹，沒抬頭看人。）＊

**你:**`#28`　「就這塊靛青的吧——粗麻，邊上還能再縫一道，就跟妳原來那塊一模一樣！」
- Sequence: `SetPortrait(MC1,pic=1);`

**赫連娜娜:**`#29`　[panel=1]「你記性倒好。那就這塊吧——只是這塊，沒有阿母縫的那道邊……」
- Sequence: `SetPortrait(MC22,pic=3);`

### ◆ 玩家選擇

1. `#26`　[em2][調情檢定][/em2]「邊上那道線，我來縫。」
   - 註記：調情檢定（難度 14）
2. `#27`　「照樣子再縫一道不就得了。」

### ▶ 選 `#26`

**你:**`#26`　[em2][調情檢定][/em2]「邊上那道線，我來縫。」
- 註記：調情檢定（難度 14）

> ⚙ `#36`　調情檢定擲骰節點（難度 14）
> - Sequence: `SetContinueMode(false); SetContinueMode(original)@Message(EndRoll); Continue()@Message(EndRoll); BeginDiceRoll(Manual,FlirtingCheck,14);`

**你:**`#37`　「這塊邊還沒縫。妳那塊是妳阿母縫的——這塊我縫。」
- Sequence: `SetPortrait(MC1,pic=15);`
- 註記：發言格＝擲骰後的過渡格

**赫連娜娜:**`#38`　[panel=1]「你會縫？拿劍的手縫布，縫出來怕是像條蜈蚣。」
- Sequence: `SetPortrait(MC22,pic=5); EnableCharacterExpression(1,MC22,Idea);`

**你:**`#39`　「方才妳說，那攤沒有妳要的。那妳要的是什麼樣——說出來我就去找。」
- Sequence: `DisableCharacterExpression(1); SetPortrait(MC1,pic=12);`

- 分流：`#40`（IsPassDice() == true）／`#42`（IsPassDice() == false）

### ▶ 分支 `#40`　條件：`IsPassDice() == true`

**旁白:**`#40`　[panel=6]＊（她張了張嘴，沒接上。半晌，才把那塊布往你懷裡一塞。）＊
- Sequence: `ModifyData(FeatExp,Player,Flirting,10);`
- Conditions: `IsPassDice() == true;`

**赫連娜娜:**`#41`　[panel=1]「……縫壞了算你的。針線我有，明兒拿給你。」
- Sequence: `SetPortrait(MC22,pic=7); EnableCharacterExpression(1,MC22,Nervous); ModifyData(FavorabilityExp,MC22,30);`

**赫連娜娜:**`#45`　[panel=1]「[em7]把布疊好，塞進懷裡[/em7]，……回府再說吧，這東西得先洗一遍——新布硬得很。」
- Sequence: `DisableCharacterExpression(1); SetPortrait(MC22,pic=1); AudioControl(StopMusic);`

> ⚙ `#46`　轉場：雲中市集 → Home（夜）
> - Sequence: `SetContinueMode(false); PlayFeelFeedback(FadeInOut,1,0.5,1,#000000,1); OpenPanel(0, close)@1; OpenPanel(1, close)@1; EnableDialogueBG(Home_Night)@1; SetContinueMode(original)@2.5; Continue()@2.5;`

**旁白:**`#47`　[panel=6]＊（回到府裡，她抱著那塊布一溜煙就跑了。等再看見她，外頭已經入夜，那塊布洗過了，還濕著，就捧在她手上。）＊
- Sequence: `AudioControl(PlayMusic,BGM_18);`

**赫連娜娜:**`#48`　[panel=1]「[var=PlayerFirstName]，坐好別動。我教你一個包法——草原上的包法，一塊布能頂半件衣裳。」
- Sequence: `SetPortrait(MC22-2,pic=5); EnableCharacterExpression(1,MC22,Proud);`

**你:**`#49`　「教我？這東西我又不戴。」
- Sequence: `DisableCharacterExpression(1); SetPortrait(MC1,pic=4); EnableCharacterExpression(0,Player,Question);`

**赫連娜娜:**`#50`　[panel=1]「往後你要陪我往北邊走，就得戴。你這一頭白髮，在草原上比在漢地還打眼。」
- Sequence: `DisableCharacterExpression(0); SetPortrait(MC22-2,pic=5); EnableCharacterExpression(1,MC22,Meditate);`

**旁白:**`#51`　[panel=6]＊（她繞到你身後，兩手一拉一折，濕布沿著額角壓下來。她的手指是冰的。）＊
- Sequence: `DisableCharacterExpression(1);`

**赫連娜娜:**`#52`　[panel=1]「小時候我嫌它勒得慌。我阿母說，勒著才知道自己是誰家的。」
- Sequence: `SetPortrait(MC22-2,pic=1);`

**赫連娜娜:**`#53`　[panel=1]「你這頭髮，生下來就是白的？」
- Sequence: `SetPortrait(MC22-2,pic=6); EnableCharacterExpression(1,MC22,Question);`

**你:**`#54`　「生下來就是白的。村子燒的那年我還小，一路流浪過來，走到哪兒都有人回頭。」
- Sequence: `DisableCharacterExpression(1); SetPortrait(MC1,pic=3); EnableCharacterExpression(0,Player,Pain);`

**你:**`#55`　「兩個孩子在路上，又頂著這麼一頭白——討得到的多半是白眼，不是飯。」
- Sequence: `DisableCharacterExpression(0); SetPortrait(MC1,pic=3);`

**旁白:**`#56`　[panel=6]＊（她手上的動作停了一下，又接著折下去。）＊

**赫連娜娜:**`#57`　[panel=1]「在部裡沒人怕我頭上這個，可也沒人真把我當尋常孩子。到了漢地更省事，見了就當妖。」
- Sequence: `SetPortrait(MC22-2,pic=3);`

**赫連娜娜:**`#58`　[panel=1]「兩邊都沒把我當個尋常人，我有時候倒真會想——我到底算不算個人。這話我沒跟誰說過。」
- Sequence: `SetPortrait(MC22-2,pic=4);`

### ◆ 玩家選擇

1. `#59`　[em2][調情檢定][/em2]「那就別遮了。」
   - 註記：選項一：調情檢定（難度 12）
2. `#60`　「跟我一樣，沒地方回的人。」
   - 註記：選項二：跟我一樣，沒地方回的人

### ▶ 選 `#59`

**你:**`#59`　[em2][調情檢定][/em2]「那就別遮了。」
- 註記：選項一：調情檢定（難度 12）

> ⚙ `#62`　調情檢定擲骰節點（難度 12）
> - Sequence: `SetContinueMode(false); SetContinueMode(original)@Message(EndRoll); Continue()@Message(EndRoll); BeginDiceRoll(Manual,FlirtingCheck,12);`

**你:**`#63`　「那就別遮了——我瞧著好看，遮了才可惜。」
- Sequence: `DisableCharacterExpression(1); SetPortrait(MC1,pic=15);`
- 註記：發言格＝擲骰後的過渡格

- 分流：`#64`（IsPassDice() == true）／`#66`（IsPassDice() == false）

### ▶ 分支 `#64`　條件：`IsPassDice() == true`

**赫連娜娜:**`#64`　[panel=1]「……你這人，話說得倒輕巧。」
- Sequence: `SetPortrait(MC22-2,pic=9); EnableCharacterExpression(1,MC22,Nervous); ModifyData(FeatExp,Player,Flirting,10);`
- Conditions: `IsPassDice() == true;`

**赫連娜娜:**`#65`　[panel=1]「那你就看著——看到哪天你不看了，我再包回去。」
- Sequence: `DisableCharacterExpression(1); SetPortrait(MC22-2,pic=7); ModifyData(FavorabilityExp,MC22,30);`

**旁白:**`#71`　[panel=6]＊（她抬手要往頭上按——手抬到一半，停住了。那塊借來的灰布早就取下來了。）＊
- Sequence: `DisableCharacterExpression(1);`

**赫連娜娜:**`#72`　[panel=1]「……習慣了，從小按到大。」
- Sequence: `SetPortrait(MC22-2,pic=1); EnableCharacterExpression(1,MC22,Meditate);`

**赫連娜娜:**`#73`　[panel=1]「往後大概是不用按了。至少在你跟前不用。」
- Sequence: `DisableCharacterExpression(1); SetPortrait(MC22-2,pic=1);`

**旁白:**`#74`　[panel=6]＊（她站起身，理了理衣襬，走到門邊又停住。）＊

**赫連娜娜:**`#75`　[panel=1]「明日再教你一回——看你自己包不包得住。」
- Sequence: `SetPortrait(MC22-2,pic=5); EnableCharacterExpression(1,MC22,Idea);`

**旁白:**`#76`　[panel=6]＊（門在身後合上，屋裡靜了下來。她方才坐的地方，地上留著一小攤水漬。）＊
- Sequence: `DisableCharacterExpression(1); OpenPanel(0, close); OpenPanel(1, close);`

> ⚙ `#77`　尾格：收立繪面板與背景；
> - Sequence: `DisableAllCharacterExpression(); DisableDialogueBG(); Continue();`

（本條結束）

### ▶ 分支 `#66`　條件：`IsPassDice() == false`

**赫連娜娜:**`#66`　[panel=1]「說得倒容易——你走在我旁邊，捱眼色的又不只我一個。」
- Sequence: `SetPortrait(MC22-2,pic=4); EnableCharacterExpression(1,MC22,Meditate);`
- Conditions: `IsPassDice() == false;`

→ 接 `#71`

### ▶ 選 `#60`

**你:**`#60`　「跟我一樣，沒地方回的人。」
- 註記：選項二：跟我一樣，沒地方回的人

**你:**`#67`　「跟我一樣——沒地方回的人。算不算人我不知道，反正這條路上不只妳一個。」
- Sequence: `DisableCharacterExpression(1); SetPortrait(MC1,pic=1);`

**赫連娜娜:**`#68`　[panel=1]「……原來你也沒地方回。那咱們倆湊一塊，算個什麼？」
- Sequence: `SetPortrait(MC22-2,pic=5); EnableCharacterExpression(1,MC22,Nervous); ModifyData(FavorabilityExp,MC22,30);`

→ 接 `#71`

### ▶ 分支 `#42`　條件：`IsPassDice() == false`

**赫連娜娜:**`#42`　[panel=1]「行了行了，我自己有針。你那雙手還是留著拿劍吧。」
- Sequence: `SetPortrait(MC22,pic=5);`
- Conditions: `IsPassDice() == false;`

→ 接 `#45`

### ▶ 選 `#27`

**你:**`#27`　「照樣子再縫一道不就得了。」

**你:**`#43`　「照樣子再縫一道不就得了——針腳一樣，看著也沒差。」
- Sequence: `SetPortrait(MC1,pic=6);`

**赫連娜娜:**`#44`　[panel=1]「……不必了，我自己縫。」
- Sequence: `SetPortrait(MC22,pic=3);`

→ 接 `#45`

### ▶ 分支 `#18`　條件：`IsPassDice() == false`

**旁白:**`#18`　[panel=6]＊（他不慌，張口報了個價，說這是雲中的行情，愛買不買。你一時接不上話。）＊
- Conditions: `IsPassDice() == false;`

→ 接 `#21`

### ▶ 分支 `#19`　條件：`IsPassDice() == false`

**旁白:**`#19`　[panel=6]＊（那人半點沒縮，反倒把嗓門提得更高，轉頭招呼起別的客人。）＊
- Sequence: `DisableCharacterExpression(0);`
- Conditions: `IsPassDice() == false;`

→ 接 `#21`

### ▶ 選 `#9`

**你:**`#9`　（不作聲。）
- 註記：選項二：不作聲（不開口選項）

**旁白:**`#20`　[panel=6]＊（你沒有開口。她低頭理了理鬢角，把壓不住的那幾綹頭髮塞回布底下。）＊
- Sequence: `DisableCharacterExpression(1);`

→ 接 `#21`

---

## 引擎面

| 項目 | 內容 |
| :--- | :--- |
| JSON | `Json/赫連娜娜線/13頭巾.json`：**65 節點，單一入口 `#1`、單一尾格 `#77`**。2026-09-12 依創作稿轉出 77 節點，**2026-09-16 作者在 Unity 改完匯出、刪成 65 節點**（刪 `#23`–`#25`、`#30`–`#35`、`#61`、`#69`、`#70`） |
| 觸發 | 寫在 `#1` 的 `Description`：**`CF45` entry 3 完成 ＋ 娜娜好感 3 級**。由作者在 Unity 掛，節點不寫 `Conditions` |
| 任務 | **本場不收任何 entry**——`#77` 的 `Script` 是空的。**作者 2026-09-16 確認刻意** |
| 場景 | 場一 `Yunzhong`（晌午）；`#46` 黑幕轉場；場二 **`Home_Night`**（入夜的府裡；創作稿原訂馬廄 `HorseFarm`，作者改掉了）。`#1` 開背景、`#77` 關 |
| 音樂 | `#1` `BGM_19`＋`PlaySFX,Others_Talking`，`#3` `StopSFX`；`#45` `StopMusic`；`#47` `BGM_18`（夜戲全段） |
| 擲骰 | `#10` 威嚇 8（`Manual`）→ 過了再 `#13` 經濟 8（**`Auto`**）；`#36` 調情 **14**（市集挑布）；`#62` 調情 **12**（夜戲）。專長：威嚇 +10、經濟 +10、調情 +10×2 |
| 好感 | `#12`／`#15`／`#17` 各 +10（市集三段，全過 +30）；`#41` +30（調情成功）；`#65`／`#68` 各 +30。**單場最高 +90，全場無扣分** |
| 立繪 | 市集有帽版；夜戲露角版 `MC22-2,pic=1–11`（§4.4.2；舊寫法 `MC22,pic=17–27` 已作廢）。布販 `role15`（`#6`），同格 `SetPanel(MC22, 2, immediate)` 把娜娜移到位置 2，之後她的台詞仍寫 `[panel=1]`——**作者 2026-09-16 確認刻意** |
| 表情特效 | 位置 1 開 12 次／關 16 次，位置 0 開 5 次／關 6 次；`#77` `DisableAllCharacterExpression()` 收尾 |
| 檔期 | **180/6 支線**（2026-09-17 作者由 10 月移來），佔位殼 `Json/探索事件/180年/6月.json`（2 節點：`#1` 頭巾、`#3` 蠶女白馬）；月檔 `劇情/180年事件/06月(支)頭巾、蠶女白馬.md` |

---

## 回讀時發現的疑點（只記錄，未動 JSON；要改請指名）

> **2026-09-16 作者裁示並第二次匯出：第 1、2 條刻意如此，不再報；第 3 條已改；第 5 條隨扣分拿掉而消失；第 4 條經立繪指南更正確認沒錯。**

1. **`CF45` entry 4 沒有接、門檻只寫在 `#1` 的 `Description`——作者確認刻意，照現況。**
2. **`#6` `SetPanel(MC22, 2, immediate)` 之後娜娜的台詞仍寫 `[panel=1]`——作者確認刻意，照現況。**
3. ~~`#74` 場二已改到府裡，卻還留著馬廄的「草屑」~~ **✅ 作者已改**：現為「＊（她站起身，理了理衣襬，走到門邊又停住。）＊」。
4. ~~`#14` 主角立繪 `pic=10`（名目總表的「哭」）~~ **✅ 不是錯**：`立繪指令轉換規則.md` 已更正（2026-09-16），**`MC1` 的 `10` 是「腹黑壓火」**——閉眼笑著、一手按胸，火氣壓在裡面，正是「這麻布起毛，經緯也歪。這種貨在雲中賣幾個錢，我剛巧知道。」這種壓價拆穿的臉；立繪自帶怒意，不另疊表情特效。
5. ~~挑布那一關沒有不扣分的保底選項~~ **✅ 已不成立**：作者拿掉了 `#44` 的 −20，`#27`「照樣子再縫一道不就得了。」現在是不加不扣的一條。
