# 白鹵澤（文案總覽）

> **本檔由 `Json/` 回讀產生，不是創作稿；遊戲內文案的唯一事實來源是 `Json/大地圖/白鹵澤.json`。**
> 改字請指名錨點（例：「`大地圖/白鹵澤.json` #4 這句改成…」）→ AI 改 JSON → Unity 同步 → 重新回讀本檔。
> **地點設定（表裡三層、結構三段、紅線）見同資料夾 `@設定_白鹵澤.md`**；卷的鏈見 `劇情/赫連娜娜/@設定_爹的筆記.md`。
> **⚠ 地城本身還沒做。** 這份對話是先寫好等作者掛的互動點（同妖林／幽冥禁地的做法）。

> 對應 JSON：`Json/大地圖/白鹵澤.json`｜節點 5（有文案 3）｜entryID 1–5｜zh_CN：有｜建檔：2026-09-06
> 入口：`#1`｜終點：`#5`

## 1. 澤心舊營：取得《遊方雜記・末卷》

> 目前全檔只收這一個互動點——**撿到末卷**（`WanderingNotesPage7`），架構同妖林／幽冥禁地：旁白一句環境 → 她一句 → 便籤 → 她一句總結 → 尾格收 `CF45` entry 3 `success`（作者 2026-09-07 改：與〈死地生綠〉合成一顆）。
> 戲文與寫作紅線見 `劇情/赫連娜娜/09 爹的筆記（CF45）.md` 份七。

**旁白:**`#1`　[panel=6]＊（火塘用石頭壓著，鍋埋了半截在鹽裡。鹽殼上刻著一個記號——一個圓，裡頭幾道分格，是天機盤的樣子。記號旁邊壓著一卷紙。）＊
- Sequence: `ModifyData(Valuable,MC22,WanderingNotesPage7,1);`

**赫連娜娜:**`#2`　[panel=1]「這……是寫給我的。你也看吧——裡頭的事，你都知道了。」
- Sequence: `SetPortrait(MC22,pic=9);`

> ⚙ `#3` 功能節點（無文案）：貴重品便籤，甲式攔截
> - Sequence: `SetContinueMode(false); SetContinueMode(original)@Message(EndMemo); Continue()@Message(EndMemo); ShowValuableMemo(WanderingNotesPage7);`

**赫連娜娜:**`#4`　[panel=1]「[em7]把卷子捲好，收進懷裡[/em7]，……他知道我會來。這一路我追得這麼慢，他倒是早就算準了。」
- Sequence: `SetPortrait(MC22,pic=3); `

> ⚙ `#5` 功能節點（無文案，`links: []`）：尾格
> - Sequence: `OpenPanel(0, close); OpenPanel(1, close); Continue();`
> - Script: `SetQuestEntryState("CF45", 3, "success");`
> - 註記：尾格：收立繪面板；**收 CF45 entry 3**（白鹵澤・末卷；entry 3 由 `赫連娜娜線/12死地生綠.json` `#37` 開 `active`）

## ⏳ 這座地城還缺的

澤面、殼那兩段的互動點（設定檔有場面、沒有對話）；雜兵與戰鬥 ID（設定：**澤心無 Boss**）；背景與事件圖（澤面／殼／舊營三張）；`MapLock` 由 `赫連娜娜線/12死地生綠.json` `#36` 開。
